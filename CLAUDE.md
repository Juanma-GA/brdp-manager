# CLAUDE.md — Contexto para Claude Code

Este fichero describe la arquitectura y patrones del proyecto para que Claude Code tenga contexto completo antes de hacer cualquier cambio.

## ⚠️ El AACF (framework ATEXIS) tiene prioridad sobre este fichero

Este proyecto está sujeto al **ATEXIS AI-Assisted Coding Framework (AACF)** (repo `Juanma-GA/cursoFSD`, carpeta `Proyecto-CCMS-Nav/aacf/`; copia local leída el 2026-09-18, `aacf/VERSION = 2.0.1`). **Donde el AACF y este `CLAUDE.md` entren en conflicto, gana el AACF** — este fichero se corrige para reflejarlo, nunca al revés.

- **Hard Rules HR0–HR21** (`aacf/rules/atexis-hard-rules.md`) son no negociables — autocomprobar cualquier cambio contra ellas antes de darlo por terminado (ver también `aacf/agents/code-reviewer.agent.md` y `security-reviewer.agent.md` para el criterio de revisión). Resumen relevante para este proyecto: HR1 (nunca localStorage/sessionStorage para estado autoritativo — Postgres es la fuente de verdad), HR6 (nunca truncar contenido de calidad, resumir con LLM), HR7 (nunca un fallback que degrade silenciosamente, escalar), HR8 (nada hardcodeado), HR9 (soft-delete / confirmación explícita antes de borrar), HR10 (deep-merge, nunca shallow-merge de config anidada), HR15/HR21 (i18n + humanizar texto de UI, nunca tokens crudos), HR20 (UI de mutación optimista).
- El framework se sirve normalmente vía el MCP `aacf_fetch` (`http://10.117.139.1:8200/mcp`, solo red corporativa/VPN); esa herramienta **no está disponible en este entorno de ejecución remoto**, así que se trabaja con la copia local del repo `cursoFSD` — puede estar desactualizada. Verificar `aacf/VERSION` contra el MCP en cuanto haya acceso, y no mezclar ficheros de versiones distintas del framework si difiere.
- **Conflicto detectado y todavía sin resolver**: el patrón "Hooks de datos" descrito más abajo en este fichero (ver "Patrones a seguir") usa localStorage como caché de estado autoritativo, lo que viola HR1. No se ha tocado código todavía — ver "Auditoría de cumplimiento AACF (pendiente)" al final de este fichero para el detalle y el plan.

## ⚠️ Rama actual: `v2-multiproyecto` — backend real distinto al descrito abajo

Todo el resto de este fichero (arquitectura Express+SQLite, sin auth, un solo proyecto) describe **v1**. El trabajo activo desde hace muchas rondas vive en la rama `v2-multiproyecto`, que tiene un backend **completamente distinto**, ya en producción dentro del repo:

- **Backend real**: `backend/` — FastAPI + Postgres (`asyncpg` + SQLAlchemy async) + Alembic (`backend/alembic/versions/`, 13 migraciones a fecha de hoy). Nada de SQLite ni Express para el backend de datos (el frontend sigue siendo Vite/React).
- **Multi-proyecto real** con auth JWT: access token en el body de `/api/auth/login`, refresh token en cookie HttpOnly (nunca localStorage — ver `backend/app/api/routes/auth.py`). Roles `admin` / `editor` / `viewer` por proyecto vía `UserProjectRole` (`backend/app/models/user_project_role.py`) — un admin global puede todo, un editor solo en sus proyectos.
- **Estructura backend**: `backend/app/models/*` (BRDP, Project, User, UserProjectRole, RuleApproval, BRDPHistory, BRDPCatalog, ImportJob, AppSettings, RefreshToken), `backend/app/api/routes/*` (un fichero por recurso: `projects.py`, `brdps.py`, `approvals.py`, `brdp_import.py`, `trash.py`, `brdp_catalog.py`, `auth.py`, `users.py`, `app_settings.py`, `similar.py`, `suggestion_feedback.py`, `llm_proxy.py`, `notes.py`, `config.py`, `validate_brex.py`), `backend/app/services/*` (`import_jobs.py`, `embeddings.py`, `history.py`, `rule_formats.py`), `backend/app/repositories/brdp_repository.py` (capa de acceso a BRDP — todas las queries pasan por aquí, incluido el filtro de soft-delete `ACTIVE_BRDP_FILTER`).
- **Soft-delete real**: borrar una BRDP la manda a Papelera (`deleted_at`/`deleted_by`), no la elimina. Borrado permanente vía `/api/trash/{id}` (admin, o editor si es de su proyecto).
- **Import de Excel** (`backend/app/services/import_jobs.py` + `backend/app/schemas/brdp_import.py`) es un job asíncrono en background (`BackgroundTasks`, no bloqueante): `/import/analyze` (fase 1, solo lectura, devuelve avisos) → `/import/apply` (fase 2, devuelve `job_id`, se sondea `/import/status/{job_id}`). Un `running` job antiguo (>60 min, `STALE_JOB_MINUTES`) se marca `failed` automáticamente al leerlo (`_reap_if_stale`) — cubre crashes; una interrupción limpia (`asyncio.CancelledError`, p.ej. un `--reload` de uvicorn) tiene su propio handler explícito porque `CancelledError` hereda de `BaseException`, no de `Exception`, desde Python 3.8.
- **Documentación de diseño original** (útil como contexto histórico, pero YA DESACTUALIZADA frente al código real tras ~106 rondas de iteración — no la trates como fuente de verdad, el código y los tests son la fuente de verdad): `docs/v2/01-arquitectura-y-estructura.md` (snapshot de v1, la base de la decisión de reescribir), `docs/v2/02-analisis-aacf-requisitos-no-cumplidos.md`, `docs/v2/03-especificacion-v2-para-claude-code.md` (spec original de v2).

### Cómo levantar el entorno de desarrollo real (v2)

```bash
service postgresql start   # o `service postgresql status` si ya está arrancado
cd backend && source .venv/bin/activate && uvicorn app.main:app --host 0.0.0.0 --port 8000   # backend, puerto 8000
cd /home/user/brdp-manager && npx vite --port 5173 --strictPort                              # frontend, puerto 5173
```

El `.venv` de `backend/` ya existe con todas las dependencias instaladas. `uvicorn` normalmente se lanza SIN `--reload` en este entorno — si editas código Python del backend con el servidor ya arrancado, hay que reiniciarlo a mano (matar el proceso real, `ps aux | grep "[u]vicorn app.main:app"` para el PID exacto — `pkill -f uvicorn` se automata a sí mismo porque su propio argv contiene el patrón) para que recoja los cambios; Vite sí hace HMR normal sobre `src/`.

Hay un proyecto de desarrollo sembrado ("Demo Project (S1000D 4.2)") y un admin de pruebas (`admin@example.com` / `AdminTest123!`, ver `backend/scripts/seed_dev_data.py`) — usarlos para verificación con navegador real en vez de crear datos nuevos cada vez, salvo que el propio caso de prueba lo requiera (y luego limpiar lo creado).

### Cómo verificar cambios (no hay test runner JS)

- **Backend**: `cd backend && source .venv/bin/activate && python -m pytest -q` (a fecha de hoy: 208 tests, Postgres real, sin mocks salvo el transporte HTTP de Mistral).
- **Frontend**: no existe vitest/jest ni script `test` en `package.json`. Toda verificación de UI se hace con scripts Node ad hoc usando `playwright-core` (Chromium real en `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`) contra el Vite dev server real y Postgres real — login real, `fetch` con refresh de cookie, screenshots. No inventar un framework de test nuevo; seguir ese patrón.
- `npm run lint` (ESLint) y `npm run build` (Vite) deben quedar limpios antes de dar por cerrada cualquier tarea de frontend.

### Últimos cambios relevantes (para no repetir investigación)

- **`_normSpace()` en `brexToSchematron.js`** ahora respeta el contenido entre comillas (no colapsa espacios significativos dentro de literales de cadena), reutilizando el patrón de escaneo "char a char, trackeando si se está dentro de comillas" que ya usan `_isSafePattern`/`_splitTopLevel` en el mismo fichero. Si aparece otro sitio del código que normaliza espacios sobre texto que puede contener literales entre comillas, reutilizar esta misma técnica, no inventar otra.
- **`rule_override`** en `ImportRowResult` (`backend/app/schemas/brdp_import.py` + `_classify_row` en `backend/app/services/import_jobs.py`): mismo patrón que `catalog_override` ya existente, pero para la columna Rule — avisa en la fase de análisis si un `action == "update"` va a reemplazar una Rule ya existente por una distinta. La comparación ya NO normaliza espacios sobre el string completo (ese enfoque tenía el mismo bug que `_normSpace()` de arriba: colapsaba espacios significativos dentro de un valor de atributo junto con la indentación). Ahora es una comparación estructural real con `lxml` (`_rule_xml_structurally_equal`/`_elements_structurally_equal` en `import_jobs.py`, mismo patrón de `<root>` tolerante que `_xml_well_formed_error` en `approvals.py`): tag, atributos y texto/tail no-blanco comparados byte a byte; solo se ignora texto/tail puramente en blanco entre hermanos (indentación real). Frontend: `ProjectConfigPage.jsx` (`ruleOverrideRows`), i18n en `src/i18n/index.js` (`config.dataManagement.ruleOverride*`, `summaryRuleOverrides`).
- **Renombrado de Project Standards** (migración `0012_rename_project_standards.py`, mismo patrón que `0002_canonicalize_project_standard.py`): los 7 strings canónicos pasan a ser 6 — `BREX — S1000D 3.0.1/4.1/4.2` → `S1000D 3.0.1/4.1/4.2`, `Schematron 1.0 — DITA` → `DITA 1.3`, y **`Schematron 1.0 — S1000D` desaparece como standard independiente** (migra a `S1000D 3.0.1`, el BREX real que siempre generó por dentro). `STANDARD_TO_RULE_FORMAT` (`rule_formats.py`/`ruleFormats.js`) ya no tiene entrada `SCH-S1000D`; `_CATALOG_STANDARD_ALIASES`/`_resolve_catalog_standard()` en `brdp_catalog.py` se eliminaron (ya no hace falta alias, no hay un standard Schematron separado al que apuntar).
- **Selector BREX / Schematron en `GeneratePage.jsx`**: para un proyecto `S1000D 3.0.1/4.1/4.2` ya no hay un formato de generación fijo por standard — un selector (`outputKind`, `BREX` | `Schematron (XPath 2.0)`) elige entre generar el BREX real o su conversión determinista a Schematron, alimentados por el MISMO conjunto de reglas aprobadas bajo el formato BREX del standard (`BREX-3.0.1`/`BREX-4.1`/`BREX-4.2`) — sin re-aprobación al cambiar. `generateBREXSch.js` ya no asume `generateBREX301` siempre: acepta `options.baseGenerator` (por defecto `generateBREX301`, retrocompatible) para que el llamador elija el generador base real (`generateBREX`/`generateBREX41`/`generateBREX301`) según el standard del proyecto. `DITA 1.3` no tiene selector (sin cambios, sigue sin equivalente BREX).
- **`GeneratePage.module.css` — layout sin scroll de página**: `.page` sigue ahora el mismo patrón flex que `RecordsPage.module.css`/`ProjectConfigPage.module.css` (`flex:1; min-height:0; display:flex; flex-direction:column`, con `overflow-y:auto` como fallback — nunca se activa en el caso normal, solo evita contenido inalcanzable si la ventana queda más baja que el propio formulario). `.card` (formulario) lleva `flex-shrink:0` para no comprimirse nunca; `.outputCard` es el item `flex:1` que ocupa el resto (mismo rol que `.tableWrap`), y dentro de él `.xmlOutput` pasó de `max-height:420px` fijo a `flex:1;min-height:0` — es el único hijo que crece/hace scroll interno (`.outputMeta`/`.xsdSection`/`.outputActions` llevan `flex-shrink:0` para quedar fijos). Verificado con Playwright a 1366×800 (portátil típico): 25 reglas no fuerzan scroll de página, 1 regla no deja el panel absurdamente vacío, y encoger la ventana por debajo de la altura del formulario activa el fallback de scroll en vez de cortar contenido sin forma de llegar a él.
- **`DITA 1.3` habilitado en `STANDARD_TO_RULE_FORMAT`** (`rule_formats.py`/`ruleFormats.js`, nueva entrada `"DITA 1.3": "SCH-DITA"`): antes de esta ronda DITA era el único standard SIN entrada en el mapa ("no hay equivalente BREX"), pero `generateSchematronDITA.js` sí tiene su propio camino de generación 100% determinista (sin LLM) que inyecta verbatim el `rule_xml` de cada fila `approved` bajo formato `SCH-DITA` — sin la entrada en el mapa, ningún BRDP de un proyecto DITA podía llegar nunca a `approved` (el import rechazaba cualquier Rule/Rule Status con "This project's standard has no rule format"). Auditados los 7 puntos que dependían del mapa: `import_jobs.py` (genérico, sin nada específico de BREX), `similar.py`/Suggest Rule (antes daba 400 explícito para DITA, no "sin resultados"; ahora indexa `SCH-DITA` real), `import_brdp_catalog.py` (`DITA 1.3` ya estaba en `_KNOWN_STANDARDS` desde el renombrado, nada que tocar), `ProjectConfigPage.jsx`/`RecordsPage.jsx` (el gating de UI ya era genérico sobre `STANDARD_TO_RULE_FORMAT[standard]`, "funciona solo" con la entrada nueva).
  - **Bug real encontrado (no asumido) verificando el editor manual**: `_xml_well_formed_error` (`approvals.py`) y `checkWellFormed` (`generateBREX.js`) envolvían el fragmento en un `<root>` plano sin namespaces — un `<sch:pattern>`/`<sch:rule>`/`<sch:assert>` real usa el prefijo `sch:` que NUNCA se autodeclara (solo se declara una vez, en el `<sch:schema xmlns:sch="...">` que añade `finalizeSchematronDocument` al final), así que todo Schematron nativo genuino fallaba como "no bien formado" al guardarlo o importarlo. Arreglado con `_wrap_rule_xml_fragment`/`wrapRuleXmlFragment` (backend y frontend, mismo criterio, mantenerlos en sync): detecta genéricamente cualquier prefijo `prefix:name` usado en el fragmento (nunca hardcodea "sch") y lo declara con una URI ficticia solo para que el parser lo acepte — mismo patrón ya usado por `_buildHeader` en `brexToSchematron.js` para namespaces de documento completo, aplicado aquí a un fragmento suelto. Usado también por `_rule_xml_structurally_equal` (mismo problema al comparar dos fragmentos Schematron para `rule_override`).
  - **Verificación end-to-end con datos reales (Navantia S80, `nav_dtm_xpath2_import_v3.xlsx`: 36 BRDPs, 8 `Verified` con Schematron nativo SIN prefijo -- `<pattern>`/`<rule>`/`<assert>` a secas, caso real distinto del `<sch:pattern>` prefijado de los few-shot curados) encontró y arregló DOS bugs reales más, ninguno hipotético:
    1. **Namespace roto para contenido sin prefijo** (`generateSchematronDITA.js`): `finalizeSchematronDocument` envolvía los bloques en `<sch:schema xmlns:sch="...">` SOLO con el prefijo `sch:` declarado, sin namespace por defecto. Un bloque `rule_xml` inyectado verbatim SIN prefijo (el caso real de Navantia) quedaba entonces en "sin namespace" al anidarse ahí -- confirmado con `lxml`/`DOMParser`: `<pattern>` parseaba como elemento suelto en vez de `{http://purl.oclc.org/dsdl/schematron}pattern`. XML bien formado, pero invisible para cualquier procesador Schematron/XSLT2 real. Arreglado declarando TAMBIÉN `xmlns="http://purl.oclc.org/dsdl/schematron"` por defecto en el wrapper (legal en XML: un elemento puede estar en el scope de un prefijo y del namespace por defecto a la vez apuntando a la misma URI) -- mismo fix en el fallback JS y en `public/schematron-dita-schema-summary.json`'s `sch_header.root_open`. Además, `checkWellFormedSchematron`'s `checkDuplicateIds`/`checkRulesAndChecks`/`lintVocabulary` tenían el prefijo `sch:` hardcodeado en sus regexes -- para contenido sin prefijo esto significaba CERO patterns/rules/checks encontrados y por tanto "0 errores" no porque estuviera confirmado correcto, sino porque el check nunca miraba ese contenido. Arreglado con una constante `SCH = "(?:sch:)?"` reutilizada en esas regexes (NO en `checkRootHeader`, que sólo mira el wrapper determinista, siempre con prefijo). De paso, con el check ya mirando contenido real, `lintVocabulary` sacó falsos positivos legítimos de XPath 2.0 (`string-join`, `if`/`then`/`else`, `for`, `number`, `doc-available`) que no estaban en `XPATH_FUNCTIONS`/`XPATH_KEYWORDS` -- **comportamiento concreto confirmado antes/después con las mismas 8 reglas reales**: antes de añadirlos, `result.vocabularyWarnings` traía líneas como `"BRDP-EXT-00001: uses unconfirmed element/attribute 'string-join'"` para cada una de esas 6 palabras (avisos no bloqueantes, pero visibles en la UI de Generate bajo "vocabulary warnings") -- no eran errores de bien-formación (`result.valid` ya daba `true`), sino ruido del lint heurístico tratando una función/keyword XPath 2.0 estándar como si fuera un nombre de elemento/atributo DITA sin confirmar. Tras añadirlos a `XPATH_FUNCTIONS`/`XPATH_KEYWORDS`, la misma llamada sobre el mismo contenido da `vocabularyWarnings: []` -- cero avisos, nada más cambió (ni `result.valid`, ni el XML generado, ni el resto de warnings reales como `footnote`/`video`/`audio` de otro fixture, que siguen apareciendo sin tocar).
    2. **`isConfigComplete` en `GeneratePage.jsx` bloqueaba el botón Generate PERMANENTEMENTE para todo proyecto DITA 1.3**: dependía de `project.project_config?.modelIdentCode` sin importar el standard, pero la página de Project Configuration de DITA (`DITA_FIELDS` en `ProjectConfigPage.jsx`) solo muestra/guarda `projectName` -- `modelIdentCode` nunca se puede rellenar para DITA (ni por la UI ni por los defaults de creación del backend, `_DEFAULT_PROJECT_CONFIG`). Confirmado en vivo: un proyecto DITA recién creado nunca tiene `modelIdentCode` en su `project_config`, así que el botón Generate quedaba deshabilitado para siempre, bloqueando toda la feature en su último paso. Arreglado: `isConfigComplete` ahora exige `projectName` para DITA, `modelIdentCode` para los tres standards S1000D reales (sin cambio ahí).
    - **Hallazgo adicional, ya corregido en el mismo momento**: `finalizeSchematronDocument` también aplicaba `escapeSchTestAttributes`/`sanitizeXmlCommentBodies` (una "segunda capa de defensa" heredada de una arquitectura anterior basada en LLM) sobre el documento COMPLETO ya ensamblado, incluidos los bloques `rule_xml` verbatim de reglas ya aprobadas. Confirmado con datos reales: esto rompía silenciosamente la garantía "verbatim" -- un comentario XML propio y legítimo dentro de una regla real de Navantia (`<!-- La columna de exención ... -->`) perdía su espacio inicial/final al pasar por `sanitizeForXmlComment`. Dado que TODO `rule_xml` que llega a `approved` ya pasó por `_xml_well_formed_error`/`checkWellFormed` en el momento de guardarse (tanto import como editor manual, doble verificación cliente+servidor), y `buildTraceabilityComment` ya sanitiza su propio texto generado, esta segunda pasada global no defendía de nada en la arquitectura actual (100% determinista, sin LLM en el camino principal) y solo podía corromper contenido ya válido. Eliminadas ambas funciones (`escapeSchTestAttributes`, `escapeAttrLiteral`, `sanitizeXmlCommentBodies`) por no tener ya ningún llamador.
    - Scripts de verificación ad hoc (mismo patrón que `test-schematron-dita.mjs`, mantenidos en el repo): `scripts/verify-dita-navantia-e2e.mjs` (flujo completo real: crear proyecto, importar el xlsx real vía UI, comprobar las 8 aprobaciones byte a byte, editor manual real, Generate real, Suggest Rule real, caso límite Verified-sin-Validar, `rule_override` con contenido real), `scripts/verify-sch-namespace-fix.mjs` (namespace + regexes contra datos reales, incluido un documento mixto prefijado+sin prefijo), `scripts/verify-dita-config-complete-fix.mjs` (regresión del fix de `isConfigComplete`), `scripts/mock-mistral-embed-server.mjs` (mock local del endpoint de embeddings de Mistral para poder correr un Apply real con filas `Validated` en este entorno sin clave real de Mistral -- mismo criterio "mockear solo el transporte HTTP de Mistral" que usa la suite de pytest).
    - **Nota sobre "byte a byte"**: la comparación real encontró que el import vía UI (SheetJS en el navegador) preserva `\r\n` tal cual está en la celda de Excel, mientras que `openpyxl` (usado para extraer los datos de referencia con Python) normaliza `\r\n`→`\n` al parsear XML, por spec de XML 1.0. No es un bug de la app -- ambos representan el mismo salto de línea, y las comparaciones estructurales reales (`_rule_xml_structurally_equal`) ya normalizan esto vía parseo XML -- pero si se escribe otro script de verificación que compare contenido extraído con Python contra lo que trae la app, normalizar `\r\n`→`\n` en ambos lados antes de comparar, o el diff sale en falso.
- **Ronda siguiente sobre el mismo generador DITA — filename con `modelIdentCode` + comentarios de trazabilidad con UUID en vez de identificador legible** (`GeneratePage.jsx` + `generateSchematronDITA.js`): mismo patrón de bug que `isConfigComplete` de la ronda anterior — un campo específico de S1000D asumido para todos los standards, sin caso especial para DITA. Dos bugs reales, confirmados con el mismo proyecto Navantia S80:
  1. **`handleDownload`** calculaba `mic` (el prefijo del nombre de fichero) SIEMPRE desde `project_config.modelIdentCode`, que para DITA nunca existe (ver el fix de `isConfigComplete`) -- confirmado con un fichero real: `UNKNOWN_2026-09-19_dita.sch`, mientras que el `<sch:title>` interno del propio documento sí traía el nombre real del proyecto. Arreglado centralizando la lógica "qué campo identifica el proyecto según el standard" en un único valor derivado, `configIdentifierValue` (`projectName` para DITA, `modelIdentCode` para los tres S1000D reales), reutilizado tanto por `isConfigComplete` como por `handleDownload` -- evita una tercera copia divergente de ese mapeo. Confirmado en vivo: el mismo proyecto ahora descarga `Navantia S80_2026-09-19_dita.sch`. Caso límite confirmado por construcción, no por prueba adicional: un proyecto DITA sin `Project Name` relleno nunca puede llegar a esta pantalla con el botón Generate habilitado (lo bloquea `isConfigComplete`, mismo valor derivado), así que el fallback a `'UNKNOWN'` en el nombre de fichero es inalcanzable vía UI normal -- mismo estatus que el fallback ya existente para S1000D con `modelIdentCode` vacío, no un caso nuevo a resolver.
  2. **`buildTraceabilityComment`** (`generateSchematronDITA.js`) usaba `${brdp.id}` -- el UUID interno de Postgres del objeto `brdp` tal como llega desde `GET /api/projects/{id}/brdps` (`BRDPOut`, que sí trae también `identifier`) -- en vez de `${brdp.identifier}`, el id legible (`BRDP-EXT-00010`). Confirmado con un `.sch` real: los comentarios de trazabilidad salían como `<!-- 3b32ddb1-...: no se pudo generar... -->` en vez de `<!-- BRDP-EXT-00010: no se pudo generar... -->`. Arreglado cambiando esa única línea (único call site de `buildTraceabilityComment`, dentro del bucle principal de `generateSchematronDITA()`) -- **NO tocadas** las líneas equivalentes de `buildFewShotBlock`/`buildDeterministicBlockFromFewShot` (`${ex.id}`/`${entry.id}`), que operan sobre el catálogo curado de few-shot donde `id` YA es el identificador legible, no un UUID -- son objetos de origen distinto, no el mismo bug. Verificado con las 28 filas `To Do` reales de Navantia: cada comentario de trazabilidad muestra el identificador correcto correspondiente a esa fila exacta (no solo "tiene forma de BRDP-EXT-algo"), y ningún UUID de Postgres aparece en ningún punto del documento generado.
  - Ambos fixes verificados end-to-end con el mismo script `scripts/verify-dita-navantia-e2e.mjs` (ampliado esta ronda: comprueba el nombre de fichero real vía el evento `download` real de Playwright, y las 28 identidades `To Do` una por una en vez de una comprobación puntual) -- sin cambios en backend, suite de 205 tests sigue en verde.
- **Mensaje de vocabulario específico para el mecanismo `ambito-mapa` de XMetal + reimport que ya no recalcula embeddings sin necesidad**:
  1. **`lintVocabulary()` (`generateSchematronDITA.js`)** — `dosier`/`ficha`/`mapa`/`exists` son el vocabulario real del mecanismo de segunda pasada de dossier virtual de XMetal (`ambito-mapa.sch`), no vocabulario DITA, así que salían siempre como `"uses unconfirmed element/attribute 'X'"` en cualquier proyecto que use ese mecanismo -- ruido esperado, no un problema real, pero indistinguible del resto de avisos genuinos. Arreglado con un segundo conjunto, `XMETAL_AMBITO_MAPA_VOCAB` (deliberadamente NO fusionado en `EXTRA_KNOWN_NAMES`/`known` -- eso habría hecho desaparecer el aviso del todo, y sigue sin poder confirmarse con certeza que un proyecto concreto usa ese mecanismo): cuando el nombre está en ese conjunto, el aviso cambia a `"uses 'X' — expected in the XMetal ditamap second-pass mechanism (ambito-mapa.sch), not core DITA vocabulary; verify manually if this project doesn't use that mechanism"` -- sigue siendo un aviso visible, nunca una afirmación de certeza. Verificado con contenido sintético que reproduce el vocabulario real (no se disponía del fichero/captura real de Navantia-XMetal con `BRDP-EXT-00004`/`BRDP-ENV-00001` en este entorno): las 4 palabras dan el mensaje ampliado, un nombre genuinamente no confirmado (`frobnicate`) sigue con el mensaje genérico de siempre, y la regresión existente (`footnote`/`video`/`audio` del fixture curado) no se contamina.
  2. **Reimport recalculaba embeddings sin comprobar si algo había cambiado de verdad** (`import_jobs.py`, rama de actualización): confirmado en código, no percepción -- cualquier fila con `Proposal Status = Validated` disparaba `_compute_brdp_embedding()` (llamada real a Mistral) SIEMPRE, aunque `title`/`definition`/`proposal`/`validation` fueran idénticos byte a byte a lo ya almacenado. En un proyecto de miles de filas `Validated` (SOPTE), reimportar el mismo fichero sin cambios costaba los mismos ~70 minutos que la importación original, en vano. Arreglado: `_classify_row` calcula ahora `unchanged` (nuevo campo en `ImportRowResult`/`ImportApplyResultSummary`, default `0`/`False` para compatibilidad con jobs ya persistidos en Postgres antes de este cambio) comparando los CUATRO campos entrantes (título/definición ya resueltos contra el catálogo, igual que escribe `run_import_job`) contra lo almacenado; `run_import_job` reutiliza ese mismo booleano ya calculado en el análisis (no lo recalcula) y, si es `True`, se salta reasignación de campos, escritura en `brdp_history` Y la llamada a `_compute_brdp_embedding` -- la fila cuenta en una categoría `unchanged` nueva, separada de `updated`, para que el resumen final sea honesto. Completamente independiente de `Rule`/`rule_override` (misma fila puede ser `unchanged` en sus 4 campos core y a la vez disparar `rule_override` si solo cambia la Rule -- verificado explícitamente con un test dedicado, confirmando además que un cambio SOLO en Rule nunca recalculaba embedding de forma correcta, ahora sí). Caso `create` (BRDP nueva) sin cambios, sigue calculando embedding siempre que `Validated` -- no hay nada previo con qué comparar. `computeApplyEta`/el aviso de Fase 1 (`ProjectConfigPage.jsx`) ahora excluyen las filas `unchanged` del conteo de filas `Validated` que disparan coste de embedding, y muestran `"N rows unchanged (no embedding recalculation)"` en el resumen de análisis ANTES de aplicar -- honestidad pedida explícitamente ("el usuario no tiene forma de saber de antemano cuántas de las Validated son en realidad idénticas"), no solo en el resumen final tras aplicar.
     - Verificado con Playwright real: importar el fichero real de Navantia (36 filas, todas `Validated`) → 36 llamadas reales al mock de Mistral; reimportar EXACTAMENTE el mismo fichero → 0 llamadas adicionales (contador expuesto vía `GET /calls` en `scripts/mock-mistral-embed-server.mjs`, nuevo endpoint de test), resumen de Fase 1 y de Apply mostrando `36 rows unchanged`. 3 tests nuevos en `test_brdp_import.py`: no-op puro (cero llamadas, cero `brdp_history`), cambio SOLO de Rule (sigue sin recalcular embedding, la Rule sí se actualiza), y cambio real de un campo core (sí recalcula, cuenta como `updated`) -- 208 tests backend en verde.
- **`DITA 1.3` se divide en `DITA 1.3 Xpath2.0` y `DITA 1.3 Xpath3.0`** (migración `0013_split_dita_xpath_standards.py`, mismo patrón que `0002`/`0012`): bug real encontrado leyendo `generateSchematronDITA.js` -- el `queryBinding` del `<sch:schema>` ensamblado estaba hardcodeado a `"xslt2"` en TRES sitios reales (plantilla del documento final, gate de validación de bien-formación, prompt de fallback LLM), así que un proyecto con Rule real en XPath 3.0 habría generado siempre un header incorrecto. A diferencia de Schematron 1.0 — S1000D (fusionado en un único selector en la migración 0012 porque ahí la Rule es el MISMO dato pasado por un conversor determinista BREX→Schematron), aquí cada BRDP trae Rule nativa hand-authored por separado según el dialecto XPath, sin conversor compartido -- hacían falta dos proyectos reales de todos modos, así que se decidió con el usuario partir el standard en dos (en vez de un campo de config compartido) para que la versión sea visible en la propia columna "Project standard" de la lista de proyectos sin abrir cada uno.
  - Ficheros tocados: `src/pages/ProjectsPage.jsx` (`STANDARD_OPTIONS`, ahora 7 entradas), `src/pages/ProjectConfigPage.jsx` (`fieldsForStandard` usa `standard.startsWith('DITA 1.3')`, ambos flavors siguen usando `DITA_FIELDS`), `src/pages/GeneratePage.jsx` (`isDITA` comprueba ambos strings; `standard` real del proyecto ahora viaja en `options` hacia `formatDef.run`), `backend/app/services/rule_formats.py` + `src/constants/ruleFormats.js` (`STANDARD_TO_RULE_FORMAT`: las dos entradas nuevas mapean al mismo `"SCH-DITA"`), `backend/scripts/import_brdp_catalog.py` (`_KNOWN_STANDARDS`, mensaje de error auto-actualizado con `len(...)` en vez de "6" hardcodeado), `backend/app/models/project.py` (comentario de columna), `backend/app/api/routes/projects.py` (docstring stale), `backend/scripts/enrich_dita_catalog.py` (docstring stale).
  - **Migración**: además de `projects.standard` (único caso: `'DITA 1.3'` → `'DITA 1.3 Xpath2.0'`, sin fila real `Xpath3.0` que migrar -- confirmado con el usuario que no existe ese proyecto todavía), también migra `brdp_catalog.standard` -- hallazgo propio, no pedido explícitamente: mismo patrón ya establecido por la migración 0012 (tabla `_TABLES_WITH_STANDARD` compartida), necesario porque `import_jobs.py`'s `_load_catalog()` hace un match de igualdad plano contra `project.standard`, así que sin migrar el catálogo, `catalog_override` habría dejado de dispararse en silencio para todo proyecto DITA tras el rename. `downgrade()` colapsa AMBOS strings nuevos de vuelta a `'DITA 1.3'` (documentado explícitamente en el propio `downgrade()`, no dejado sin cubrir). Verificado con filas sintéticas insertadas por SQL directo, en ambas direcciones (upgrade→downgrade→upgrade), luego limpiadas.
  - **`generateSchematronDITA.js`**: nuevo helper `queryBindingForStandard(standard)` (`"xslt3"` solo si `standard === "DITA 1.3 Xpath3.0"`, `"xslt2"` en cualquier otro caso incluido el string viejo pre-migración -- nunca adivina `xslt3`). Threaded como parámetro `queryBinding` (default `"xslt2"`, retrocompatible) por los tres sitios reales: `finalizeSchematronDocument` (nuevo placeholder `{QUERY_BINDING}` en `sch_header.root_open` de `public/schematron-dita-schema-summary.json`, mismo mecanismo que el `{PROJECT_TITLE}` ya existente), `checkRootHeader`/`checkWellFormedSchematron` (regex ya no hardcodea `xslt2`), y `buildSchematronPrompt`/`generateSingleRule` (prompt de fallback LLM). `generateSchematronDITA()` calcula `queryBinding` desde `options.standard` y lo pasa a los dos call sites reales (finalize + check).
  - **Hallazgo de investigación, no fix**: el "prompt de fallback LLM" (`buildSchematronPrompt`/`generateSingleRule`, uno de los 3 sitios con `xslt2` hardcodeado) resultó ser, tras trazar la cadena de llamadas (`generateSuggestedRule.js`'s caso `"SCH-DITA"` ← `useChat.js` ← `ChatPanel.jsx`), código MUERTO -- `useChat()` no se llama desde ningún sitio de `src/`, `ChatPanel` no se importa desde ninguna página. El botón real "Suggest Rule" de `RecordsPage.jsx` usa un mecanismo totalmente distinto y más simple (`GET /similar?kind=rule`, búsqueda pgvector pura sin LLM en el backend, más un prompt few-shot genérico inline que nunca toca `generateSchematronDITA.js`). Se arregló igualmente el hardcode (lo pedía el encargo explícitamente) pero queda documentado que no es verificable vía interacción real de UI, solo mediante llamada directa a la función (mismo criterio que ya usa `test-schematron-dita-lets.mjs` para probar internals del generador).
  - **Vocabulario XPath 3.0**: análisis de `NAME_TOKEN_RE` char a char (no asumido) confirmó que el operador flecha `=>` es pura puntuación y nunca puede tokenizarse como "nombre" (nada que arreglar), que `map` ya estaba en `EXTRA_KNOWN_NAMES` por coincidencia (es también el elemento real `<map>` de DITA, así que `map{...}`/`map {...}` nunca dio falso positivo), y que el único hueco real era `array {...}` (con espacio antes de la llave, sintaxis XPath 3.0 válida, SÍ cae en el lookahead de la regex) -- confirmado escribiendo y ejecutando un test real, no solo razonando. Arreglado con `XPATH3_ONLY_VOCAB = new Set(["array"])`, fusionado en `known` dentro de `lintVocabulary` SOLO `if (queryBinding === "xslt3")` -- nunca amplía el vocabulario conocido de un proyecto Xpath2.0.
  - **Verificación**: `scripts/verify-dita-xpath3-querybinding.mjs` (nuevo, contenido SINTÉTICO -- confirma `queryBinding="xslt3"` para un proyecto Xpath3.0, `"xslt2"` sin cambios para Xpath2.0 y para el string viejo pre-migración por defecto, cero falsos positivos de vocabulario para `=>`/`map{}`/`array{}`/`string-join` de un solo argumento en un proyecto Xpath3.0, y que `array` SIGUE marcándose como no confirmado en un proyecto Xpath2.0 -- sin fuga de vocabulario entre dialectos). `scripts/verify-standards-rename.mjs` reescrito para 7 standards (antes 6) con ambos DITA nuevos y sin el string viejo. Los tres scripts de regresión DITA ya existentes (`verify-dita-navantia-e2e.mjs`, `verify-dita-config-complete-fix.mjs`, `verify-reimport-unchanged-skips-embeddings.mjs`) actualizados a `selectOption("DITA 1.3 Xpath2.0")` y re-ejecutados en verde contra el fichero real de Navantia S80 -- confirma que todas las features DITA de rondas anteriores siguen funcionando igual tras el split. `backend/tests/test_standard_consistency.py::EXPECTED_STANDARD_COUNT` (6→7) es el único sitio que la suite de pytest hizo fallar de verdad al no haberlo actualizado en el primer pase -- detectado y corregido por la propia suite, no por inspección manual.
- **Cierre con datos reales de `nav_dtm_xpath3_import.xlsx`** (36 filas, 9 `Verified`, entregado por el usuario en la ronda siguiente a la del split): confirma, con Schematron XPath 3.0 nativo real de Navantia (no sintético), lo que la ronda anterior solo había verificado con contenido construido a mano.
  - **Los constructos nombrados explícitamente en el encargo (`=>`, `map{}`/`array{}`, `string-join` de un argumento) NO aparecen ni una vez en las 9 reglas `Verified` reales** -- confirmado por grep exhaustivo antes de tocar nada, no asumido. En su lugar, el fichero real usa OTRA sintaxis XPath 3.0 genuina que el encargo no anticipaba pero sí pedía cubrir explícitamente ("y cualquier otra que se encuentre al verificar con datos reales"): `fn:head()` (BRDP-EXT-00007/00008/00009), la expresión de función inline `function($t as element()) as xs:string { let ... return ... }` (BRDP-EXT-00004, variables `escalonPlan`/`escalonProc`) y el operador de mapeo simple `!` (BRDP-EXT-00004/00007/00008/00009, `$docPrec//cmd ! normalize-space(.)`) -- las tres son sintaxis de orden superior/función-item que NO existe en XPath 2.0. Además, el fichero expuso un hueco de vocabulario más amplio y NO ligado a la versión de XPath: `exists`, `empty`, `distinct-values`, `analyze-string`, `local-name` (funciones XPath 2.0 estándar, nunca antes confirmadas pese a la ronda Navantia S80/Xpath2.0 anterior), `element` (node-kind test) y `as` (declaración de tipo de secuencia, válida desde XPath 2.0).
  - **Arreglado en `generateSchematronDITA.js`** distinguiendo por si el gap es realmente exclusivo de 3.0 o no: `exists`/`empty`/`distinct-values`/`analyze-string`/`local-name`/`element` → añadidos sin condición a `XPATH_FUNCTIONS` (igual que `string-join`/`number`/`doc-available` de la ronda S80: válidos en 2.0 y 3.0, así que nunca deben limitarse a proyectos Xpath3.0); `as` → añadido sin condición a `XPATH_KEYWORDS` (mismo motivo). `head`/`function` → añadidos a `XPATH3_ONLY_VOCAB` (ahora `["array", "head", "function"]`), ya que `fn:head` y las expresiones de función inline SÍ son exclusivas de 3.0 -- confirmado con un test aislado que un proyecto Xpath2.0 sigue marcando ambos como vocabulario no confirmado (sin fuga entre dialectos), mientras que el mismo contenido bajo Xpath3.0 da `vocabularyWarnings: []`.
  - **Hallazgo de datos, no de código, encontrado por el propio check**: las filas `BRDP-EXT-00007`, `00008` y `00009` del fichero real comparten Rule byte a byte idéntica (mismo `id="p-BRDP-EXT-00007"` en el `sch:pattern`, mismos tres `sch:assert` `BRDP-EXT-00007a`/`00007`/`00007b`) -- aparenta ser un error de copiar-pegar al preparar el fichero de prueba, no un problema de la app. Al ensamblarse las 9 reglas en un único documento, `checkWellFormedSchematron` detecta correctamente `"Duplicate sch:assert/sch:report id(s): BRDP-EXT-00007a, BRDP-EXT-00007, BRDP-EXT-00007b"` -- comportamiento CORRECTO del check (está haciendo su trabajo), no un bug a arreglar; no bloquea la descarga (el badge de error es solo informativo, `result.xml` sigue disponible). Si el usuario reimporta una versión corregida del fichero con Rule realmente distinta por fila, este aviso desaparece solo.
  - **Verificado con Playwright real** (`scripts/verify-dita-xpath3-real-navantia.mjs`, sustituye la cobertura sintética de `verify-dita-xpath3-querybinding.mjs` para el cierre real): proyecto `DITA 1.3 Xpath3.0` real creado, las 36 BRDPs importadas (27 `To Do`, 9 con `rule_approvals.status = "approved"` -- Rule Status vive en `rule_approvals`, no en el propio BRDP, confirmado leyendo `BRDPOut`/`approvals.py`, no asumido), `queryBinding="xslt3"` real en el documento generado, las 4 construcciones reales de arriba inyectadas verbatim (incluido el texto español con su entidad XML literal `&#211;` tal cual, sin decodificar -- `<pre>{result.xml}</pre>` de React inserta texto plano, nunca interpreta entidades), cero avisos de vocabulario, el error de ids duplicados correctamente mostrado, y descarga real con el nombre del proyecto (`<nombre>_<fecha>_dita.sch`). Regresión completa contra el proyecto Xpath2.0/Navantia S80 real re-ejecutada tras los nuevos cambios de vocabulario (`verify-dita-navantia-e2e.mjs`, todo en verde) -- confirma que ampliar `XPATH_FUNCTIONS`/`XPATH_KEYWORDS` sin condición no introdujo ningún efecto colateral en Xpath2.0. `npm run lint`/`npm run build` limpios.
- **8 huecos de vocabulario más, encontrados en producción real (proyecto Navantia Xpath3.0 del usuario, no reproducible en este entorno)**: al crecer el Rule de `BRDP-EXT-00001`/`00002`/`00003`/`00004a-d`/`00005`/`00006a`/`00006b`/`00007`/`00008`/`00009` para incluir las DEFINICIONES reales de las funciones auxiliares compartidas del proyecto (`valor`, `docFicha`, etc. -- hasta entonces `nav_dtm_xpath3_import.xlsx` solo traía sus llamadas vía `$valor(...)`/`$docFicha(...)`, nunca la definición en sí; confirmado con `grep` sobre el fichero real de este entorno que sigue sin contenerlas, así que el caso de 45 avisos ocurrió en el uso real del usuario, no reproducible aquí byte a byte) aparecieron 45 avisos no bloqueantes por 8 nombres XPath estándar nunca antes confirmados: `document-node` (node-kind test, 2.0), `substring-before`/`substring-after`/`root` (funciones XPath 1.0), `resolve-uri`/`base-uri`/`doc` (funciones XPath 2.0) y `ge` (operador de comparación general XPath 2.0, junto a `eq`/`ne`/`lt`/`le`/`gt`). Ninguno es exclusivo de 3.0 -- añadidos sin condición a `XPATH_FUNCTIONS`/`XPATH_KEYWORDS` (mismo criterio que `exists`/`empty`/`distinct-values`/`analyze-string`/`local-name`/`element`/`as` de la ronda anterior), aplican igual a proyectos `DITA 1.3 Xpath2.0`.
  - **Verificación**: como el fichero real de este entorno no reproduce el escenario de 45 avisos (no trae las definiciones), se reconstruyó un bloque `sch:let` realista con las definiciones reales de `valor`/`docFicha` usando los 8 tokens (`test-8-more-xpath-vocab.mjs`, script de prueba aislado, no committeado) -- confirma `vocabularyWarnings: []` para los 8 en ambos dialectos, Y que un nombre genuinamente inventado (`frobnicate`) sigue marcándose correctamente (sin sobre-ensanchar el vocabulario). De paso, esa reconstrucción sintética introdujo dos tokens XPath reales MÁS no pedidos por el encargo (`position`, el operador `is`) que siguen apareciendo como no confirmados -- comportamiento CORRECTO (no son parte de este encargo, no hay dato real que confirme su uso, así que no se tocan, mismo criterio "no especular" ya aplicado a `upper-case` en una ronda anterior). Regresión completa re-ejecutada contra AMBOS proyectos DITA reales de este entorno (`verify-dita-navantia-e2e.mjs` para Xpath2.0/Navantia S80, `verify-dita-xpath3-real-navantia.mjs` para el Xpath3.0 real que si se tiene aquí) -- todo en verde, cero avisos nuevos, cero regresiones. `npm run lint`/`npm run build` limpios.
- **Deduplicación automática de funciones compartidas en Schematron DITA Xpath3.0** (`generateSchematronDITA.js`, `dedupeSharedLets()`, nueva, solo para proyectos `DITA 1.3 Xpath3.0`): confirmado con el proyecto Navantia Xpath3.0 real del usuario -- `valor`/`colDe`/`colPart` repetidas 5 veces cada una en el documento ensamblado, `docFicha` 4 veces, otros helpers 2 veces, 62 `sch:let` en total donde la intención real era declarar cada función una sola vez. Viene de que cada `Rule` aprobada se inyecta como un `<sch:pattern>` autocontenido por fila (STRICT RULE 2) -- funcionalmente inofensivo (ISO Schematron limita un `sch:let` de nivel `sch:rule` a esa regla, así que las copias nunca chocan entre sí) pero muy alejado de cómo se escribiría el documento a mano. XPath 2.0 no tiene equivalente (las expresiones de función inline no existen en su gramática), así que esto nunca se ejecuta para proyectos `Xpath2.0` ni para ningún standard S1000D -- gateado en `generateSchematronDITA()` con `if (queryBinding === "xslt3")`.
  - **Mecánica**: `dedupeSharedLets(blocks)` recorre cada `<sch:rule>` de cada bloque aprobado (comentarios de trazabilidad no contienen `sch:rule`, pasan intactos sin caso especial), agrupa los `<sch:let name="..." value="...">` de primer nivel por `(name, value)` -- comparación exacta byte a byte, SIN normalizar espacios (mismo criterio ya aplicado en `_normSpace()`/`rule_override`: normalizar aquí colapsaría contenido significativo de un literal XPath). Un `(name, value)` que aparece en más de una regla se extrae una vez a nivel de `<sch:schema>` (ámbito global en ISO Schematron, visible para toda regla posterior que lo referencie por `$nombre`) y se retira de cada regla donde aparecía; el resto del bloque (assert/report, mensaje, otros `sch:let`) queda intacto. Un mismo `name` con `value` DISTINTO entre reglas nunca se fusiona -- cada copia se queda donde estaba, y se genera un aviso no bloqueante (`"Shared sch:let name 'X' is declared with N different values across approved rules -- none were merged..."`) fusionado en el MISMO array `vocabularyWarnings` que ya renderiza `GeneratePage.jsx` (mismo panel de UI, mismo criterio "informativo, nunca bloqueante", sin tocar la UI).
  - **`finalizeSchematronDocument`** gana un quinto parámetro opcional `sharedLets` (default `[]`, retrocompatible con toda llamada existente) que renderiza esos `sch:let` justo tras `<sch:title>` y antes del primer bloque -- el sitio más cercano en este documento ensamblado a "tras las declaraciones `sch:ns`, antes del primer `sch:pattern>`" del fichero original (este documento no declara `sch:ns` propios; cada bloque ya trae su `xmlns:xs` inline).
  - **Hallazgo propio, corrección necesaria no pedida explícitamente**: `lintVocabulary()` solo escaneaba `sch:let` DENTRO de un `sch:rule` -- una vez hoisteados a nivel de schema, el vocabulario de esas definiciones (p. ej. `position()` dentro de la propia `valor`, ya detectado en la ronda anterior) habría dejado de comprobarse en silencio, justo el tipo de regresión silenciosa que este lint existe para evitar. Añadido un tercer paso a `lintVocabulary()` que escanea `sch:let` FUERA de todo `sch:pattern` (regex sobre el XML con los `sch:pattern` ya recortados), atribuyendo cualquier aviso a `"shared:<name>"` en vez de a un BRDP concreto (un `sch:let` ya hoisteado no pertenece a una sola regla).
  - **Verificación real**: `scripts/verify-dedupe-shared-lets-xpath3.mjs` (nuevo, cubre los 4 casos límite del encargo contra un proyecto `DITA 1.3 Xpath3.0` REAL creado vía API/UI, con Rules aprobadas reconstruidas siguiendo el patrón de llamada YA confirmado real en `nav_dtm_xpath3_import.xlsx` -- 5 filas comparten `valor`/`colPart`/`colDe`/`cab` byte a byte, 4 comparten `docFicha`/`docs`): (1) tras generar, cada función compartida aparece EXACTAMENTE una vez en el documento real, con su contenido íntegro, y ningún `id`/mensaje de `assert` cambia; (2) una regla que usa un `sch:let` sin repetirlo en ninguna otra no se toca; (3) dos reglas con el mismo `name` pero `value` distinto (`"1 + 1"` vs `"2 + 2"`) nunca se fusionan -- ambas sobreviven, aparece el aviso nombrando la colisión; (4) aprobar una BRDP nueva que reutiliza una función ya compartida y regenerar (tras recargar la página real -- el listado de BRDPs de `GeneratePage.jsx` se carga una vez al montar, así que una BRDP creada por API fuera de banda no aparece sin un reload real, no es un bug del feature) la incorpora automáticamente al grupo, sin ningún paso manual, sin duplicar la definición. Documento final sigue siendo `queryBinding="xslt3"` y bien formado en todos los casos. Además, confirmado que el mecanismo también dedupea contenido genuinamente real y no solo reconstruido: el `sch:let name="cab"` de `nav_dtm_xpath3_import.xlsx` (idéntico byte a byte en `BRDP-EXT-00001/00002/00003/00005/00006`, confirmado por grep directo sobre el fichero real) pasa de 5 ocurrencias a 1 tras `dedupeSharedLets()`, sin tocar ningún `id`, y el error preexistente de ids duplicados de `00007/00008/00009` (hallazgo de la ronda anterior, bug del fichero de prueba) sigue detectándose sin cambios. Regresión completa contra el proyecto Xpath2.0/Navantia S80 real (`verify-dita-navantia-e2e.mjs`) y contra el proyecto Xpath3.0 real (`verify-dita-xpath3-real-navantia.mjs`) -- ambas en verde, confirmando que el feature no toca nada en Xpath2.0 (el `if (queryBinding === "xslt3")` nunca se ejecuta ahí) ni regresiona ningún comportamiento ya verificado en Xpath3.0. Suite backend completa (208 tests, sin cambios de backend en esta ronda) y `npm run lint`/`npm run build` limpios.
  - **Pendiente honesto**: el escenario EXACTO de producción (62 `sch:let`, conteos 5/5/5/4/2/2 con nombres como `nodoConref` nunca vistos en este entorno) no es reproducible byte a byte aquí -- el fichero estático de este sandbox no incluye las definiciones de las funciones compartidas, y las filas `00007`/`00008`/`00009` de este fichero siguen siendo un duplicado exacto entre sí (bug del fichero de prueba ya documentado), mientras que en producción evidentemente ya son reglas distintas. La verificación de arriba usa el patrón de llamada REAL confirmado (qué fila llama a qué función, con qué valor) más las definiciones reales que el usuario confirma que ahora existen, no una invención sin base.
- **`Download Excel template` sirve una plantilla real y curada por standard, no un genérico vacío** (`ProjectConfigPage.jsx`'s `handleDownloadTemplate`, `excelUtils.js`): `generateTemplate()` (mock, `Rule Status: To Do`, `Rule` vacío) era deliberadamente genérico porque en el momento de escribirlo el standard del proyecto no estaba disponible ahí -- ya no es cierto, `DataManagementSection` recibe `standard` como prop desde `ProjectConfigPage`. El usuario entregó 5 ficheros reales (10 BRDPs cada uno: 5 fáciles, 3 de complejidad media, 2 difíciles, todos `Rule Status: Verified` con `Rule` real ya relleno) para `S1000D 3.0.1`/`4.1`/`4.2` y `DITA 1.3 Xpath2.0`/`Xpath3.0`.
  - **Ficheros** copiados a `public/` con el mismo criterio de nombrado que los assets ya existentes (`brex-schema-summary-3-0-1.json`, etc. -- punto de versión con guiones, no puntos): `brdp-template-3-0-1.xlsx`, `brdp-template-4-1.xlsx`, `brdp-template-4-2.xlsx`, `brdp-template-dita-xpath2.xlsx`, `brdp-template-dita-xpath3.xlsx`.
  - **`CURATED_TEMPLATE_BY_STANDARD`** (nuevo, `excelUtils.js`, único sitio con el mapeo standard→fichero, tal como pedía el encargo): `handleDownloadTemplate` ahora es `async`, mira este mapa por `standard`, y si existe entrada hace `fetch(path)` + `blob()` (mismo patrón fetch+blob ya usado en el resto de la app para assets estáticos de `public/`, p. ej. `loadSchemaSummary()`) en vez de generar el mock; si no existe entrada (`S1000D 5.0`/`6.0`, sin motor de generación todavía, nada real que mostrar) cae al `generateTemplate()` de siempre, sin cambios. `generateTemplate()`/`mockBRDPs` NO se borran -- documentado explícitamente como fallback para 5.0/6.0 y para cualquier standard futuro sin plantilla curada aún.
  - **Nota de calidad del propio usuario, no ocultada**: de las 50 filas totales, 47 son datos reales de Postgres verificados contra el `.sch`/proyecto de origen; 3 (`BRDP-D1-00020`/`BRDP-D1-00065` en Xpath2.0, `BRDP-D1-00020` en Xpath3.0) están basadas en temas reales del catálogo DITA pero con `Rule` escrito a mano sin pasar por un validador Schematron real -- bien formadas, pero sin el mismo nivel de verificación. Confirmadas presentes exactamente en esos 3 sitios al inspeccionar los ficheros reales antes de copiarlos a `public/`.
  - **Verificado con Playwright real** (`scripts/verify-curated-templates.mjs`): para los 5 standards curados, clic real en "Download Excel template" descarga el fichero correcto (nombre exacto), contenido leído con `xlsx` confirma 10 filas reales, columna `Rule` poblada en las 10 (ninguna vacía) y `Rule Status: Verified` en las 10 -- no solo tamaño de fichero, contenido real inspeccionado fila a fila. `S1000D 5.0`/`6.0` siguen descargando el `brdp-template.xlsx` genérico de siempre, sin romper nada. Excede el "al menos una" del encargo: las 5 plantillas descargadas se reimportaron cada una en un proyecto nuevo del mismo standard real -- 10 BRDPs creados, 0 rechazadas, las 10 filas quedan con su Rule aprobada automáticamente (`rule_approvals.status = "approved"`) bajo el formato real de cada standard (`BREX-3.0.1`/`BREX-4.1`/`BREX-4.2`/`SCH-DITA`), confirmando que el fichero real es válido de punta a punta y no solo "se descarga". Bug real encontrado y corregido EN EL PROPIO SCRIPT de verificación (no en la app) durante esta ronda: un `for (const [standard, format] of Object.entries(CURATED))` reutilizaba por error el nombre de fichero como si fuera el id de formato de reglas -- confirmado con un script de depuración aislado que la app sí aprobaba las 10 filas correctamente; el fallo era 100% del test, no del código de producción. Suite backend completa (208 tests, sin cambios de backend en esta ronda) y `npm run lint`/`npm run build` limpios.
- **Cifras de `Proposal Status`/`Rule Status` en `BRDP Projects`/`BRDP Records`, más filtro en `BRDP Records`** (backend: `app/repositories/brdp_repository.py`, `app/schemas/status_counts.py` nuevo, `app/api/routes/projects.py`, `app/api/routes/brdps.py`; frontend: `src/components/StatusCountsSummary.jsx` nuevo, `src/pages/ProjectsPage.jsx`, `src/pages/RecordsPage.jsx`):
  - **`compute_status_counts(db, projects)`** (nuevo, `brdp_repository.py`): dadas N `Project`, calcula `proposal_status_counts`/`rule_status_counts` por proyecto con **exactamente 2 queries SIEMPRE** (no N+1 -- mismo tipo de bug ya arreglado dos veces antes en esta rama, para Reset Data e Import), verificado con un `QueryCounter` propio (`event.listen(engine.sync_engine, "before_cursor_execute", ...)`, no existía infraestructura de test para esto en el repo, construida desde cero) que confirma el conteo de queries IDÉNTICO con 1 proyecto o con 7. Query A: `GROUP BY project_id, validation` sobre `brdps` (activas, `ACTIVE_BRDP_FILTER`) -- da `proposal_status_counts` y, sumando, el total de BRDPs activas por proyecto. Query B: `GROUP BY project_id, format, status` sobre `rule_approvals JOIN brdps` -- da un lookup `(project_id, format, status) → count`. Por proyecto, `fmt = STANDARD_TO_RULE_FORMAT.get(project.standard)` (`None` para S1000D 5.0/6.0, que no tienen formato de reglas); `draft`/`verified` salen del lookup bajo `fmt` (0 si `fmt` es `None`, o si no hay filas -- crucialmente, esto también hace que una fila `rule_approvals` residual bajo un formato YA NO vigente para ese proyecto -- p. ej. tras un rename de standard -- nunca cuente, verificado con una fila de test dedicada); `to_do = total - draft - verified` (nunca se consulta directamente, "sin fila de aprobación" no es un valor almacenado).
  - **`ProjectOut`** (`schemas/project.py`) gana `proposal_status_counts`/`rule_status_counts` (nuevo `schemas/status_counts.py`, `ProposalStatusCounts`/`RuleStatusCounts`, módulo propio para no cruzar imports entre `schemas/project.py` y `schemas/brdp.py` por un concepto que en realidad es un tercero -- una distribución de estados). `projects.py`'s `list_projects` calcula `compute_status_counts` UNA VEZ para toda la lista (admin y no-admin) y se lo pasa a `_to_out`; los otros 4 call sites de `_to_out` (crear/config/rename) siguen calculando su propio conteo de 1 proyecto, aceptable por no ser de listado.
  - **`GET /api/projects/{id}/brdps/stats`** (nuevo endpoint, no metadata embebida en `GET /brdps`) -- decisión explícita, documentada en el propio docstring de `get_brdp_stats`, por dos motivos independientes: (a) `GET /brdps` devuelve un `list[BRDPOut]` plano consumido directamente por varios llamadores ya existentes (Export a Excel, el dataset de `GeneratePage`, el conteo de BRDPs de la confirmación de borrado de proyecto) -- envolverlo en un objeto sería un cambio incompatible para todos ellos; (b) aunque no lo fuera, sería semánticamente incorrecto: la cabecera debe mostrar SIEMPRE los totales reales del proyecto, sin importar qué filtro `proposal_status`/`rule_status` tenga activo la llamada a la lista en ese momento -- mezclarlos obligaría a recalcular esta misma agregación sin filtrar una segunda vez de todos modos en cuanto hubiera un filtro activo.
  - **Filtro real en SQL** (`brdps.py`'s `list_brdps` → `brdp_repository.py`'s `list_active_brdps`, extendida con `proposal_status`/`rule_status`/`rule_format`): `proposal_status` es un `WHERE BRDP.validation == ...` directo; `rule_status == "todo"` es un `NOT IN` sobre los ids con aprobación bajo el formato vigente (o ningún filtro extra si el standard no tiene formato -- todo cuenta como "todo"); `draft`/`verified` son un `IN` sobre los ids con aprobación bajo el formato vigente y el `status` correspondiente (`pending_review`/`approved`); pedir `draft`/`verified` en un standard sin formato de reglas devuelve `[]` (nunca puede haber ninguna). Los 3 parámetros nuevos son opcionales (`None` por defecto), así que ningún llamador existente de `GET /brdps` (Export, `GeneratePage`, etc.) se ve afectado.
  - **Hallazgo arquitectónico real, no asumido, que corrige la propia premisa del encargo**: leyendo `brdps.py`/`RecordsPage.jsx` se confirma que `GET /api/projects/{id}/brdps` **no tiene paginación real en el servidor** -- devuelve la lista COMPLETA sin paginar, y la "paginación" de 15 filas de `RecordsPage.jsx` (`TABLE_PAGE_SIZE`) es enteramente client-side (`filteredBrdps`/`sortedBrdps`/`pagedBrdps` son `.filter()`/`.sort()`/`.slice()` en JS sobre el array ya cargado entero en memoria del navegador). El encargo asumía explícitamente que la paginación ya existente era server-side ("el filtro tiene que reducir el resultado en el servidor, igual que ya hace la paginación existente"), y no lo es. Decisión tomada y confirmada por el resultado, no ampliada sin permiso: se implementó SOLO el filtrado real en SQL pedido (una reducción real, aunque parcial, del número de filas devueltas por el servidor cuando hay un filtro activo) -- NO se introdujo paginación server-side completa, que habría sido un cambio de arquitectura no pedido. Sin filtro activo el comportamiento es idéntico a antes (lista completa, paginada en cliente); con un filtro activo, el servidor ya devuelve un subconjunto real y más pequeño, que se sigue paginando en cliente sobre ese conjunto reducido -- una mejora de rendimiento real aunque parcial, sin regresión cuando no hay filtro.
  - **`StatusCountsSummary.jsx`** (nuevo, compartido por `ProjectsPage.jsx` -- 2 columnas nuevas de tabla -- y `RecordsPage.jsx` -- resumen en la cabecera): formato compacto `V 120 · P 45 · R 3` (etiqueta completa solo en `title`/tooltip -- 3 columnas por estado habría sido demasiado ancho, pedido explícito). Colores reutilizan la paleta YA EXISTENTE, nunca una nueva: Proposal Status reutiliza los hex exactos de `.badge_Validated`/`.badge_Pending`/`.badge_Refused` de `RecordsPage.module.css`; Rule Status no tenía color por-estado propio (el stepper `.dots` existente es una progresión, no 3 categorías independientes), así que reutiliza el azul "primario" de la app (`#2563eb`, igual que `.dotCurrent`/el botón primario de `ProjectsPage`) para Verified más dos tonos slate ya usados en la UI (`#64748b`/`#94a3b8`) para Draft/To Do.
  - **`RecordsPage.jsx`**: cabecera pasa de bloque apilado a fila flex (mismo patrón `.header`/`.headerActions` que `ProjectsPage.module.css` ya usaba, replicado en `RecordsPage.module.css`, que no los tenía). Dos `<select>` nuevos junto a la caja de búsqueda (`filterSelect`, distinto de `.select` -- ese es `width:100%`, pensado para el panel de detalle apilado, no para una fila inline) -- valor `''` = "All", omitido de la query string por completo; los valores del filtro de Rule Status son literalmente `RULE_STATES` (`todo`/`draft`/`verified`) y los de Proposal Status son los valores reales de `validation`, así que no hace falta capa de traducción entre el `<select>` y el vocabulario de la API. `refresh()` construye la query string desde ambos filtros; `refreshStats()` (nuevo, `GET .../stats`, siempre sin filtro) alimenta el resumen de cabecera, con sus propias llamadas tras cualquier mutación que pueda cambiar los totales (crear/editar/borrar BRDP, y vía `approvalsRefreshToken` para Verify/Revoke/edición manual de Rule/aceptar sugerencia de Rule, que no pasan por `refresh()`).
  - **Verificación a escala real, con hallazgo honesto sobre el entorno**: este sandbox no tiene ningún proyecto SOPTE/Lufthansa real (solo `Demo Project (S1000D 4.2)`, vacío). Se sintetizó un proyecto real en Postgres a la misma escala (`backend/scripts/seed_sopte_scale_verification.py`, nuevo, reutilizable en rondas futuras -- 2819 BRDPs con distribución de verdad-conocida exacta: 1200 Pending/1400 Validated/219 Refused, 719 to_do/600 draft/1500 verified) para poder verificar el "cuenta exacta, no aproximada" del encargo contra un ground truth real, no solo "parece correcto". Confirmado con Playwright real (`scripts/verify-status-counts-and-filters.mjs`, nuevo) contra ese proyecto y contra `Demo Project`: cabecera y columnas de `BRDP Projects` muestran las cifras reales (capturas en `/tmp/verify-projects-page.png`/`verify-records-page-sopte.png`); `Rule Status=Verified` solo → exactamente 1500 filas; `Proposal Status=Refused` solo → exactamente 219; combinando `Proposal Status=Validated` + `Rule Status=Draft` → exactamente 600 (AND real, ni 1400 ni 600 por separado); sin filtro → 2819, `Page 1 of 188` (la cifra exacta que citaba el propio encargo); búsqueda de texto ("Alpha") + `Proposal Status=Validated` combinados sobre datos reales de `Demo Project` → solo la fila que cumple ambos, ni la Pending ni la que no matchea el texto. Tiempos reales medidos con el proyecto de 2819 filas en la mezcla: `GET /api/projects` 18ms, `GET .../stats` 32ms, `GET /brdps?rule_status=verified` 110ms, resto por debajo de 70ms -- ninguna regresión de rendimiento perceptible. El proyecto sintético se borra al final del propio script de verificación (no queda colgado en la lista real de proyectos); rerun de `seed_sopte_scale_verification.py` lo reconstruye para una ronda futura. 13 tests nuevos en `test_brdp_status_counts_and_filters.py` (221 tests backend en total, todos en verde) cubren además los casos límite: proyecto recién creado con 0 BRDPs (cifras en `0`, nunca `undefined`/`NaN`), standard sin formato de reglas (todo cuenta como `to_do`), y una fila `rule_approvals` bajo un formato ya no vigente (nunca cuenta). `npm run lint`/`npm run build` limpios.
- **Ronda siguiente: dos arreglos de alineación sobre las mismas features (filtros de `BRDP Records`, columnas de resumen de `BRDP Projects`)** -- ninguno de los dos era un problema de datos ni de lógica, ambos eran de layout puro, arreglados sin tocar backend:
  - **Filtros de `BRDP Records` desalineados de sus columnas** (`RecordsPage.jsx`/`RecordsPage.module.css`): la ronda anterior colocaba los dos `<select>` de filtro en `.createForm` (una fila flex encima de la tabla, junto al buscador) y confiaba en que `colValidation`/`colRuleStatus` (130px cada una) coincidieran por casualidad con el ancho que ese `<select>` acabara ocupando en SU PROPIO contenedor flex -- dos cálculos de ancho independientes que solo coincidían mientras nada cambiara a ningún lado. Arreglado sacando ambos `<select>` de `.createForm` (que vuelve a tener solo el buscador + "Add BRDP") y metiéndolos en una SEGUNDA fila de `<thead>` (`.filterRow`), en las mismas `<th>` (vía `colSpan` en las columnas ID/Title/Actions, que no llevan filtro) que ya usa la fila de `SortableHeader` -- la alineación queda garantizada por el propio `colgroup` de la tabla (una única fuente de verdad para el ancho de columna), no por dos sitios que tienen que coincidir. `.filterSelect` pasa de `flex-shrink:0` (pensado para una fila flex) a `width:100%` + `min-width:0` dentro de su `<th>` (el `min-width:0` es necesario: el ancho intrínseco por defecto de un `<select>` puede forzar la celda -- y con ella la columna, pese a `table-layout:fixed` -- más ancha que sus 130px en cuanto la opción más larga lo pida). Verificado con Playwright real (`scripts/verify-status-summary-alignment.mjs`, nuevo) midiendo `boundingBox()` real del `<th>` de cabecera contra el `<select>` de filtro: x/width idénticos (±0.5px, achacable a subpíxel de fuente) tanto en inglés como en español (`Estado de propuesta` envuelve a 2 líneas, la opción `Rechazado`/`Verificada` es más larga que su equivalente en inglés -- el caso límite explícito del encargo), y los filtros siguen funcionando de verdad tras el cambio de posición (filtrar `Rule Status=Verified` en un proyecto real de 963 BRDPs sigue devolviendo el subconjunto SQL correcto, 463 filas).
  - **Columnas de resumen de `BRDP Projects` desalineadas entre filas** (`StatusCountsSummary.jsx`/`StatusCountsSummary.module.css`): `V 120 · P 45 · R 3` metía la letra y el número en el mismo nodo de texto dentro del `<span>` coloreado -- sin ancho fijo para la parte numérica, la posición de `P`/`R` (o `D`/`T`) en cada fila dependía de cuántos dígitos tuviera el valor anterior, así que una fila con `V 2819` y otra con `V 2` en la tabla desalineaban sus `P`/`R` verticalmente. Arreglado envolviendo el número en su propio `<span className={styles.num}>` (`display:inline-block; min-width:4ch; text-align:right`) -- 4 caracteres cubre hoy el recuento real más ancho que existe (SOPTE, ~2819) con un dígito de margen, no un número arbitrario sin justificar (documentado en el propio CSS qué pasa si algún proyecto futuro llega a 5 dígitos: ensanchar el valor). Se añadió también `font-variant-numeric: tabular-nums` -- sin él, IBM Plex Sans (la fuente de toda la app) no garantiza que los dígitos midan lo mismo entre sí (un "1" puede renderizar más estrecho que un "8" incluso con la misma cantidad de dígitos), lo que habría dejado el `min-width` fijo pero la alineación real rota de todos modos. Verificado con datos reales a la escala exacta que citaba el encargo: `backend/scripts/seed_alignment_check_projects.py` (nuevo, reutilizable) siembra 10 proyectos sintéticos-pero-realistas de 2 a 2819 BRDPs (incluyendo el extremo citado explícitamente -- un proyecto a escala SOPTE con `V 2818 · P 0 · R 1`, junto a uno de 2 BRDPs con `V 2 · P 0 · R 0`) -- `scripts/verify-status-summary-alignment.mjs` (nuevo) mide con Playwright la posición x real de cada etiqueta `P`/`R`/`D`/`T` en las 11 filas de la tabla (10 sintéticas + `Demo Project`) y confirma que las cuatro son idénticas entre sí en las 11 filas (±0.5px), no solo "parece alineado" en una captura. Los 10 proyectos sintéticos se borran al final del propio script (no quedan colgados en la lista real); rerun del seed script los reconstruye para una ronda futura.
  - Capturas reales guardadas en `/tmp/projects-alignment-final.png` (10 filas reales, de 2 a 2819 BRDPs, alineación verificada visualmente), `/tmp/records-alignment-en.png` y `/tmp/records-alignment-es.png` (filtros ya dentro del `<thead>`, alineados con sus columnas en ambos idiomas). `npm run lint`/`npm run build` limpios; sin cambios de backend en esta ronda (no se re-ejecutó la suite pytest completa por no haber tocado ningún fichero Python).
- **Bug real de HTML sin escapar en `Generate Report` (`buildBREXdocReport.js`) + columna `Comment` sustituida por `Rule Status`**: confirmado con un informe real de Lufthansa que adjuntó el usuario -- `BRDP-S1-00123` tiene un `Title` real del catálogo S1000D que menciona un nombre de elemento entre corchetes angulares tal cual ("...the element `<table>`"). `renderPage()` (el script embebido dentro del propio HTML generado, que corre en el navegador del usuario al ABRIR el informe descargado, no en la app de React) insertaba `${b.title}`/`${b.definition}`/`${b.proposal}`/`${b.comment}` sin escapar vía `tbody.innerHTML = ...` -- el navegador interpretaba `<table>` como el inicio de una tabla HTML real sin cerrar dentro de un `<td>`, HTML inválido que "corrige" reflotando el resto de esa fila fuera de la tabla (exactamente lo visto en la captura). SOPTE y el fixture Official Default 3.0.1 no lo disparaban por pura suerte de los datos (ninguno de los dos tiene casualmente una subcadena con forma de etiqueta), no porque el código fuera distinto -- cualquier proyecto cuyo texto real mencione un elemento entre `<` `>` (Lufthansa tiene varios más) reproduce el mismo fallo.
  - **`escapeHtml(value)`** (nueva, duplicada a propósito en DOS sitios que son dos contextos de ejecución distintos, no un descuido): una copia a nivel de módulo de `buildBREXdocReport.js` (corre en la app de React, al GENERAR el informe -- usada en `<title>`, la cabecera `cover-meta` y el footer, todos los sitios donde `projectConfig.projectName`/`modelIdentCode`/`issueNumber`/`inWork` -- texto editable por el usuario en Project Configuration -- se insertan directamente en la plantilla) y una copia idéntica embebida dentro del propio `<script>` del HTML generado (corre en el navegador que ABRE el informe descargado, sobre `id`/`title`/`definition`/`proposal`/`validation`/`ruleStatus` de cada BRDP en `renderPage()`) -- son dos runtimes completamente distintos (build-time en la SPA vs. runtime dentro de un fichero `.html` standalone abierto días/semanas después), así que no hay forma de compartir una sola función entre ambos sin cargar un módulo externo en el HTML exportado (roto: el propio fichero debe ser autocontenido, sin dependencias, para poder compartirse/archivarse suelto).
  - **Segundo hallazgo relacionado, no pedido explícitamente pero de la misma categoría** ("vale la pena arreglarlo con ese rigor"): `brdpsJSON` (los datos de BRDPs) se serializa con `JSON.stringify(...)` y se pega directamente dentro de ese mismo `<script>` como `const data = ${brdpsJSON};` -- si algún campo contuviera literalmente la subcadena `</script>`, el PARSER HTML (no el JS) cerraría la etiqueta `<script>` ahí mismo al cargar la página, corrompiendo todo el informe antes de que el JS llegara a ejecutarse -- mismo tipo de bug ("texto sin escapar rompe el marcado que lo rodea"), en un punto de inserción distinto (parseo HTML, no innerHTML en tiempo de render). Arreglado con `.replace(/</g, '\\u003c')` sobre el JSON ya serializado (técnica estándar para JSON embebido en un `<script>`, válida porque `<` es un escape JSON legítimo que sigue parseando como `<` normal).
  - **Tercer hallazgo, en la exportación Markdown (línea 203 original, revisada explícitamente a petición del encargo)**: un `|` sin escapar en `title`/`definition`/`proposal`/`comment` rompe una tabla GFM real (se interpreta como delimitador de columna, desplazando el resto de la fila) -- menor severidad que el caso HTML (no hay ejecución de nada, "solo" corrompe el layout de la tabla), pero misma causa raíz. Arreglada con `escapeMarkdownCell(value)` (nueva): normaliza saltos de línea a espacio (ya lo hacía) y escapa `|` → `\|`. `id` se deja tal cual (formato de identificador estructurado, nunca contiene `|` en la práctica) y `validation` también (enum fijo `Pending`/`Validated`/`Refused`).
  - **Columna `Comment` → `Rule Status`** (`To Do`/`Draft`/`Verified`, mismos tres estados y la MISMA paleta de colores ya establecida para Rule Status en `BRDP Records`/`BRDP Projects` -- `StatusCountsSummary.module.css`: `#2563eb` Verified, `#64748b` Draft, `#94a3b8` To Do, nunca una paleta nueva): en la tabla HTML se renderiza como badge-pill (`.badge-rule-verified`/`.badge-rule-draft`/`.badge-rule-todo`, mismo patrón visual de pill light-bg+texto-saturado que el informe ya usaba para Proposal Status, con esos tres hex exactos como color de texto -- los fondos claros son solo el tinte derivado de esos mismos tonos, no una cuarta paleta inventada); en Markdown, la columna de texto plano equivalente. `GenerateBREXdocPage.jsx` (el único caller vivo real -- ver hallazgo de código muerto abajo) ahora hace `Promise.all` con el fetch de BRDPs YA existente más `GET /api/projects/{id}/approvals/{format}/export` (mismo endpoint y patrón bulk que `ProjectConfigPage.jsx`'s Export to Excel ya usa), calcula `ruleStateOf(approval)` por BRDP y lo traduce a `RULE_STATUS_LABELS` (mismo mapa literal `{todo:'To Do',draft:'Draft',verified:'Verified'}`, sin i18n -- todo el resto del texto de este informe ya es inglés hardcodeado, mismo criterio que Export to Excel). Un BRDP `To Do` (sin fila `rule_approvals`) muestra su estado real "To Do", nunca vacío ni un placeholder -- es el valor CORRECTO de "sin aprobación todavía", no una ausencia de dato (mismo principio ya aplicado en `ruleStateOf`/`RULE_STATES` en rondas anteriores). Standards sin formato de reglas (S1000D 5.0/6.0) saltan el fetch de aprobaciones por completo y cada fila cae en "To Do" vía `ruleStateOf(null)`, mismo convenio que el resto de la app.
  - **Hallazgo de código muerto, confirmado por grep, no tocado**: `BREXdocModal.jsx` (que también llama a `downloadReport` con una forma `comment` antigua) no está importado desde ningún sitio de `src/` -- mismo patrón que el hallazgo de `ChatPanel`/`useChat` de una ronda anterior. El camino real y único es `GenerateBREXdocPage.jsx` (enrutado, con el botón real "Generate Report" de `ProjectsPage.jsx` navegando a `/projects/{id}/brexdoc`). No se tocó `BREXdocModal.jsx` por no ser alcanzable, documentado aquí para no repetir la investigación si reaparece en una auditoría futura.
  - **Verificación con datos reales**: este sandbox no tiene el proyecto Lufthansa real, así que se reprodujo el defecto exacto reportado con un proyecto sintético pequeño (`scripts/verify-report-escaping-and-rule-status.mjs`, nuevo, auto-limpia el proyecto al final) -- 4 BRDPs reales vía API: uno con `Title`/`Definition`/`Proposal` conteniendo literalmente `<table>`/`<graphic>`/`<copyright>` (reproduce BRDP-S1-00123 byte a byte en estructura), uno con `&` sin escapar ("Tools & Equipment"), uno con `|` (para el caso Markdown), y uno neutro sin caracteres especiales (regresión: confirma que el escapado no cambia nada cuando no hace falta). Verificación real, no solo visual: se descarga el HTML real generado, se ABRE en una página Playwright nueva (el mismo fichero que el usuario abriría), y se inspecciona el DOM real resultante -- la fila de `BRDP-S1-00123` sigue siendo hija directa de `#brdp-tbody` con exactamente 6 `<td>` reales (nunca menos/más, que es lo que pasaría si el `<table>` sin escapar hubiera roto la estructura), el texto `<table>` se ve literalmente como texto (confirmado además que NO existe ningún elemento `<table>` real anidado dentro de esa celda), y las 4 filas totales siguen siendo 4 hijas directas de `#brdp-tbody` (ninguna se escapó). Columna `Rule Status` verificada con sus 3 estados reales (`Verified`/`Draft`/`To Do`) y sus 3 clases de badge correctas. Descarga Markdown real inspeccionada por contenido: la fila con `|` sigue teniendo exactamente 8 columnas tras el split (el `|` quedó escapado como `\|`, no rompió la tabla). Capturas reales en `/tmp/brexdoc-report-html-fixed.png` (el fix visible: `<table>`/`<graphic>`/`<copyright>` como texto normal dentro de sus celdas, columna Rule Status con badges de color correcto) y `/tmp/brexdoc-report-demo-regression.png` (Demo Project, sin regresión). `npm run lint`/`npm run build` limpios; sin cambios de backend en esta ronda.
- **`BRDP Projects`: cabecera de dos niveles con solo números; `BRDP Records`: etiquetas completas en el resumen de cabecera** (`StatusCountsSummary.jsx`/`.module.css`, `ProjectsPage.jsx`/`.module.css`, `SortableHeader.jsx`, `RecordsPage.module.css`) -- ambos cambios puramente de layout/texto sobre features ya existentes de rondas anteriores, sin tocar backend:
  - **`StatusCountsSummary`** gana un prop `variant="full" | "numbersOnly"` (default `'full'`) en vez de duplicar componentes, tal como pedía el encargo -- internamente, `ProposalStatusSummary`/`RuleStatusSummary` ahora derivan un array de "fields" (`{key, label, className, value, fullLabel}`, uno por estado) vía un hook interno (`useProposalStatusFields`/`useRuleStatusFields`) y lo pasan a uno de dos renderers según el variant: `FullSummary` (el `<span>` de siempre, ahora con etiqueta completa + dos puntos -- "Validated: 36, Pending: 0, Refused: 0" en vez de "V 36 · P 0 · R 0") o `NumberCells` (nuevo -- un Fragment de 3 `<td>` reales, cada uno con SOLO el número coloreado, sin letra -- pensado para spreadearse directamente dentro de un `<tr>`, un `<td>` por columna hoja de la cabecera de dos niveles).
  - **`ProjectsPage.jsx` -- cabecera de dos niveles**: fila 1 con `colSpan={3}` en "Proposal Status"/"Rule Status" (mismo texto de siempre) y `rowSpan={2}` en Project name/Project standard/Actions (para no quedar empujadas por la segunda fila); fila 2 con las 6 subcabeceras `V`/`P`/`R`, `V`/`D`/`T` en el mismo orden que `useProposalStatusFields`/`useRuleStatusFields` (comentario explícito en el código de que ambos órdenes NUNCA deben divergir). `SortableHeader.jsx` gana un prop opcional `rowSpan` (passthrough al `<th>`, `undefined` por defecto -- ningún otro llamador existente se ve afectado) para que Name/Standard sigan siendo sorteables Y abarquen las dos filas de cabecera. Las filas de datos ya no tienen un único `<td>` por grupo -- `<ProposalStatusSummary variant="numbersOnly" />`/`<RuleStatusSummary variant="numbersOnly" />` se colocan directamente dentro del `<tr>` (cada uno se expande a sus 3 `<td>` reales), así que la alineación entre filas ahora la garantiza el propio layout nativo de columnas de la tabla HTML, no el truco de `min-width`/`tabular-nums` de la ronda anterior (que dejó de aplicar aquí -- documentado en el propio CSS como obsoleto para este variant, `.num` se sustituye por `.numCell`).
  - **`RecordsPage.module.css`**: con el resumen ahora mucho más largo ("Validated: 36, Pending: 0, Refused: 0" en vez de "V 36 · P 0 · R 0"), `.header`/`.headerActions` ganan `flex-wrap: wrap` -- si una ventana más estrecha no cabe título+resumen en una fila, el resumen (o cada uno de sus dos bloques) cae a su propia línea en vez de desbordar horizontalmente, mismo tipo de fallo (asumir un ancho fijo en inglés) que rompió la alineación de los filtros en la ronda anterior. Verificado que a 1440px (el ancho ya usado en todas las capturas de rondas anteriores) ni inglés ni español necesitan el wrap -- las 6 etiquetas completas caben en una sola línea en ambos idiomas, `document.documentElement.scrollWidth` confirmado igual al viewport (sin overflow horizontal) en los dos casos.
  - **Verificación real**: reutiliza los mismos 10 proyectos sintéticos de 2 a 2819 BRDPs de la ronda de alineación anterior (`backend/scripts/seed_alignment_check_projects.py`, sin cambios, solo re-ejecutado) -- `scripts/verify-two-level-header-and-full-labels.mjs` (nuevo) confirma con Playwright real: el `<thead>` tiene exactamente 2 filas reales; la fila de subcabeceras lee literalmente `V,P,R,V,D,T`; `colspan="3"`/`rowspan="2"` reales en los atributos DOM (no solo visual); la fila de SOPTE muestra el número bare `2818`/`0`/`1`/`1500` en sus celdas de datos, sin ninguna letra `V`/`P`/`R`/`D`/`T` mezclada en el texto; y, el mismo tipo de prueba de alineación por `boundingBox()` que la ronda anterior pero ahora sobre las 6 columnas reales de `<td>` (no un `<span>` compartido) -- las 6 quedan idénticas en x (±0.5px) a lo largo de las 11 filas (10 sintéticas + `Demo Project`), del extremo de 4 dígitos (SOPTE) al de 1 dígito (Navantia S80-scale/Demo Project). En `BRDP Records`, confirma las 6 etiquetas completas reales ("Validated: 900", "Pending: 50", etc.) tanto en inglés como en español (`Validado: 900`, `Pendiente: 50`, `Rechazado: 13`, `Verificada: 463`, `Borrador: 200`, `Por hacer: 300`), y que `document.documentElement.scrollWidth` no crece por encima del viewport de 1440px en ninguno de los dos idiomas. Capturas reales en `/tmp/projects-two-level-header.png` (10 filas + `Demo Project`, cabecera de dos niveles, solo números) y `/tmp/records-full-labels-en.png`/`/tmp/records-full-labels-es.png`. Los 10 proyectos sintéticos se borran al final del propio script (no quedan colgados en la lista real); rerun del seed script los reconstruye para una ronda futura. `npm run lint`/`npm run build` limpios; sin cambios de backend en esta ronda.
- **Textbox de motivo al marcar `Refused` + `BRDP Projects`: cabeceras centradas y tooltips por letra** (`RecordsPage.jsx`, `ProjectsPage.jsx`/`.module.css`, `src/i18n/index.js`) -- de nuevo sin tocar backend, `brdps.comments` ya existía de punta a punta en el modelo/esquemas (`create`/`update`/`out`) pero nunca se había cableado en el frontend, confirmado por grep antes de tocar nada (cero referencias a `comments` en `RecordsPage.jsx`).
  - **`<textarea>` de motivo de rechazo**: aparece justo debajo del `<select>` de `Proposal Status` en el panel de detalle, SOLO cuando `selected.validation === 'Refused'` -- mismo patrón de edición que `proposal`/`definition` ya usan (`onChange` actualiza el estado local vía `setBrdps`, `onBlur` llama a `handleUpdate(selected.id, { comments: e.target.value })`). Opcional, tal como se confirmó con el usuario: nada bloquea guardar `Refused` con el campo vacío. Cambiar a `Validated`/`Pending` oculta el textbox pero NO borra el `comments` ya guardado -- confirmado explícitamente con un ciclo real Refused→Validated→Refused: el motivo sigue ahí sin volver a escribirlo. Añadida también, por ser trivial tal como sugería el propio encargo, una tercera cosa NO pedida explícitamente pero de coste mínimo: el badge `Refused` de la tabla (`RecordsPage.jsx`, celda de Proposal Status) lleva `title={b.comments}` cuando hay motivo guardado -- mismo mecanismo `title` que ya usa el resto de la app, sin componente ni estilo nuevo.
  - **`ProjectsPage.jsx` -- centrado + tooltips en la cabecera de dos niveles**: la cabecera de grupo (`<th colSpan={3}>Proposal Status/Rule Status</th>`) heredaba `text-align:left` de `.table th`, así que el texto quedaba sobre la PRIMERA de sus 3 columnas, no centrado sobre las 3 -- nueva clase `.groupHeader { text-align: center; }`. Cada una de las 6 subcabeceras hoja (`V`/`P`/`R`, `V`/`D`/`T`) gana un `title` con el nombre completo del estado, usando las mismas claves i18n que ya usa `StatusCountsSummary` (`records.validationOptions.*` para Proposal Status, `records.rule.states.*` para Rule Status) -- la misma letra `V` significa `Validated` en el primer grupo y `Verified` en el segundo, cada `<th>` lleva el `title` correcto según a cuál de los dos pertenece, no un mapeo letra→texto único y ambiguo. Descartado explícitamente por el usuario: hacer la sección plegable/desplegable -- no se implementó.
  - **Verificación real** (`scripts/verify-refusal-reason-and-header-tooltips.mjs`, nuevo, auto-limpia lo que siembra): un BRDP real creado vía API en `Demo Project`, ciclo completo por UI real -- confirma que el textbox NO aparece en `Pending`, aparece de inmediato al cambiar a `Refused`, el valor sobrevive un F5 real (recarga completa, no solo estado de React -- prueba de que se guardó en Postgres, no solo en memoria), el badge `Refused` de la tabla lleva el mismo texto como tooltip, el textbox desaparece al volver a `Validated` sin que el campo se borre en el backend (confirmado con un `GET` real tras el cambio), y que al volver a `Refused` el motivo antiguo reaparece solo, sin pedirlo. Para la cabecera: mide con `boundingBox()` que el centro x del texto "Proposal Status" coincide (±1px) con el centro x real del span de sus 3 columnas hoja, y confirma los 6 `title` reales vía `getAttribute` (no solo visual) -- `V`→`Validated`, `P`→`Pending`, `R`→`Refused`, `V`→`Verified`, `D`→`Draft`, `T`→`To Do`. Capturas reales: `/tmp/refusal-textbox-visible.png`/`/tmp/refusal-textbox-hidden.png` (aparece/desaparece) y `/tmp/projects-header-centered-multirow.png` (10 proyectos reales de 2 a 2819 BRDPs, cabeceras ya centradas, reutilizando `backend/scripts/seed_alignment_check_projects.py` de rondas anteriores, borrados de nuevo al terminar). `npm run lint`/`npm run build` limpios; sin cambios de backend en esta ronda (el campo `comments` ya existía completo).
- **Ronda siguiente: el centrado de la cabecera de `BRDP Projects` de la ronda anterior NUNCA se aplicó de verdad -- bug real de especificidad CSS, encontrado por el usuario, no por esta sesión**: `.table th { text-align: left; }` (`ProjectsPage.module.css`) tiene especificidad `(0,1,1)` (una clase + un elemento `th`) -- estrictamente mayor que `.groupHeader`/`.subHeader { text-align: center; }` solas, `(0,1,0)` (una clase, cero elementos). En CSS, una regla de especificidad más baja NUNCA gana sobre una más alta por muy tarde que aparezca en el fichero -- `.table th`'s `text-align:left` seguía ganando la cascada pese a que el comentario de la ronda anterior ya identificaba correctamente la causa raíz ("era left-aligned por defecto"), el CSS añadido simplemente no tenía especificidad suficiente para vencerla.
  - **Arreglo**: `.groupHeader`/`.subHeader` pasan a `.table th.groupHeader`/`.table th.subHeader` -- misma forma de especificidad que la regla a vencer (una clase + el elemento `th`), sin `!important`.
  - **Fallo propio de verificación de la ronda anterior, reconocido explícitamente**: `scripts/verify-two-level-header-and-full-labels.mjs`/`verify-refusal-reason-and-header-tooltips.mjs` medían `boundingBox()` del propio `<th>` (la celda `colSpan={3}` entera) y lo comparaban contra el span de sus 3 columnas hoja -- esa comparación es CIERTA POR CONSTRUCCIÓN (el `<th>` con `colSpan={3}` ocupa esas 3 columnas exactamente, tenga el texto que tenga el `text-align` que sea), así que el test "pasaba" sin comprobar nunca dónde caía realmente el TEXTO dentro de esa celda -- confirmaba la geometría de la celda, no la alineación del contenido. Nunca se tomó una captura real para confirmarlo visualmente tampoco, tal como señaló el encargo.
  - **Verificación real esta vez** (`scripts/verify-header-centering-css-specificity-fix.mjs`, nuevo): dos comprobaciones que el fallo anterior no puede volver a colar. (1) `getComputedStyle(th).textAlign` -- el valor que el navegador REALMENTE resolvió tras aplicar toda la cascada, no el CSS fuente -- confirmado `"center"` en las 8 celdas de cabecera (2 de grupo + 6 hoja). (2) Posición real de los GLIFOS del texto vía un `Range` de DOM (`range.selectNodeContents(textNode); range.getBoundingClientRect()`), comparada contra el centro de la celda -- coincide en las 8 (±2px), y cada `<th>` hoja comparado además contra el `<td>` de datos real debajo (misma x/ancho exactos) para confirmar que la letra cae sobre su columna de verdad, no solo "centrada en algo". Captura real en `/tmp/projects-header-truly-centered.png` (10 proyectos reales de 2 a 2819 BRDPs, mismo seed reutilizado, borrados al terminar) -- visualmente confirma "Proposal Status"/"Rule Status" centrados sobre sus 3 columnas y cada letra centrada sobre su número. `npm run lint`/`npm run build` limpios; sin cambios de backend.
- **`comments` (motivo de `Refused`) ahora se registra en el histórico** (`backend/app/api/routes/brdps.py`): bug real confirmado por el usuario, no percepción -- `_HISTORY_FIELDS` (el diccionario DB-column→audit-field-name que tanto `update_brdp` como `run_import_job` de `import_jobs.py` recorren para decidir qué campos auditar) nunca incluía `"comments"`, así que el textbox de motivo de rechazo añadido dos rondas atrás guardaba correctamente en Postgres pero no dejaba NINGÚN rastro en el histórico al editarlo -- ni al rellenarlo por primera vez, ni al cambiarlo después. Arreglado con una única entrada nueva, `"comments": "refusal_reason"` -- nombre de auditoría deliberadamente distinto del nombre de columna interno (mismo criterio ya usado por `"validation": "proposal_status"`), ya que la UI nunca llama a este campo "comments", solo "Reason for refusal" (`records.fieldRefusalReason`, añadida cuando se creó el textbox).
  - **Confirmado, no asumido, que `import_jobs.py` hereda el fix automáticamente**: `from app.api.routes.brdps import _HISTORY_FIELDS, _compute_brdp_embedding` (línea 60) importa el MISMO objeto dict, y `run_import_job` lo recorre exactamente igual que `update_brdp` (`for field, history_name in _HISTORY_FIELDS.items(): record_change(...)`, líneas 576-577) -- ninguna copia local, ningún subconjunto propio. No hay Rule/`Rule Status` en las columnas de Excel de import equivalentes a `comments` hoy, así que el camino de import no dispara esto en la práctica todavía, pero el mecanismo queda correcto para cuando lo haga, sin haber tocado `import_jobs.py` en absoluto (confirmado leyendo el fichero, no supuesto por compartir el diccionario).
  - **i18n**: nueva clave `records.history.fields.refusal_reason` (`'Reason for refusal'` / `'Motivo de rechazo'`) en `src/i18n/index.js` -- sin ella, el histórico habría mostrado el nombre crudo `refusal_reason` como fallback de `t(..., {defaultValue: h.field_name})`, violando HR21 (humanizar texto de UI). El VALOR del cambio (texto libre, no un enum) no necesita entrada en `HISTORY_TRANSLATED_FIELDS` -- ya cae correctamente en la rama por defecto de `formatHistoryValue` (texto tal cual, o `—` si vacío). Deliberadamente NO añadido a `REVERTIBLE_HISTORY_FIELDS` (`RecordsPage.jsx`) -- el encargo no pedía un botón "Revert to this" para este campo, y es una feature más consecuente (una acción, no solo lectura) que no se amplía sin que se pida explícitamente.
  - **3 tests nuevos** en `backend/tests/test_brdp_history.py` (224 tests backend en total, todos en verde) cubren los 3 casos límite exactos del encargo: marcar `Refused` con motivo relleno produce una entrada `refusal_reason` con `old_value=""`; editar el motivo de una BRDP ya `Refused` (sin tocar el estado) produce una SEGUNDA entrada independiente, igual que ya hace `proposal` al editarse solo; una BRDP nunca marcada `Refused` no tiene ninguna entrada `refusal_reason` aunque se editen otros campos.
  - **Verificado con Playwright real** (`scripts/verify-comments-history-tracking.mjs`, nuevo, auto-limpia lo que siembra): reproduce los 3 casos límite contra `Demo Project` real -- marcar `Refused` + rellenar motivo aparece de inmediato como entrada "Reason for refusal" con old="—"/new=el texto; editar el motivo otra vez (estado ya `Refused`, sin tocarlo) añade una SEGUNDA entrada distinta con old=el texto anterior/new=el texto nuevo; una BRDP nunca `Refused` pero editada en `title` no muestra ninguna entrada `refusal_reason`. Cruzado además contra la API directa (`GET .../history`): exactamente 2 filas `field_name="refusal_reason"` con los pares old/new correctos, y confirmado que NINGUNA fila usa el nombre de columna crudo `"comments"`. Captura real en `/tmp/comments-history-tracking.png` -- histórico visible con las dos entradas reales "REASON FOR REFUSAL" (tachado el valor antiguo, en negrita el nuevo) apiladas sobre la entrada "PROPOSAL STATUS" de Pending→Refused, sin botón "Revert to this" en ninguna de las dos (confirma que no se amplió `REVERTIBLE_HISTORY_FIELDS` sin pedirlo). Bug real encontrado y corregido EN EL PROPIO ENTORNO de verificación durante esta ronda, no en la app: el primer intento de captura dio 0 entradas nuevas porque el proceso `uvicorn` seguía siendo el arrancado ANTES de editar `_HISTORY_FIELDS` -- exactamente el caso ya documentado en este fichero ("si editas código Python del backend con el servidor ya arrancado, hay que reiniciarlo a mano"); tras matar el proceso viejo y relanzar `uvicorn`, la verificación pasó limpia. `npm run lint`/`npm run build` limpios.
- **"Ask a Question": prompt nuevo scopeado a la BRDP, encadenado de un turno y comparación opcional con otra BRDP** (`src/pages/RecordsPage.jsx`, `src/i18n/index.js`; sin cambios de backend -- todo lo necesario, incluido `rule_xml` por BRDP vía `GET .../approvals/{format}`, ya existía) -- `askGeneric()` pasa de una frase genérica + `identifier`/`definition` pegados al mensaje de usuario, sin turnos previos, a un pipeline con contexto completo, memoria acotada y comparación opcional. `buildSystemPrompt()` de `llmAPI.js` (código muerto ya documentado -- bug `comment`/`comments` y UUID en vez de identificador) confirmado sin tocar ni reutilizar, tal como pedía el encargo.
  - **`buildAskSystemPrompt(brdp, ruleApproval, compareBrdp)`** (nueva, módulo aparte dentro de `RecordsPage.jsx`, no inline en `askGeneric`): construye el system prompt literal del encargo, con el bloque de contexto real de la BRDP seleccionada -- `ID`/`Title`/`Definition`/`Proposal`/`Proposal Status`, `Refusal reason` SOLO si `validation === 'Refused'` y hay `comments` (reutiliza el campo cableado en el histórico dos rondas atrás), y `Rule Status`/`Rule` derivados de `ruleApproval` (mismo `ruleStateOf()` que ya usa el stepper -- `Rule: Not yet defined` para `to_do`, sin error, caso límite del encargo). `RULE_STATUS_LABELS` es el mismo mapa local `{todo:'To Do',draft:'Draft',verified:'Verified'}` ya duplicado en `ProjectConfigPage.jsx`/`GenerateBREXdocPage.jsx` -- mismo criterio "no centralizar" ya documentado en este fichero, tercera copia consistente con las otras dos.
  - **Truncado explícito de `Rule`, nunca silencioso (HR7)**: `ruleTextForAsk()` corta cualquier `rule_xml` por encima de `ASK_RULE_MAX_CHARS = 6000` y añade `[Rule truncated at 6000 characters]` dentro del propio prompt -- caso límite del encargo ("una Rule muy larga... confirmar que no rompe el límite de contexto; si hace falta, truncar con aviso explícito, nunca en silencio"). Verificado con una Rule real de más de 7000 caracteres (un comentario XML de relleno bien formado + la regla real): el prompt recibido por el proveedor trae el marcador de corte y NO contiene el relleno completo más allá del corte.
  - **Encadenado de un turno**: nuevo estado `prevTurn` (`{question, answer}` o `null`) -- `askGeneric` antepone `[{role:'user',content:prevTurn.question},{role:'assistant',content:prevTurn.answer}]` al array de `messages` antes de la pregunta nueva, solo cuando hay un turno previo, y lo actualiza tras cada respuesta con éxito. `sendMessage()` (`llmAPI.js`) ya aceptaba un array de mensajes real -- no tocado, el cambio es enteramente en qué le pasa `askGeneric`. Verificado que un seguimiento real ("And why?") encadena exactamente 3 mensajes no-`system` (pregunta anterior, respuesta anterior verbatim, pregunta nueva), nunca más -- un historial ilimitado no era lo pedido.
  - **Bug corregido (pedido explícito del encargo, confirmado antes de tocar código)**: cambiar de BRDP seleccionada NO reseteaba `question`/`answer` -- una respuesta de la BRDP anterior seguía visible (y se habría encadenado) tras seleccionar otra. Nuevo `useEffect` dedicado, sobre `[selected?.id]`, resetea `question`/`answer`/`prevTurn`/`compareOpen`/`compareQuery`/`compareBrdp` juntos -- mismo patrón ya usado por el efecto de `ruleApproval`/`history` que ya resetean sobre el mismo cambio de selección.
  - **`Clear`**: enlace pequeño (`.linkButton`, nueva clase CSS -- sin borde/fondo, mismo azul ATEXIS que el resto de acciones primarias) bajo la respuesta -- limpia `question`/`answer`/`prevTurn` sin tocar la BRDP seleccionada ni la comparación activa.
  - **`+ Compare with another BRDP`** (colapsado por defecto): enlace pequeño bajo el textarea. Al abrirse, busca en dos fuentes reales combinadas en una sola lista -- las BRDPs del proyecto ya cargadas en `brdps` (Records, filtro client-side por id/título, excluyendo la propia BRDP seleccionada) y `GET /api/brdp-catalog?standard=...` (Catálogo, cargado una sola vez de forma perezosa la primera vez que se abre -- reutiliza exactamente el mismo patrón ya usado por el picker de Add BRDP, dato de referencia global no ligado a la BRDP seleccionada así que sobrevive a un cambio de BRDP sin refetch). Cada resultado lleva una etiqueta de origen (`Records`/`Catalog`, `.compareSourceTag`). Elegir una entrada de Records dispara un fetch dedicado a `GET .../approvals/{format}` para esa BRDP concreta -- necesario porque `ruleApprovalsById` (el mapa ya cargado en bulk para ordenar la columna Rule Status) solo trae `status`, nunca `rule_xml`, así que no basta para construir el bloque de comparación; mismo endpoint y forma que ya usa el `ruleApproval` de la BRDP seleccionada. Elegido, se muestra como chip (`Comparing with: BRDP-XXX ✕`, `.compareChip`) y el system prompt gana un segundo bloque: `BRDP being compared against (source: Records|Catalog):` con `ID`/`Title`/`Definition` siempre, más `Proposal`/`Proposal Status`/`Rule Status`/`Rule` SOLO si el origen es Records (el catálogo no tiene esos campos -- confirmado que el bloque de Catálogo nunca lleva esas líneas, ni vacías ni con placeholder, tal como pedía el caso límite del encargo), cerrado con la instrucción explícita "The user may ask you to compare the current BRDP with the one above; in that case both are in scope." Quitar el chip (`✕`) o cambiar de BRDP seleccionada limpia la comparación.
  - **Verificación real contra el proveedor configurado, con una limitación de entorno honesta**: este sandbox tiene `ACTIVE_LLM_PROVIDER=mistral` configurado de verdad (`GET /api/config/ai-provider` → `{"provider":"mistral",...}`), pero confirmado por el propio log de `uvicorn` que la salida a `api.mistral.ai` está bloqueada a nivel de RED, no solo por la clave placeholder del `.env`: `httpcore.ProxyError: 403 Forbidden` del proxy de agente, antes siquiera de que Mistral rechace la clave. No es un bug de la app ni algo reproducible con una clave real distinta -- mismo tipo de limitación de sandbox ya documentada en otros puntos de este fichero (datos de producción no disponibles aquí). Arreglado el mismo tipo de brecha con el mismo criterio ya establecido para `import`/embeddings (`mock-mistral-embed-server.mjs`): `scripts/mock-mistral-chat-server.mjs` (nuevo) mockea el ENDPOINT de chat completions de Mistral (no el proxy de la app, no el código de negocio) -- `uvicorn` se relanza con `MISTRAL_ENDPOINT=http://localhost:8902` solo durante la verificación, la petición real sigue viajando por el camino real de producción (`RecordsPage.jsx` → `sendMessage()` → `POST /api/llm-proxy` → `_resolve_provider()` → el endpoint configurado), solo que ese endpoint apunta al mock en vez de a Internet. El mock expone `GET /last-request` (mismo espíritu que `GET /calls` del mock de embeddings) para que el script de verificación pueda comprobar el system prompt y el array de mensajes EXACTOS que la app mandó, no solo que "se mandó algo", y responde de forma determinista según el contenido recibido (detecta una pregunta fuera de tema por patrón, y si hay más de un mensaje no-`system` para simular el seguimiento) para poder ejercitar esos dos caminos reales de UI sin depender de comprensión real de lenguaje. Tras verificar, `uvicorn` se relanza sin el override, dejando el entorno como estaba.
  - **`backend/scripts/seed_ask_compare_catalog.py`** (nuevo, reutilizable, acepta `cleanup` como argumento): este sandbox no tiene NINGÚN catálogo oficial importado para ningún standard (`GET /api/brdp-catalog/count` da `0` en los 5 standards reales, confirmado antes de escribir nada) -- sin esto, la mitad "Catalog" del buscador de comparación sería intestable aquí. Siembra 2 filas reales en `brdp_catalog` para `S1000D 4.2` directamente en Postgres (mismo patrón que los demás scripts de seed de esta rama), limpiadas al final de la ronda con `cleanup`.
  - **`scripts/verify-ask-question-scope-and-compare.mjs`** (nuevo, auto-limpia las 2 BRDPs que siembra vía API + trash): dos BRDPs reales en `Demo Project` -- BRDP-ASK-TEST-A (`Refused` con motivo real, Rule `Draft` con más de 7000 caracteres) como la seleccionada, BRDP-ASK-TEST-B (`Validated`, Rule `Verified`) como objetivo de comparación Records. Cubre, contra el proveedor real (transporte mockeado): pregunta normal (respuesta real, prompt confirmado con `ID`/`Title`/`Proposal Status`/`Refusal reason`/`Rule Status`/`Rule` truncado, exactamente 1 mensaje no-`system`, sin turno previo); pregunta de seguimiento ("And why?") encadenada (exactamente 3 mensajes no-`system`, pregunta+respuesta anteriores verbatim + la nueva); `Clear` (vacía pregunta/respuesta, y confirma que la siguiente pregunta YA NO encadena); pregunta fuera de tema (el mock detecta el patrón y responde con el rechazo simulado, ejercitando el camino real de UI); comparación con una BRDP de Records (chip real, bloque de prompt con los 4 campos extra reales incluida la Rule de verdad, la instrucción final); quitar el chip; comparación con una entrada de Catálogo real (chip real, bloque de prompt SOLO con `ID`/`Title`/`Definition`, confirmado que NO aparecen `Proposal Status:`/`Rule Status:` en ese bloque); y cambiar de BRDP seleccionada limpia pregunta/respuesta/turno previo/chip de comparación, con el enlace `+ Compare` vuelto a su estado colapsado. Captura real en `/tmp/ask-question-compare.png` -- panel de detalle con el histórico de Refusal reason visible, el chip `Comparing with: BRDP-CAT-ASKTEST-001 ✕`, y una respuesta `MOCK-FOLLOWUP` real bajo el enlace `Clear`. `npm run lint`/`npm run build` limpios; suite backend completa (224 tests, sin cambios de backend en esta ronda) re-ejecutada en verde tras devolver `uvicorn` a su configuración normal.
- **Ronda siguiente sobre "Ask a Question": mini-hilo de conversación + markdown real en la respuesta** (`RecordsPage.jsx`/`.module.css`, `src/i18n/index.js`; sin cambios de backend) -- la ronda anterior ya dejaba el encadenado de un turno funcionando por debajo, pero la UI seguía leyéndose como un formulario: tras responder, la pregunta se quedaba escrita en el textarea y la respuesta aparecía debajo del botón `Ask` como texto plano (el markdown del LLM -- `**negrita**`, listas, ``código``  -- se veía en crudo).
  - **Estado reestructurado**: `question` pasa a ser SOLO el borrador vivo del textarea (se vacía solo al recibir respuesta, se deja intacto si falla). Nuevos `askError` (mensaje de error o `null`), `lastAsked` (la pregunta del intercambio actualmente mostrado -- pendiente, respondida o con error -- o `null` si no hay nada que mostrar todavía) y `askPending` (booleano dedicado SOLO a esta petición, separado del `busy` compartido con los botones Suggest, para que un `Suggest Rule` en curso nunca haga parecer que el intercambio de Ask está cargando). `prevTurn` (memoria del turno anterior para el encadenado, de la ronda pasada) sigue poniéndose SOLO en éxito -- una pregunta con error nunca pasa a ser algo que el LLM "recuerda".
  - **Orden nuevo en el panel** (docs request, de arriba abajo): (1) el último intercambio, si existe -- `Tú: <pregunta>` en estilo discreto (`.exchangeQuestion`/`.exchangeYou`) y justo debajo la respuesta/error/indicador de carga, con `Clear` asociado; (2) el textarea, cuyo placeholder cambia a `askFollowupPlaceholder` (`'Ask a follow-up about this BRDP…'`/`'Haz una pregunta de seguimiento sobre este BRDP…'`) en cuanto hay `prevTurn`, y vuelve a `askPlaceholder` si no lo hay o tras `Clear`; (3) `+ Compare with another BRDP` y el botón `Ask` (sin cambios de posición relativa entre ellos, solo desplazados por debajo del intercambio+textarea).
  - **`askGeneric`**: al enviar, `setLastAsked(askedQuestion)` ocurre ANTES del `await` -- la pregunta aparece de inmediato en la zona de intercambio con `Thinking…` (`records.assistant.thinking`) mientras se espera, tal como pedía el encargo ("mostrar la pregunta enviada ya... con un indicador de carga"). Éxito: `setAnswer`/`setPrevTurn` + `setQuestion('')` (vacía el textarea). Error: `setAskError(err.message)`, el textarea NUNCA se toca -- el usuario no pierde lo escrito. Como `lastAsked` se sobreescribe en cada envío (nunca se acumula un array), "solo se muestra un intercambio" queda garantizado por construcción, y coincide exactamente con lo que `askGeneric` manda como historial (el turno anterior completo + la pregunta nueva) -- lo que se ve y lo que el LLM recuerda son la misma cosa, tal como pedía el encargo explícitamente.
  - **`clearAsk`** ya NO toca `question` (antes sí) -- en la UI nueva el textarea es un borrador independiente del intercambio mostrado (se vacía solo en éxito, o conserva un error en curso), así que "empezar de cero" solo debe limpiar el intercambio (`answer`/`askError`/`lastAsked`) y `prevTurn`, nunca destruir algo que el usuario esté escribiendo en ese momento.
  - **`Enter`/`Shift+Enter`**: `onKeyDown` en el textarea -- `Enter` sin `Shift` hace `preventDefault()` + `askGeneric()` (con guarda `!busy` para no duplicar el envío si se pulsa dos veces rápido, mismo criterio que ya usa el `disabled` del botón); `Shift+Enter` no se intercepta, cae al comportamiento nativo del `<textarea>` (salto de línea).
  - **Markdown real vía `react-markdown`** (ya en `package.json`, usada de referencia en el `ChatPanel.jsx` muerto documentado en rondas anteriores -- mismo uso mínimo por defecto, `<ReactMarkdown>{text}</ReactMarkdown>`, sin plugins): sustituye el `{answer}` de texto plano dentro de `.answerBox`. Deliberadamente SIN `rehype-raw` ni similar -- el comportamiento seguro por defecto de `react-markdown` (nunca interpreta HTML embebido en el markdown fuente) es el pedido explícitamente, mismo principio que el arreglo de inyección HTML de `buildBREXdocReport.js` (`90b7e12`): un `<table>`/`<originator>` literal en la respuesta del LLM se ve como texto, nunca se parsea como elemento real. La pregunta del usuario (`lastAsked`) se sigue mostrando como texto plano sin pasar por `ReactMarkdown` -- no lo pedía el encargo.
  - **CSS**: `.answerBox`/`.suggestionBox` se separan -- `.suggestionBox` (texto plano de Suggest Definition/Proposal/Rule) conserva `white-space: pre-wrap`; `.answerBox` lo pierde (sus párrafos ya traen su propio salto de línea vía markdown real, `pre-wrap` heredado habría duplicado saltos) y gana reglas compactas para `p`/`ul`/`ol`/`li`/`code`/`pre` (márgenes reducidos, pensados para el panel lateral estrecho, no los por-defecto de un documento completo). Nuevas `.exchange`/`.exchangeQuestion`/`.exchangeYou` para el estilo discreto de la pregunta mostrada.
  - **Verificación real contra el proveedor configurado** (mismo criterio de la ronda anterior: `api.mistral.ai` sigue bloqueado a nivel de red en este sandbox -- `httpcore.ProxyError: 403 Forbidden`, confirmado de nuevo antes de tocar nada -- así que se reutiliza `scripts/mock-mistral-chat-server.mjs` con `MISTRAL_ENDPOINT` apuntando a él solo durante la verificación, restaurado al terminar): el mock gana tres disparadores deterministas nuevos -- `MARKDOWN_TEST` (responde con negrita/lista/cursiva/código real para poder comprobar el renderizado), `HTML_TEST` (responde con `<table>`/`<originator>` sin comillas, para el caso límite de inyección) y `ERROR_TEST` (responde 500, para ejercitar el camino de error real de `askGeneric` sin depender de una condición de red inestable).
  - **`scripts/verify-ask-question-thread-and-markdown.mjs`** (nuevo, auto-limpia las 2 BRDPs que siembra vía API + trash): confirma, contra el proveedor real (transporte mockeado) y por UI real -- sin exchange antes de la primera pregunta; la pregunta enviada aparece de inmediato en la zona de intercambio con `Thinking…` visible mientras se espera; tras la respuesta, el textarea queda vacío Y el placeholder cambia a la variante de seguimiento; una pregunta de seguimiento SUSTITUYE el intercambio mostrado (nunca lo acumula -- confirmado que la pregunta anterior deja de ser visible) y también vacía el textarea; `**MOCK-MARKDOWN**`/lista/`*emphasized*`/``código`` se renderizan como elementos DOM reales (`<strong>`/`<li>`/`<em>`/`<code>`, confirmado con locators específicos, no solo con una captura), sin que quede ningún asterisco/backtick crudo visible; una respuesta con `<table>`/`<originator>` sin comillas se ve como texto literal Y se confirma por DOM real que NO existe ningún `<table>` ni `<originator>` real en la página; `Clear` vacía el intercambio y revierte el placeholder; un error de red real (disparado con `ERROR_TEST`) muestra el mensaje de error donde iría la respuesta Y dejar el texto exacto ("ERROR_TEST please") intacto en el textarea; y cambiar de BRDP limpia el intercambio y revierte el placeholder. Capturas reales en `/tmp/ask-thread-followup.png` (intercambio visible arriba, textarea vacío tras un seguimiento, placeholder de seguimiento), `/tmp/ask-thread-markdown-code.png` (negrita/lista/cursiva/código inline renderizados de verdad) y `/tmp/ask-thread-error.png` (error mostrado donde iría la respuesta, textarea con el texto intacto). `npm run lint`/`npm run build` limpios; sin cambios de backend en esta ronda (no se re-ejecutó la suite pytest completa por no haber tocado ningún fichero Python), `uvicorn` devuelto a su configuración normal (sin el mock) al terminar.
- **Ronda siguiente sobre "Ask a Question": el standard del proyecto se incluye en el system prompt** (`RecordsPage.jsx`; sin cambios de backend) -- bug real reportado por el usuario con el proveedor real, no hipotético: `buildAskSystemPrompt` hablaba genéricamente de "an S1000D and DITA business-rules expert assistant" sin decir NUNCA qué standard/versión es el del proyecto actual, así que el LLM respondía cubriéndose entre los dos mundos ("`<originator>` en S1000D o `<prodinfo>` en DITA") incluso en un proyecto inequívocamente `S1000D 3.0.1`.
  - **`buildAskSystemPrompt(brdp, ruleApproval, compareBrdp, standard)`** gana un cuarto parámetro -- `project.standard` (ya disponible en el componente vía `useOutletContext()`, no requiere ningún fetch nuevo) -- y un bloque nuevo insertado justo antes del contexto de la BRDP, con el texto literal del encargo: `This project uses the standard: {standard}.` seguido de la instrucción de no mezclar versiones salvo que el usuario pida explícitamente una comparación. Un único call site (`askGeneric`) pasa `project.standard`.
  - **Deliberadamente NO repetido en el bloque de comparación**: si hay una BRDP de comparación del catálogo, ese bloque ya es del mismo standard por construcción (`GET /api/brdp-catalog?standard=...` se filtra por el standard del proyecto actual, confirmado leyendo `openCompareSearch` de la ronda de comparación) -- añadir la línea otra vez ahí habría sido ruido redundante, no información nueva.
  - **Verificación real, sin inventar un framework de test nuevo** (`buildAskSystemPrompt` es una función privada de módulo dentro de `RecordsPage.jsx`, no exportada, y este repo no tiene test runner JS -- mismo criterio ya documentado en este fichero): en vez de un test unitario aislado, se capturó el prompt REAL que la app construye y manda, por el camino real de código, para dos proyectos de standard genuinamente distinto -- los dos casos límite que cita el propio encargo. Reutiliza `scripts/mock-mistral-chat-server.mjs`/`GET /last-request` (mismo mecanismo ya establecido en las dos rondas anteriores de Ask a Question) con `MISTRAL_ENDPOINT` apuntando al mock solo durante la verificación.
  - **`scripts/verify-ask-question-standard-in-prompt.mjs`** (nuevo, auto-limpia los 2 proyectos que siembra vía API): crea dos proyectos reales, `S1000D 3.0.1` y `DITA 1.3 Xpath3.0` (el sandbox no tenía ninguno de los dos antes de esta ronda -- solo `Demo Project (S1000D 4.2)`), una BRDP en cada uno, hace una pregunta real por UI en cada proyecto y captura el system prompt exacto recibido por el mock. Confirma para AMBOS: la línea `This project uses the standard: <standard>.` aparece con el string exacto (incluida, para DITA, la parte del dialecto XPath -- `DITA 1.3 Xpath3.0`, no solo `DITA 1.3`, el caso límite explícito del encargo); los dos prompts capturados son genuinamente distintos entre sí (no un string estático); y ninguno de los dos menciona el standard del OTRO proyecto. Los dos prompts completos capturados quedan impresos en el log del script (el "log de `buildAskSystemPrompt`" que pedía el cierre del encargo, ya que no hay forma de testearla unitariamente sin romper la convención "no hay test runner JS, no inventar uno" de este repo). `npm run lint`/`npm run build` limpios; sin cambios de backend en esta ronda, `uvicorn` devuelto a su configuración normal (sin el mock) al terminar.
- **Embeddings bajo demanda -- fuera del import y del guardado manual** (backend: `app/models/brdp.py`/`brdp_catalog.py`/`embedding_job.py` nuevo, `app/services/embeddings.py`/`embedding_jobs.py` nuevo, `app/api/routes/embedding_jobs.py` nuevo, `app/schemas/embedding_job.py` nuevo, `alembic/versions/0014_on_demand_embeddings.py`; frontend: `src/hooks/useEmbeddingJob.js` nuevo, `RecordsPage.jsx`, `ProjectConfigPage.jsx`, `SettingsPage.jsx`, `src/i18n/index.js`): cambio de modelo confirmado real (`import_jobs.py:530,543` + `brdps.py:245,280` disparaban un embedding real de Mistral en cada validar/editar/importar) a bajo demanda -- validar, editar e importar ya NUNCA llaman a Mistral; el embedding se calcula solo cuando un editor pulsa "Compute embeddings" en un job en segundo plano nuevo.
  - **Detección de pendientes por hash, no por flag**: `brdps.embedding_text_hash`/`brdp_catalog.embedding_text_hash` (columnas nuevas, migración 0014) guardan el SHA-256 del texto que se embebió de verdad (`embeddings.py`'s `compute_text_hash`/`brdp_embedding_text` -- title+definition+proposal -- /`catalog_embedding_text` -- title+definition, el catálogo no tiene proposal). `embedding_jobs.py`'s `is_pending_brdp`/`is_pending_catalog` comparan ese hash guardado contra uno calculado AL VUELO desde el texto ACTUAL de la fila -- una BRDP Validated está pendiente si nunca se embebió (hash `None`) o si el hash ya no coincide (se editó título/definición/proposal después de embeberse). Deliberadamente NO un booleano mantenido por cada write path: `create_brdp`/`update_brdp` en `brdps.py` ya no tocan `embedding`/`embedding_text_hash` en absoluto, así que editar el título de una BRDP ya embebida la marca pendiente de nuevo sin que el código de guardado necesite saberlo -- confirmado con un test real (`test_editing_title_after_embedding_marks_brdp_pending_again`, PUT real por la API, no una mutación directa en BD) y con Playwright real más abajo.
  - **`brdp_catalog` gana `embedding`/`embedding_text_hash`** (nunca los tuvo antes): el mismo job embebe, en una sola pasada, las BRDPs pendientes del proyecto Y las entradas pendientes del catálogo del standard del proyecto -- "el catálogo solo se calcula si tiene pendientes" (docs request) sale gratis de la propia mecánica: si `_pending_catalog()` devuelve una lista vacía, el bucle simplemente no itera nada, sin rama especial.
  - **Migración `0014_on_demand_embeddings.py`**: añade las 3 columnas nuevas, **pone a `NULL` `embedding`/`embedding_text_hash` de TODAS las BRDPs existentes** (la composición del texto cambió de `definition+proposal` a `title+definition+proposal` -- "no mezclar embeddings de composiciones distintas", pedido explícito), crea `embedding_jobs` (mismas columnas que `import_jobs`: `id`/`project_id`/`started_by`/`status`/`total_items`/`processed_items`/`error`/`result` JSONB `{brdps_embedded, catalog_embedded}`/`started_at`/`finished_at`), y elimina `app_settings` (ver más abajo) y `import_jobs.validated_rows_total` (ver más abajo) por HR13. `downgrade()` real: recrea `app_settings` reseedeado con los mismos valores fallback que la 0009, recrea `validated_rows_total`, dropea `embedding_jobs` y las 3 columnas nuevas -- las embeddings calculadas bajo la composición nueva NO se borran al hacer downgrade (dato real y válido bajo la composición que estaba vigente cuando se calcularon; no hay ya ningún camino inline que "volver a" recalcularlas bajo la composición vieja). Probada de verdad en ambos sentidos contra Postgres real (`alembic upgrade head` → verificar columnas/tabla → `alembic downgrade -1` → verificar reversión exacta incluido el reseed de `app_settings` → `alembic upgrade head` de vuelta), no solo revisada por lectura.
  - **`embedding_jobs.py` (servicio) difiere deliberadamente de `import_jobs.py` en un punto**, documentado en su propio docstring: `run_embedding_job` hace `work_session.commit()` tras CADA item, no una sola vez al final. Import necesita atomicidad todo-o-nada porque sus filas interactúan (mismo identifier, match de catálogo, upsert de `rule_approvals`); un embedding no tiene esa interacción entre filas -- es una llamada cara e independiente a Mistral por fila, así que un job que muere a mitad debe conservar lo ya calculado (sin re-pagar esas llamadas) en vez de descartarlo. El resto del patrón SÍ se reutiliza tal cual: dos sesiones (`work_session`/`progress_session`), `STALE_JOB_MINUTES=60` + reap del mismo tipo que `import_jobs.py`, `except asyncio.CancelledError` explícito (mismo motivo real ya documentado: hereda de `BaseException` desde Python 3.8, no de `Exception`), un solo job `running` por proyecto vía `get_running_job` + 409 en `POST /compute`.
  - **`similar.py`**: dos cambios, uno pedido y uno encontrado leyendo el código. (1) `excluded_pending_other_projects` nuevo en `SimilarOut` (HR7 -- nunca degradar en silencio): cuenta BRDPs Validated de OTROS proyectos del mismo standard con `embedding IS NULL` -- deliberadamente solo "otros proyectos", nunca el proyecto actual (sus propias pendientes ya bloquean Suggest por completo vía el punto 6 del encargo, así que nunca llegan aquí en la práctica). (2) hallazgo propio no pedido explícitamente: `query_text` (el texto que se embebe para la BRDP origen de Suggest) usaba `definition+proposal`, la composición VIEJA -- con el resto del sistema ya en `title+definition+proposal`, una búsqueda de similitud real habría comparado dos espacios de embedding incompatibles. Corregido reusando el mismo helper compartido (`brdp_embedding_text`) que usa el job, para que query y candidatos usen siempre la misma composición.
  - **Import** (`import_jobs.py`/`brdp_import.py`): eliminadas las dos llamadas a `_compute_brdp_embedding` (creación y update) y el parámetro `transport` de `run_import_job`/`apply_import` (ya no hace falta, import no toca `httpx` en absoluto). El comentario de la rama `unchanged` de una ronda anterior ("critically no `_compute_brdp_embedding` call") se reescribió -- ya no hay ninguna llamada de embedding que saltar en absoluto, en ninguna rama, así que el motivo de existir de esa optimización pasó de "ahorrar Mistral" a simplemente "no escribir campos/historial que no cambiaron".
  - **AppSettings / Import ETA eliminado por completo (HR13, sin código muerto)**: confirmado por grep exhaustivo que nada fuera del propio mecanismo lo consumía. Eliminados: `backend/app/models/app_settings.py`, `app/api/routes/app_settings.py`, `app/schemas/app_settings.py`, `backend/tests/test_app_settings.py`, la tabla `app_settings` (migración 0014) y la columna `import_jobs.validated_rows_total` (ya no tiene sentido: solo alimentaba el aviso "N filas disparan una llamada real de embeddings" durante el import, que ya no existe). Frontend: `ProjectConfigPage.jsx` pierde `computeApplyEta`/`applyEtaMessage`/el modal de confirmación "Apply grande" (`showApplyConfirm`/`dontAskAgain`/`handleApplyClick`/`handleConfirmProceed`/`handleConfirmCancel`) -- el botón Apply llama a `handleApplyImport` directamente, sin gate; el resumen `unchangedRows`/`summaryUnchanged` SÍ se mantiene (sigue siendo información real y válida, no relacionada con el coste de embeddings). `SettingsPage.jsx` pierde la sección entera `ImportEtaSettingsSection`. Borrado `src/hooks/useImportEtaSettings.js`. i18n: `config.dataManagement.applyEta*`/`applyConfirm*`/`jobValidatedContext` y el bloque `settings.importEta.*` completo (EN+ES) eliminados.
  - **UI nueva (`RecordsPage.jsx`, zona de Suggest, nunca en Ask -- Ask no usa embeddings)**: `usePendingEmbeddings`/`useActiveEmbeddingJob`/`useComputeEmbeddings`/`useInvalidatePendingEmbeddings` (`useEmbeddingJob.js` nuevo, mismo patrón que `useImportJob.js` -- cache compartida de React Query, `refetchInterval` condicional a `status==='running'`). Si hay pendientes (proyecto o catálogo) o el job está corriendo: banner `"N BRDPs pending embedding"` + botón `"Compute embeddings"` (solo editor) cuando NO corre, o `"Computing embeddings… N/M processed"` + `<progress>` real + una estimación de tiempo restante cuando SÍ corre. La estimación de ETA es deliberadamente un cálculo en vivo desde el progreso real observado (`elapsed / processed * restantes`, `estimateEmbeddingEtaSeconds`), NO un ms-por-fila configurado como el mecanismo eliminado -- HR8, nada hardcodeado, y ya no hay ninguna pantalla de Settings de la que leerlo. Los 3 botones Suggest quedan `disabled` mientras haya pendientes (proyecto o catálogo) o el job esté corriendo. La respuesta de `/similar` que trae `excluded_pending_other_projects > 0` se muestra como aviso propio, separado del de "insufficient precedent" (pueden darse los dos a la vez o por separado).
  - **Tests backend**: `test_embedding_jobs.py` nuevo (15 tests -- pendiente por hash nunca-embebido/hash-coincide/editar-título-tras-embeber, catálogo scopeado por standard, compute embebe BRDPs+catálogo y limpia pendientes, catálogo se salta si no tiene pendientes, 403 viewer en `/compute`, 200 viewer en lecturas, 409 con job corriendo, reap de job `stale`, `CancelledError` deja `failed` con mensaje claro y libera el bloqueo, `/status/active` devuelve el job más reciente, `/status/{id}` rechaza job de otro proyecto, 401 sin auth). `test_brdp_import.py`: los 3 tests que probaban "no recalcular embedding en reimport sin cambios" se sustituyen por `test_reimport_fully_unchanged_row_skips_history_write` (el mismo comportamiento de histórico, ya sin nada de embeddings) y `test_import_never_calls_mistral_regardless_of_validated_rows` (nuevo, `monkeypatch` de `compute_embedding` para que lance si se llama -- garantía más dura que solo contar llamadas a un mock, cubre el caso SOPTE del encargo explícitamente); el test de "fallo de embedding a mitad de job" se elimina (ya no existe ese modo de fallo durante import). `test_brdp_trash.py`'s test de precedente reescrito para fijar `.embedding` directo por sesión de BD (mismo patrón que `test_similar.py`) en vez de confiar en que `PUT .../brdps/{id}` con `validation=Validated` lo calculara solo. `test_similar.py` gana 2 tests para `excluded_pending_other_projects` (cuenta real de otro proyecto, scopeado a otro standard poniendo cero, nunca cuenta pendientes del proyecto propio). Fixtures `_mock_embeddings_transport` ya sin uso (nada llama a Mistral en validar/editar) eliminadas de `test_brdp_history.py`/`test_brdp_status_counts_and_filters.py`/`test_brdps_notes_approvals.py`. Suite completa: 234 tests backend en verde.
  - **Verificación real con Playwright** (`scripts/verify-on-demand-embeddings.mjs`, nuevo, auto-limpia los 2 proyectos que siembra vía API; `scripts/mock-mistral-embed-server-delayed.mjs`, nuevo -- mismo mock de siempre pero con 700ms de retardo por llamada para que el estado "corriendo" del job sea observable en vez de terminar entre un poll y el siguiente; reutiliza `mock-mistral-chat-server.mjs` con `MISTRAL_ENDPOINT` apuntando a él, mismo criterio ya establecido en las rondas de Ask a Question): importa un Excel real de 5 filas `Validated` por la UI real -- confirmado con el contador `/calls` del mock que da CERO llamadas a Mistral durante el import; banner "5 BRDPs pending embedding" real + botón "Compute embeddings" visibles, los 3 botones Suggest deshabilitados; lanzar el job muestra progreso real (`<progress>` con un valor parcial genuino observado, ni 0 ni completo) y una estimación de tiempo mientras corre; al terminar, exactamente 5 llamadas reales al mock (una por BRDP pendiente), `GET /pending` confirma `{0,0}` por API directa, banner y botón desaparecen, Suggest se habilita; un segundo proyecto del mismo standard con 2 BRDPs Validated sin embeber dispara el aviso real `"2 BRDPs from other projects excluded: pending embedding"` al pedir Suggest Definition. Bug real encontrado y corregido EN EL PROPIO SCRIPT de verificación durante esta ronda, no en la app: el primer intento esperaba a que desapareciera el texto "pending embedding" para confirmar que el job había terminado, pero ese texto desaparece en cuanto el job EMPIEZA (se sustituye por "Computing embeddings…"), así que la espera se resolvía de inmediato y la comprobación de pendientes corría con el job aún a mitad -- confirmado con una reproducción directa por API (sin UI) que probó que el backend nunca tiene una ventana real de inconsistencia; corregido esperando a que desaparezca el texto "Computing embeddings" (el del estado EN CURSO), no el del banner de pendientes. Capturas reales en `/tmp/embeddings-pending-banner.png`, `/tmp/embeddings-job-running.png` (progreso + ETA visibles), `/tmp/embeddings-job-completed.png`, `/tmp/embeddings-excluded-notice.png`. `scripts/verify-settings-no-import-eta.mjs` (nuevo, pequeño) confirma por UI real que la sección "Import Settings" ya no existe en Settings. `npm run lint`/`npm run build` limpios; `uvicorn` devuelto a su configuración normal (sin el mock) al terminar.
- **Embeddings por lotes en el job de "Compute embeddings"** (backend: `app/services/embeddings.py`, `app/services/embedding_jobs.py`; sin cambios de frontend) -- confirmado real por el usuario con el proveedor real: `compute_embedding` mandaba `"input": [text]` (un solo texto) y `run_embedding_job` lo llamaba una vez por elemento, a ~3s/elemento medidos; extrapolado, SOPTE (2.819 filas) tardaría ~2h 15min pese a que la API de Mistral acepta una lista completa de textos en `input` y devuelve todos los vectores en una sola respuesta.
  - **`compute_embeddings_batch(texts)`** (nueva, `embeddings.py`) manda la lista COMPLETA que recibe en una única petición y nunca vuelve a trocearla por dentro -- el tamaño de lote es una decisión de quien la llama (`run_embedding_job`), no suya. Empareja cada vector de la respuesta con su texto por el campo `index` de cada elemento de `data`, nunca por posición -- verificado con un test que devuelve la respuesta en orden INVERSO al de la petición y confirma que igual queda bien emparejado. `compute_embedding` (un solo texto) se mantiene sin cambios de comportamiento para el embedding de consulta de `/similar` -- ambas funciones comparten ahora un `_post_embeddings()` interno común (mismo endpoint, mismo manejo de error/429), no duplicado.
  - **Límites de Mistral -- investigados, no asumidos, con una limitación de entorno honesta**: `docs.mistral.ai` está bloqueado a nivel de red desde este sandbox (mismo tipo de limitación ya documentada en este fichero para `api.mistral.ai`), así que no se pudo confirmar contra la página oficial directamente. Con `WebSearch`/`WebFetch` contra fuentes de segundo orden: el límite de tokens por texto de entrada (8192) aparece corroborado de forma consistente en varias fuentes independientes (un mensaje de error real de una integración de Weaviate cita literalmente "max tokens per batch: 8192" para este modelo; varios issues de GitHub de librerías cliente citan la misma cifra como ventana de contexto del modelo) -- usado tal cual como `EMBED_MAX_TOKENS_PER_INPUT`. El límite de elementos por petición (tamaño de lote) NO se pudo confirmar con una cifra única fiable -- fuentes de segunda mano citan 16, 32.000 o 128 sin acuerdo entre sí -- pero el propio cookbook OFICIAL de Mistral (`github.com/mistralai/cookbook`, `mistral/embeddings/embeddings.ipynb`, código real y vigente) hace lotes de 50 textos reales con éxito sobre 1.153 muestras, lo más cercano a confirmación de primera mano alcanzable desde aquí de que 32 (el valor de arranque pedido en el encargo) es seguro. `EMBED_BATCH_SIZE = 32` se mantiene tal cual, documentando esta investigación en el propio código -- si la cifra real de Mistral resultara ser menor, eso sale a la luz como un fallo de lote real (reintentado una vez, luego job `failed` con mensaje claro), nunca en silencio.
  - **Texto demasiado largo, nunca en silencio (HR7)**: `truncate_for_embedding_input(text)` (nueva) corta el texto a una estimación conservadora de caracteres (`_CHARS_PER_TOKEN_ESTIMATE = 3`, sin ratio oficial car/token publicado y alcanzable para el tokenizer real de `mistral-embed`, así que se documenta como aproximación deliberadamente conservadora, no una cifra medida) y añade un marcador explícito (`"[Text truncated to fit the embeddings model's ~8192-token input limit]"`) cuando de verdad recorta algo -- mismo patrón ya usado por `ruleTextForAsk()` en el frontend para Rule en Ask a Question. **Importante**: el hash que se guarda (`embedding_text_hash`) se calcula SIEMPRE sobre el texto COMPLETO sin truncar, nunca sobre el texto realmente enviado a Mistral -- si se hasheara el texto truncado, la fila quedaría "pendiente" para siempre en cuanto se comprobara contra el hash del texto actual completo (bug real que se evitó por diseño, documentado explícitamente en el docstring de la función, no encontrado por accidente).
  - **429 (rate limit), nunca falla el job**: `_post_embeddings()` reintenta un 429 respetando la cabecera `Retry-After` si Mistral la manda (soporta tanto segundos como fecha HTTP, formato `RFC 9110`), o una espera por defecto de 5s si no la manda -- hasta `_MAX_429_RETRIES = 5` veces antes de rendirse con un error explícito (nunca cuelga el job indefinidamente si el endpoint no se recupera). Distinto y por encima del reintento de lote: un 429 nunca consume el presupuesto de "reintentar el lote una vez" de `run_embedding_job` -- es una señal de "espera", no un fallo real.
  - **Guardado y progreso por lote** (`run_embedding_job`, reescrito): `work_session.commit()` tras CADA LOTE (antes era tras cada item individual) y `processed_items += tamaño del lote`. Si un lote falla, se reintenta ESE LOTE una vez (`_compute_batch_with_retry`, separado del reintento de 429 ya interno); si vuelve a fallar, `work_session.rollback()` (solo deshace el lote en curso, nunca sin commitear todavía -- los lotes anteriores ya están comprometidos en transacciones propias, independientes) y el job pasa a `failed` con el mensaje real del error. Mismo criterio de `CancelledError` ya establecido (hereda de `BaseException` desde Python 3.8) -- también hace rollback del lote en curso antes de marcar `failed`.
  - **Casos límite**: lote final más pequeño que `EMBED_BATCH_SIZE` (31 en el caso de 63 elementos) -- cubierto por construcción, `_chunked()` no rellena el último grupo. Texto vacío dentro de un lote -- no necesita código especial: `truncate_for_embedding_input("")` lo deja igual (nunca activa el marcador), y si Mistral llegara a rechazarlo de todos modos, eso sale como un fallo de lote real por el camino ya existente (reintento, luego `failed`), nunca en silencio. Corte a mitad de job -- ya cubierto por el propio diseño (lotes ya comprometidos se quedan, los pendientes se recalculan solos en el siguiente lanzamiento vía el mismo mecanismo de hash ya existente, sin cambios). El sondeo de 3s del frontend ahora muestra saltos por lote en vez de por item (aceptado explícitamente en el encargo, sin cambios de frontend).
  - **`scripts/mock-mistral-embed-server.mjs`** (actualizado, no solo para esta ronda -- reutilizable en el futuro): antes devolvía SIEMPRE un único `data: [{embedding, index: 0}]` sin mirar cuántos textos venían en `input`, lo que habría roto cualquier verificación real de lotes (`compute_embeddings_batch` habría visto un índice por debajo de lo esperado y fallado). Ahora devuelve un elemento por cada texto de `input`, cada uno con su `index` real, en orden INVERTIDO respecto al de la petición (a propósito, para forzar que cualquier script que lo use ejercite de verdad la lógica de reordenar-por-índice) y con un vector derivado del propio texto (hash simple, no un vector constante) para que dos textos distintos den vectores distintos y el mismo texto dé siempre el mismo vector. `/calls` sigue contando peticiones reales, nunca items -- exactamente lo que hace falta para el cierre del encargo. `scripts/verify-batch-embeddings.mjs` (nuevo) confirma contra el backend real + este mock real: 63 BRDPs `Validated` pendientes → exactamente 2 peticiones reales (32 + 31, nunca 63); 1 BRDP pendiente → exactamente 1 petición; ambos jobs terminan `completed` con el `brdps_embedded` correcto y sin nada pendiente después.
  - **Vector asignado a la fila correcta**: verificado de la forma más rigurosa alcanzable, no solo visualmente -- `test_embeddings.py::test_single_and_batch_calls_return_identical_vector_for_the_same_text` recalcula un texto suelto vía `compute_embedding` y confirma que coincide EXACTAMENTE con el vector que `compute_embeddings_batch` le asignó dentro de un lote de 3, contra un mock determinista por texto (el propio método de cierre sugerido en el encargo). `test_embedding_jobs.py::test_batch_assigns_the_vector_matching_each_brdps_own_text` va más allá y lo prueba a través del PIPELINE COMPLETO del job (5 BRDPs reales en Postgres, mock que además devuelve la respuesta en orden invertido) -- lee cada `embedding` de verdad guardado en la fila y confirma que es el que le correspondía a su propio texto, no el de otra fila ni un vector mezclado por el reordenamiento.
  - **Tests backend**: `test_embeddings.py` gana 15 tests nuevos (batch en una sola petición, reordenar por índice contra respuesta invertida, lista vacía sin petición, índice faltante/item malformado → `EmbeddingUnavailable`, single-vs-batch mismo vector para el mismo texto, truncado explícito con marcador y sin tocar texto corto/vacío, 429 respeta `Retry-After` numérico y por fecha HTTP, 429 usa espera por defecto sin cabecera, 429 agota reintentos → `EmbeddingUnavailable` en vez de colgarse, parseo de `Retry-After` en sus 4 formas). El mock de 429 usa un `_FakeAsyncio` inyectado (reemplaza la referencia a `asyncio` DENTRO del módulo `embeddings.py` únicamente, nunca el `asyncio` real global) para que estos tests corran al instante sin esperar de verdad y sin arriesgar interferir con el propio runtime asíncrono de pytest. `test_embedding_jobs.py` gana 3 tests (63→2 peticiones de 32+31, 1→1 petición, asignación de vector por fila a través del job completo) y su fixture `_mock_embeddings_transport` (antes fija a un solo item) se corrige para devolver un item por texto real del lote -- sin este arreglo, CUALQUIER test existente que embebiera más de una fila a la vez habría empezado a fallar con el cambio a lotes. Suite backend completa: 252 tests en verde (234 + 18 nuevos, ninguno reescrito además de la fixture ya mencionada).
- **Suggest Definition: corpus con catálogo, prompt de especialista y referencias visibles** (backend: `app/api/routes/similar.py`, `app/schemas/similar.py`; frontend: `src/pages/RecordsPage.jsx`/`.module.css`, `src/i18n/index.js`) -- afecta SOLO a `kind=definition`; `Suggest Proposal`/`Suggest Rule` quedan byte-idénticos (mismo `MIN_CANDIDATES=3`, mismo pool sin catálogo, mismo `CANDIDATE_LIMIT=10`).
  - **`_get_definition_similar()`** (nueva, `similar.py`), llamada desde `get_similar` justo tras calcular el embedding de consulta cuando `kind=="definition"` (return temprano, el resto de la función -- proposal/rule -- queda sin tocar salvo un `if kind=="definition"` eliminado del bucle de candidatos, ya inalcanzable). Corpus: BRDPs `Validated` con embedding del mismo standard de TODOS los proyectos (excluida la propia, igual que proposal/rule ya hacían -- no hay project_id en el filtro, nunca lo hubo) **más** `brdp_catalog` del mismo standard con embedding. `MIN_CANDIDATES` deja de aplicar a esta rama -- `sufficient_precedent` siempre `True`, `message` siempre `None`, la respuesta se devuelve aunque el corpus esté vacío.
  - **Selección determinista sin cargar el corpus entero en Python**: 4 queries SQL independientes (BRDP top-5 por similitud, BRDP bottom-3, catálogo top-5, catálogo bottom-3, cada una `ORDER BY distance[.desc()], id` -- el `id` como desempate para que el resultado sea reproducible, no dependiente del orden físico de la tabla), fusionadas y reordenadas en Python. Correcto por construcción: el top-N/bottom-N global de dos fuentes nunca puede incluir un elemento fuera del top-N/bottom-N de su propia fuente (una fuente solo puede aportar como mucho N elementos al N global), así que nunca hace falta traer miles de filas (escala SOPTE) para elegir 5+3. `candidates` = hasta `DEFINITION_SIMILAR_LIMIT=5` con similitud `>= MIN_SIMILARITY` (reutilizado de proposal/rule); `style_references` = hasta `DEFINITION_STYLE_REFERENCE_LIMIT=3` (la constante hace doble papel a propósito -- también es el umbral "`< 3` parecidas" que las dispara, mismo número en el encargo) tomadas de menor a mayor similitud, excluyendo por `(tabla, id)` cualquier candidato que ya esté en `candidates` -- probado explícitamente con un corpus de solo 2 elementos, ambos ya en `candidates`, donde `style_references` queda vacío en vez de repetirlos.
  - **Prohibido sobre BRDPs de catálogo**: antes de calcular ningún embedding, `similar.py` comprueba `SELECT count(*) FROM brdp_catalog WHERE standard=... AND identifier=...` (contra la tabla real, nunca por prefijo -- test dedicado confirma que un identificador con la MISMA convención de nombre que el catálogo pero ausente de la tabla se acepta, y que el mismo identificador en el catálogo de OTRO standard tampoco bloquea) -- si existe, 400 antes de gastar una llamada real a Mistral. Frontend: `catalogIdentifierSet` (nuevo, `RecordsPage.jsx`) -- `Set` de identificadores del catálogo del standard del proyecto, cargado una vez por proyecto (a diferencia de `catalogEntries`/`compareCatalogEntries`, que son perezosos y solo cargan cuando su propio panel se abre, este se necesita en cuanto se selecciona una fila) en el mismo `useEffect` que ya resetea los filtros al cambiar de proyecto. El botón "Suggest Definition" lleva `disabled` + `title="Official definition from the standard catalog"` cuando `catalogIdentifierSet.has(selected.identifier)` -- Suggest Proposal/Rule no comprueban nada de esto.
  - **`SimilarCandidateOut`** gana `title` (vacío salvo `kind=definition`) y `source` (`"Records: <nombre del proyecto>"` / `"Catalog"`, formato literal que exige la plantilla del prompt del encargo -- probado con dos puntos, no con guión, tras una primera versión con em-dash corregida antes de escribir el test). `SimilarOut` gana `style_references: list[SimilarCandidateOut] = []` (vacío siempre para proposal/rule).
  - **`buildSuggestDefinitionPrompt(brdp, standard, similar, styleReferences)`** (nueva, módulo-privada en `RecordsPage.jsx`, mismo criterio arquitectónico ya documentado para `buildAskSystemPrompt` -- `/similar` nunca construye prompts, §4). Construida ÍNTEGRAMENTE desde los arrays estructurados que devuelve `/similar` (nunca desde texto del LLM): el bloque `SIMILAR BRDPs` aparece solo si `similar.length>0`, `STYLE REFERENCES` solo si `styleReferences.length>0`, y el mensaje "No reference BRDPs are available..." solo cuando ambos están vacíos -- las tres ramas son mutuamente excluyentes salvo que puedan coexistir `SIMILAR`+`STYLE REFERENCES` (nunca ninguna de las dos con el mensaje de fallback). Mensaje de usuario fijo `"Write the Definition for this BRDP."` (sin historial, a diferencia de Ask a Question) y `temperature: 0.3` vía el `options` que `sendMessage()` ya aceptaba (`llmAPI.js`, sin cambios -- el parámetro ya existía, Suggest Definition es el primer llamador que lo usa).
  - **UI de referencias** (`RecordsPage.jsx`, bloque nuevo dentro de `suggestion?.text`, solo cuando `suggestion.kind==='definition'`): dos grupos "Similar"/"Style references" con `identifier — source — similarity X.XX` (sin similarity para style references, igual que el prompt), generados desde los mismos `suggestion.similar`/`suggestion.styleReferences` que construyeron el prompt -- lo que el usuario ve y lo que vio el LLM son literalmente el mismo array, no dos formateos que puedan divergir. Sin ninguna referencia: `"Definition generated without reference BRDPs"`. El bloque `insufficientPrecedent` ya existente no necesitó ningún caso especial para excluir `kind=definition` -- como el backend ya no devuelve `sufficient_precedent=False` para esa `kind`, y `requestSuggestion('definition')` retorna antes de llegar a esa rama, el mensaje es inalcanzable para Definition por construcción, no por un `if` añadido.
  - **Tests backend** (`test_similar.py`): 6 tests genéricos que en realidad probaban semántica de `MIN_CANDIDATES`/tope-10/`sufficient_precedent` (0 candidatas, 2 candidatas, 15+ candidatas topadas en 10, standard distinto excluido, las dos de `excluded_pending_other_projects`) se cambiaron de `kind=definition` a `kind=proposal` -- seguían siendo válidos, solo que ya no describían el comportamiento real de Definition; documentado en el docstring del módulo por qué. `test_brdp_trash.py::test_deleted_brdp_excluded_from_similar_precedent` (prueba `ACTIVE_BRDP_FILTER`, no la lógica de corpus) se cambió igual. 12 tests nuevos para `_get_definition_similar`: nunca insuficiente con 0 candidatas, candidatos reales de catálogo+Records con `source` correcto, tope en 5, `style_references` solo con <3 parecidas y sin duplicar las ya incluidas, vacío con exactamente 3 parecidas aunque haya candidatas lejanas disponibles, corpus diminuto (2 candidatas, ambas ya en `candidates`) deja `style_references` vacío, catálogo de otro standard excluido, rechazo 400 sobre una BRDP-catálogo, el mismo rechazo NO se dispara por prefijo ni por el identificador de OTRO standard. Bug real encontrado y corregido en el PROPIO test durante esta ronda (no en la app): una primera versión comparaba `close_one.id` (UUID de SQLAlchemy) contra un `set` de ids ya serializados a string por el JSON de la respuesta -- la comparación era `True` por construcción sin comprobar nada; corregido con `str(close_one.id)`. Suite backend completa: 262 tests en verde (252 + 12 nuevos, 2 solo re-etiquetados de `kind`).
  - **Verificación real contra el proveedor configurado** (mismo criterio ya establecido -- `api.mistral.ai` bloqueado a nivel de red en este sandbox, transporte mockeado vía `mock-mistral-chat-server.mjs`/`mock-mistral-embed-server.mjs`, `MISTRAL_ENDPOINT` de `uvicorn` apuntado al mock de chat solo durante la verificación): `scripts/verify-suggest-definition-corpus.mjs` (nuevo) y `backend/scripts/seed_suggest_definition_catalog.py` (nuevo, reutilizable, mismo patrón que `seed_ask_compare_catalog.py` -- siembra 1 fila de catálogo real para `S1000D 4.2`, reutiliza además las 2 filas ya sembradas por `seed_ask_compare_catalog.py` para tener 3 candidatas de catálogo reales, no sintéticas). Confirma contra dos proyectos reales: (Proyecto A, `S1000D 4.2`, 2 BRDPs `Validated` propias + 3 entradas de catálogo, embeddings reales calculados vía el job de embeddings bajo demanda) -- `GET /similar?kind=definition` real da exactamente 5 candidatas (2 Records + 3 Catalog) con `source` correcto, `style_references` vacío; clic real en "Suggest Definition" en la UI renderiza el grupo "Similar" con las 5 referencias reales (identifier, origen, similarity) y el prompt capturado por `GET /last-request` del mock coincide línea por línea con la plantilla del encargo, incluida `temperature: 0.3` y el mensaje de usuario fijo sin historial; una BRDP con el identificador del catálogo (`BRDP-SDCAT-LIVE-001`) tiene el botón real deshabilitado con el tooltip exacto, y la llamada directa a la API da 400 real. (Proyecto B, `DITA 1.3 Xpath2.0`, sin ninguna BRDP `Validated` ni catálogo para ese standard) -- el botón sigue habilitado (sin gate de `MIN_CANDIDATES`), la UI muestra real "Definition generated without reference BRDPs", y el prompt capturado trae la línea de fallback con el standard real. Capturas reales en `/tmp/suggest-definition-with-references.png`, `/tmp/suggest-definition-catalog-disabled.png`, `/tmp/suggest-definition-without-references.png`. Los dos proyectos sembrados se borran al final del propio script; las filas de catálogo (`seed_suggest_definition_catalog.py`/`seed_ask_compare_catalog.py`) se dejan sembradas, mismo criterio de fixture reutilizable ya establecido para el catálogo de pruebas de este sandbox. `npm run lint`/`npm run build` limpios; `uvicorn` devuelto a su configuración normal (sin el mock) al terminar.
- **Ronda siguiente: idioma, salto de línea y duplicados catálogo/records en Suggest Definition** (`src/pages/RecordsPage.jsx`/`.module.css`, `backend/app/api/routes/similar.py`) -- tres arreglos independientes sobre el mismo feature, ninguno tocando el resto.
  - **Idioma**: confirmado real por el usuario con Mistral -- un BRDP con `Title` en español y `Proposal` vacía recibía la Definition en INGLÉS, porque tanto las instrucciones como las 5 referencias (few-shot) del prompt estaban en inglés y no había ninguna instrucción de idioma con prioridad explícita. La línea genérica `"Write in the same language as the BRDP's Title and Proposal."` (colocada ANTES de `Do not cite...`/`Return ONLY...`, es decir en medio del prompt, la posición menos respetada por el modelo) se elimina y se sustituye por un bloque `LANGUAGE:` nuevo, colocado justo ANTES de `Return ONLY the Definition text...` (el final real de las instrucciones, antes del bloque de datos "BRDP to define:") -- cita el `Title` real entre comillas, dice explícitamente que tiene prioridad sobre las referencias (que pueden estar en otro idioma) y da un fallback al `Proposal` si el idioma del `Title` no es claro. Verificado que el prompt real construido por la app tiene el bloque `LANGUAGE:` EXACTO (byte a byte) inmediatamente antes de `Return ONLY...`, sin nada más entre medias, y que cita el `Title` real (probado con un `Title` en español) -- la calidad real de la traducción la verifica el usuario con Mistral, como pide el encargo.
  - **Salto de línea**: bug real confirmado -- la caja de sugerencia (`<div className={styles.mono}>{suggestion.text}</div>`, dentro de `.suggestionBox`) reutilizaba `.mono` (pensada para identificadores cortos en toda la página: `font-family: monospace; white-space: nowrap;`) para el texto sugerido de LAS TRES `kind` (Definition/Proposal/Rule) -- su `white-space: nowrap` ganaba sobre el `pre-wrap` que `.suggestionBox` (el contenedor) ya tenía, así que cualquier línea larga se cortaba en el borde derecho del panel en vez de partirse. Arreglado con dos clases nuevas que sustituyen a `.mono` en ese único sitio, elegidas por `kind`: `.suggestionText` (Definition/Proposal, prosa -- `white-space: pre-wrap; overflow-wrap: anywhere;`, sin monospace, rompe incluso una palabra sin espacios si hace falta) y `.suggestionCode` (Rule/XML -- mantiene monospace + `white-space: pre` sin partir, pero con `overflow-x: auto; max-width: 100%;` propio, así que su scroll horizontal queda contenido dentro de la propia caja y nunca ensancha el panel). Verificado con Playwright real contra el proveedor mockeado (respuestas deliberadamente largas, incluida una palabra de +200 caracteres sin ningún espacio): la caja de Definition envuelve todo el texto, sin scroll horizontal de página (`document.documentElement.scrollWidth` igual al viewport); la caja de Rule mantiene el XML en una sola línea monospace sin partir, y el `scrollWidth` de la página TAMPOCO crece (el scroll queda dentro de la caja, confirmado también visualmente en captura).
  - **Duplicados catálogo/records**: `_dedupe_records_matching_catalog()` (nueva, `similar.py`) -- si una entrada de catálogo y una BRDP de Records comparten identificador Y tienen el mismo texto de Definition byte a byte, se descarta la de Records y se conserva solo la de Catalog (la fuente oficial), para no gastar uno de los 5 huecos de "Similar" (o de los 3 de "Style references") en el mismo contenido mostrado dos veces. Si el proyecto adaptó la redacción (texto distinto), se quedan AMBAS -- son precedente genuinamente distinto. Aplicada tanto al pool de "Similar" (top-5) como al de "Style references" (bottom-3) -- una llamada compartida en los dos sitios donde `_get_definition_similar` construye esos pools, antes de ordenar/recortar. 3 tests nuevos en `test_similar.py`: dedup real con texto idéntico (1 sola candidata sobrevive, con `source: "Catalog"`), ambas sobreviven cuando el texto difiere (adaptación del proyecto), y el mismo dedup aplica al pool de `style_references`.
  - **Hallazgo de entorno, no de la app**: al re-ejecutar `test_similar.py` en este sandbox, 3 tests que asumían "sin catálogo para `S1000D 4.2`" empezaron a fallar -- no por ningún bug, sino porque las filas de catálogo reales sembradas en la ronda anterior (`seed_suggest_definition_catalog.py`/`seed_ask_compare_catalog.py`, documentadas como fixtures reutilizables) siguen vivas en Postgres entre rondas de esta misma sesión, y esos 3 tests usaban el standard por defecto (`S1000D 4.2`) sin aislarse de ese catálogo persistente. Corregido cambiando esos 3 tests a `S1000D 4.1` (standard sin ninguna fila de catálogo sembrada en este sandbox) -- no es un cambio de comportamiento de la app, es una corrección de acoplamiento test↔entorno, documentada en el docstring de cada test afectado. Suite completa tras el fix: 265 tests backend en verde (262 + 3 nuevos).
  - **Verificación real** (`scripts/verify-suggest-definition-lang-wrap-dedup.mjs`, nuevo, auto-limpia el proyecto que siembra): BRDP real con `Title` en español, prompt capturado vía `GET /last-request` del mock de chat -- confirma el bloque `LANGUAGE:` exacto (comparación byte a byte contra el string esperado) inmediatamente antes de `Return ONLY...`, sin la línea vieja genérica, citando el `Title` español real entre comillas. Mismo script añade un disparador determinista nuevo al mock (`mock-mistral-chat-server.mjs`, detecta el mensaje de usuario fijo de Suggest Definition) que responde con una frase larga MÁS una palabra de +200 caracteres sin espacios, para forzar de verdad el caso límite del salto de línea -- captura real en `/tmp/suggest-definition-long-text-wrapped.png` (todo el texto visible, la palabra larga partida a mitad, sin scroll horizontal). `scripts/verify-rule-box-scoped-scroll-check.mjs` (nuevo, auto-limpia el proyecto que siembra) cubre el caso Rule por separado -- 3 BRDPs `Validated` con Rule aprobada real bajo `BREX-4.2`, embeddings calculados vía el job bajo demanda, Suggest Rule real con una respuesta mockeada de XML largo sin espacios -- confirma que el `scrollWidth` de la página NO crece (el XML se mantiene en una sola línea monospace, con su propio scroll interno), captura real en `/tmp/suggest-rule-box-scoped-scroll.png`. `npm run lint`/`npm run build` limpios; `uvicorn` devuelto a su configuración normal (sin el mock) al terminar.
- **Ronda siguiente: referencias de Suggest Definition legibles -- título + Definition desplegable** (`src/pages/RecordsPage.jsx`/`.module.css`, `backend/app/schemas/similar.py`/`app/api/routes/similar.py`; sin cambios en `Suggest Proposal`/`Suggest Rule`, que no usan esta lista): la fila de referencia antes solo mostraba `ID — origen — similitud`, sin decir de qué trataba cada una.
  - **`SimilarCandidateOut`** gana `definition: str = ""` (vacío salvo `kind=definition`, mismo criterio ya establecido para `title`/`source`) -- deliberadamente un campo propio, no reutilizar `text` (que para `kind=definition` da la casualidad de contener el mismo string, pero es un campo genérico por-`kind` -- proposal o rule_xml en los otros dos -- al que una UI de lista de referencias no debería asignarle semántica). `_get_definition_similar`'s `brdp_candidate`/`catalog_candidate` lo rellenan con `b.definition`/`c.definition` en los dos sitios donde ya construían el resto de campos, sin lógica nueva.
  - **`ReferenceRow`** (nuevo componente, `RecordsPage.jsx`, compartido por los grupos Similar/Style references): fila flex `identifier — title — origen[ — similitud]`, con el identificador como `<button type="button">` clicable (nunca navega) que alterna un `Set` de ids expandidos (`expandedReferenceIds`, nuevo estado) -- varias filas pueden estar abiertas a la vez, cada una independiente. El título va en un `<span title={c.title}>` con `flex:1; min-width:0; overflow:hidden; text-overflow:ellipsis; white-space:nowrap;` (el `min-width:0` es necesario dentro de un flex row -- sin él, el elemento se niega a encogerse por debajo del ancho intrínseco de su contenido y la elipsis nunca se activa) -- el texto completo sigue disponible al pasar el ratón vía el propio `title=`. La similitud deja de mostrarse como `"similarity X.XX"` (palabra completa, ronda anterior) y pasa al formato bare que pide el encargo (`— 0.79`) -- la clave i18n `definitionSimilarity` (EN+ES) que ya no se usa en ningún sitio se elimina, no se deja como código muerto.
  - **Definition expandida**: `{expanded && <div className={styles.referenceDefinition}>{candidate.definition}</div>}` justo debajo de la fila -- mismo criterio de wrap ya arreglado en la ronda anterior para la caja de sugerencia (`white-space: pre-wrap; overflow-wrap: anywhere;`), porque es igualmente prosa real que no debe forzar scroll horizontal del panel.
  - **Reinicio del estado de expansión**: `expandedReferenceIds` se limpia en dos sitios, tal como pedía el encargo -- al principio de `requestSuggestion` (cualquier `kind`, junto al `setSuggestion(null)` ya existente, así que una sugerencia nueva siempre arranca con todo plegado) y en el `useEffect` ya existente que resetea el panel de Ask al cambiar de BRDP seleccionada (mismo array de dependencias `[selected?.id]`, una línea añadida). Deliberadamente NO se resetea al pulsar Discard -- el encargo no lo pedía, y `suggestion` en sí tampoco se limpia en ese `useEffect` de cambio de BRDP (gap preexistente, no tocado, fuera del alcance de esta ronda).
  - **Tests backend** (`test_similar.py`): extendidas dos tests ya existentes en vez de duplicar fixtures -- `test_definition_candidates_include_catalog_and_other_projects_records_with_source_labels` gana la comprobación de que `definition` viaja igual que `text` para una candidata de catálogo y de Records; `test_definition_style_references_added_only_below_three_similar` gana la comprobación de que `definition` también viaja en `style_references` (no solo en `candidates`), comparando cada fila devuelta contra el BRDP real que la originó por `id`. Suite backend completa: 265 tests en verde (sin tests nuevos añadidos, solo aserciones nuevas sobre fixtures ya existentes -- el campo es un dato adicional sobre comportamiento ya cubierto, no un comportamiento nuevo que necesite su propio test).
  - **Verificación real** (`scripts/verify-suggest-definition-reference-expand.mjs`, nuevo, auto-limpia el proyecto que siembra): proyecto real `S1000D 4.2` con 1 BRDP origen y 2 candidatas Validated reales (una con un título deliberadamente largo -- "Numbering of procedural steps within a maintenance task that spans multiple pages and sub-procedures, including exceptions"), embeddings calculados vía el job bajo demanda, Suggest Definition real contra el proveedor mockeado. Confirma: el `title=` del `<span>` trae el título completo aunque se vea recortado (`getAttribute`, no solo visual); la altura del `<span>` es de una sola línea (nunca dos) y su `text-overflow` computado es `ellipsis`; `document.documentElement.scrollWidth` no crece por el título largo; la Definition NO es visible antes de hacer clic; clicar el identificador la muestra; abrir una segunda fila deja la primera igual de visible (dos abiertas a la vez, estado independiente); clicar de nuevo el primer identificador la pliega SIN afectar a la segunda (que sigue abierta); y pedir una sugerencia nueva colapsa ambas de golpe. Captura real en `/tmp/suggest-definition-reference-expanded.png` -- fila `BRDP-REFEXP-CLOSE-1` con el título recortado y su Definition real desplegada debajo, identificadores con el estilo de enlace azul subrayado ya establecido para acciones clicables de esta página. `npm run lint`/`npm run build` limpios; `uvicorn` devuelto a su configuración normal (sin el mock) al terminar.
- **Ronda siguiente: Suggest arrastraba la sugerencia a la BRDP siguiente + tests de `test_similar.py` dependían de qué datos reales hubiera en Postgres** (`src/pages/RecordsPage.jsx`; `backend/tests/test_similar.py`, `test_embedding_jobs.py`, `test_brdp_trash.py`) -- dos bugs independientes, uno de UI y uno de aislamiento de tests, cerrados en la misma ronda porque el segundo era la causa raíz documentada explícitamente como "gap preexistente, no tocado, fuera del alcance" en la ronda anterior (ver la nota de `expandedReferenceIds` justo arriba: `suggestion` en sí nunca se limpiaba al cambiar de BRDP).
  - **Bug real confirmado por el usuario**: tras `Suggest Definition/Proposal/Rule` en una BRDP, al seleccionar otra en la tabla la sugerencia seguía visible en el panel -- `Accept` la habría escrito en la BRDP equivocada. El `useEffect` sobre `[selected?.id]` (`RecordsPage.jsx`) que ya reseteaba el panel de Ask (ronda `c09bd8e`) nunca tocaba `suggestion`/`busy`/`expandedReferenceIds`.
  - **`suggestRequestGenerationRef`** (nuevo, `useRef(0)`): un contador que se incrementa (a) en el `useEffect` de cambio de BRDP -- invalida cualquier petición de Suggest todavía en vuelo para la fila que se acaba de abandonar -- y (b) al principio de cada `requestSuggestion()` -- invalida también cualquier petición anterior sobre la MISMA BRDP, para que un segundo clic en Suggest sobre la fila actual siempre gane. `requestSuggestion` captura su propio `requestToken` al arrancar y una función `isStale()` que compara contra el valor actual del ref; los CUATRO sitios donde escribía `setSuggestion(...)` (éxito de Definition, insufficient-precedent, éxito de Proposal/Rule, catch de error) y el `setBusy(false)` del `finally` quedan condicionados a `!isStale()` -- soft-cancel, ya que `authFetchJson`/`sendMessage` no exponen cancelación real de la petición en curso en este código; comparar el id capturado al arrancar contra el actualmente seleccionado (lo que sugiere el encargo como alternativa) habría servido igual, pero el contador cubre TAMBIÉN el caso "misma BRDP, segunda petición" sin lógica extra. Como consecuencia, `busy` de una petición vieja NUNCA pisa el `busy=true` de una petición nueva ya en marcha para la fila recién seleccionada -- el `finally` de la vieja simplemente no hace nada si ya es stale.
  - **`suggestion` gana `brdpId`** (la BRDP para la que se pidió, capturada en `requestedBrdpId = selected.id` al arrancar `requestSuggestion`) en los mismos cuatro payloads. `acceptSuggestion` comprueba `suggestion.brdpId !== selected.id` justo después de su guarda existente y, si no coincide, limpia `suggestion` y no escribe nada -- defensa explícita pedida por el encargo, aunque en la práctica ya es inalcanzable (el `useEffect`/el descarte por staleness garantizan que `suggestion` nunca sobrevive a un cambio de fila ni a una petición más nueva).
  - **Verificación real con retardo real, no simulado con timers de test** (`scripts/verify-suggest-clear-on-brdp-change.mjs`, nuevo, auto-limpia el proyecto que siembra; `scripts/mock-mistral-chat-server.mjs` gana `POST /slow-next`, armado de un solo uso -- retarda la SIGUIENTE respuesta del mock 2500ms antes de servirla, luego vuelve a comportarse con normalidad; no ligado al contenido del mensaje porque el mensaje de usuario de Suggest Definition es siempre el mismo string fijo independientemente de la BRDP, así que un marcador de texto no habría servido para distinguir peticiones): proyecto real `S1000D 4.2` con 2 BRDPs `Pending` (A, B) para alternar. Confirma contra el proveedor mockeado y por UI real: (1) `Suggest Definition` en A, esperar la respuesta real, cambiar a B -- el panel de B aparece vacío (sin botón Accept, sin aviso de precedente insuficiente, sin error); (2) `Suggest Definition` en A con la respuesta armada para retardarse, cambiar a B ANTES de que llegue -- B aparece vacío de inmediato, y SIGUE vacío incluso después de que pasen los 2500ms y la respuesta (ahora obsoleta) haya llegado de verdad -- nunca aparece en B ni deja ningún botón Accept en pantalla; (3) volver a A (la BRDP para la que realmente se pidió) -- tampoco reaparece ahí, confirmando que se descartó de verdad y no quedó cacheada en ningún sitio. Regresión confirmada sin tocar Suggest Proposal/Rule ni el resto del panel: `scripts/verify-suggest-definition-reference-expand.mjs` (ronda anterior) re-ejecutado en verde sin cambios.
  - **Tests dependientes de datos reales**: en `27393ff` se habían "arreglado" 3 tests de `test_similar.py` moviéndolos de `S1000D 4.2` (con catálogo real persistente en este sandbox por rondas anteriores) a `S1000D 4.1` -- un aplazamiento, no un arreglo, tal como señalaba el encargo: el mismo problema reaparecería en cuanto este sandbox (u otro entorno real cualquiera) tuviera catálogo para 4.1 también. Arreglo real: `_make_project(standard: str | None = None)` ahora genera un standard sintético fresco por llamada (`f"TEST-STANDARD-{uuid.uuid4()}"`) cuando no se pasa `standard=` explícito, en vez de por defecto usar `"S1000D 4.2"` -- la mayoría de los tests del fichero solo necesitaban "algún standard, usado consistentemente dentro de sí mismos", nunca uno real, así que quedan aislados por construcción sin tocar el cuerpo de cada test.
  - **Auditoría completa de `test_similar.py`** (54 tests): 2 tests con dos proyectos que necesitaban compartir standard (`test_different_standard_is_excluded_from_candidates`, `test_excluded_pending_other_projects_counts_other_projects_without_embedding`) pasan ahora una variable `standard`/`shared_standard` fresca generada una vez y reusada; 6 tests de catálogo (rechazo por ser BRDP-catálogo, rechazo por coincidencia exacta de tabla no por prefijo, scoping por standard, y los 3 de dedup catálogo/Records) leen `project.standard` del objeto ya creado en vez de hardcodear `"S1000D 4.2"` -- identificadores realistas como `"BRDP-S1-00070"` (que SÍ podrían colisionar con catálogo real, a diferencia de identificadores claramente sintéticos como `"BRDP-DUP-001"`) quedan protegidos porque el standard bajo el que se crean ya nunca es uno real; `test_definition_candidates_exclude_catalog_of_a_different_standard` usa un segundo standard sintético explícito para la entrada de catálogo "de otro standard". Los 2 tests que necesitan un standard REAL para probar el mapeo real `STANDARD_TO_RULE_FORMAT` (`test_kind_rule_maps_project_standard_to_rule_format` → `S1000D 4.2`/`BREX-4.2`, `test_kind_rule_maps_dita_standard_to_sch_dita_format` → `DITA 1.3 Xpath2.0`/`SCH-DITA`) mantienen su standard real pero cambian de igualdad de conjunto exacta (`texts == {...}`) a subconjunto (`{...} <= texts`) -- sus 3 filas propias siguen garantizadas presentes (score 1.0, el máximo posible, nunca puede quedar fuera del top-10/top-3 real) sin romperse si ya existen más reglas aprobadas reales bajo ese formato en el entorno. `test_kind_rule_unsupported_standard_returns_400` (S1000D 5.0, genuinamente sin formato) no necesitó cambio -- solo comprueba un 400, sin ninguna suposición sobre datos.
  - **`test_embedding_jobs.py`**: su fixture compartida `editor_and_project` (usada por prácticamente todos los tests del fichero) tenía `standard="S1000D 4.2"` hardcodeado, y varios tests aserta conteos EXACTOS de `project_pending`/`catalog_pending` -- un único cambio de línea (standard sintético `f"TEST-EMBED-STANDARD-{uuid.uuid4()}"`) los arregla todos a la vez, sin tocar ningún cuerpo de test.
  - **`test_brdp_trash.py`**: su fixture `admin_editor_and_project` tenía el mismo problema, pero solo UN test lo explotaba de verdad (`test_deleted_brdp_excluded_from_similar_precedent`, que llama a `GET .../similar` de verdad en el límite exacto de `MIN_CANDIDATES`) -- el resto de tests del fichero (papelera, restauración, borrado permanente, histórico) son operaciones scopeadas por `project_id`, no por standard, así que nunca les afectaba qué standard usara la fixture; se cambió igualmente por consistencia y porque ese único test SÍ era vulnerable a datos reales con similitud moderada contra su vector de consulta.
  - **Auditados y confirmados YA seguros, sin cambios** (búsqueda explícita en el resto de la suite, pedida por el encargo: "buscar en toda la suite otros tests que asuman ausencia o presencia de datos de un standard real"): `test_brdp_catalog.py` (ya usa standards sintéticos únicos por test, el patrón de referencia); `test_brdp_import.py` (su fixture de catálogo usa identificadores `BRDP-CAT-{uuid4()}` globalmente únicos, colisión imposible con datos reales cualquiera que sea el standard); `test_authorization.py`/`test_db_wiring.py`/`test_suggestion_feedback.py`/`test_projects.py`/`test_brdps_notes_approvals.py`/`test_brdp_history.py`/`test_brdp_status_counts_and_filters.py` (todas sus aserciones son sobre un recurso/proyecto propio buscado por su propio id, nunca un conteo agregado entre TODOS los proyectos de un standard -- confirmado leyendo `compute_status_counts()`, que filtra por `project_id.in_(...)`, no por standard, así que ni siquiera el fichero de cifras de estado puede leakear datos de otros proyectos reales); `test_standard_consistency.py` (prueba constantes estáticas, no datos de la base).
  - **Cierre pedido explícitamente**: "la suite debe pasar tanto con la base de datos vacía de catálogo como con catálogo cargado para todos los standards". `scripts/verify_similar_tests_survive_full_catalog.py` (nuevo, backend, reutilizable, auto-limpia) siembra datos ADVERSARIOS -- 5 BRDPs `Validated` + 5 catalog rows por cada uno de los 7 standards reales (`S1000D 3.0.1/4.1/4.2/5.0/6.0`, `DITA 1.3 Xpath2.0/Xpath3.0`), con embeddings puestos DELIBERADAMENTE al mismo vector exacto que usan los mocks de consulta de `test_similar.py`/`test_embedding_jobs.py` (`[1.0, 0, ...]`) y de `test_brdp_trash.py` (`[0.1]*1024`) -- el peor caso posible para cualquier test que todavía asumiera "sin datos reales para este standard". Suite completa ejecutada con esos datos sembrados: **265/265 en verde** (mismo resultado que sin ellos). Datos adversarios borrados al terminar.
  - `npm run lint`/`npm run build` limpios (frontend); `uvicorn`/`vite`/los mocks levantados solo para la verificación Playwright, parados al terminar.
- **Ronda siguiente: la sugerencia de Suggest se queda en su BRDP hasta aceptarla o descartarla** (`src/pages/RecordsPage.jsx`, `src/i18n/index.js`, `src/pages/RecordsPage.module.css`; sin cambios de backend) -- decisión explícita del usuario que INVIERTE el comportamiento de la ronda anterior (`2ad1bd3`): allí, cambiar de BRDP borraba la sugerencia y descartaba cualquier respuesta tardía para no escribir en la BRDP equivocada -- correcto para ese riesgo, pero tiraba trabajo real del LLM cada vez que el usuario simplemente miraba otra fila. Ahora la sugerencia pertenece a la BRDP para la que se pidió y sobrevive a cambiar de fila; solo `Accept`/`Discard` la borran.
  - **`suggestionsByBrdpId`** (nuevo, `Map<brdpId, entry>`, sustituye al `suggestion` único de la ronda anterior) -- como mucho una entrada por BRDP, nunca en localStorage (HR1: vive solo en memoria de React, se pierde al recargar/cerrar sesión, y se vacía explícitamente al cambiar de proyecto). Cada entrada es una de cuatro formas: `loading` (`{brdpId, kind, loading: true, expandedReferenceIds}`), sugerencia real (`+ text, sourceBrdpIds, format?, similar?, styleReferences?, excludedPendingOtherProjects`), aviso de precedente insuficiente (`+ insufficientPrecedent: true, count`) o error (`+ error`) -- las dos últimas SIN `text`, así que `Accept` nunca las confunde con una sugerencia real (sigue comprobando `entry?.text`). `expandedReferenceIds` (qué referencias de Suggest Definition están desplegadas, ronda anterior) pasa a vivir DENTRO de cada entrada en vez de ser un estado único de página -- viaja con su BRDP al cambiar de fila y volver, en vez de compartirse entre todas.
  - **Bloqueo por BRDP, no global**: los 3 botones Suggest se deshabilitan mientras `suggestionsByBrdpId` tenga CUALQUIER entrada (cargando o resuelta) para la BRDP seleccionada -- no solo el mismo `kind`, los tres, tal como pedía el encargo explícitamente ("esto incluye volver a pedir del mismo tipo"). Tooltip nuevo, `pendingSuggestionBlocksNew` (i18n EN/ES): "Accept or discard the pending suggestion first" / "Acepta o descarta la sugerencia pendiente primero". Cada BRDP es independiente -- los botones de una fila sin entrada en el mapa nunca se ven afectados por lo que tenga otra.
  - **Generación por `brdpId`, no global**: `suggestGenerationRef` pasa de un contador único (`useRef(0)`, ronda anterior) a un `Map<brdpId, número>` -- se incrementa SOLO al arrancar una petición nueva para esa BRDP concreta (nunca en `Accept`/`Discard`). Una respuesta tardía se compromete (`commit()`) solo si se cumplen DOS condiciones a la vez: (a) el mapa de sugerencias TODAVÍA tiene una entrada para ese `brdpId` (falso si `Accept`/`Discard`/borrado de BRDP/cambio de proyecto ya la quitaron -- nunca resucita una entrada eliminada) y (b) el contador de esa `brdpId` en el ref sigue coincidiendo con el token capturado al arrancar (falso si mientras tanto se lanzó una petición MÁS NUEVA para la MISMA BRDP -- posible tras un `Discard` que la desbloquea). Ninguna de las dos condiciones basta sola: (a) sin (b) dejaría que una respuesta vieja pisara la entrada `loading` de una petición nueva ya en marcha; (b) sin (a) resucitaría una entrada legítimamente borrada. Sigue siendo un soft-cancel (no hay cancelación real de `authFetchJson`/`sendMessage` a mitad de vuelo en este código), mismo criterio que la ronda anterior.
  - **El `useEffect` de `[selected?.id]`** (el que ya reseteaba el panel de Ask al cambiar de BRDP, ronda `c09bd8e`, y que la ronda anterior extendió también a Suggest) pierde TODAS las líneas de Suggest que había ganado (`setSuggestion(null)`/`setBusy(false)`/`setExpandedReferenceIds(new Set())`/el bump del contador) -- cambiar de fila ya no toca `suggestionsByBrdpId` en absoluto, el panel de Ask sigue reseteándose exactamente igual que antes. El `useEffect` de `[projectId]` (que ya reseteaba filtros/proveedor IA/catálogo al cambiar de proyecto) gana `setSuggestionsByBrdpId(new Map())` + `suggestGenerationRef.current = new Map()` -- pedido explícito del encargo ("al cambiar de proyecto, vaciar el mapa"), y limpiar también el ref evita que una respuesta del proyecto ANTERIOR pase su propio check de "generación vigente" antes de que el check de "el mapa ya no tiene esa entrada" la descarte igualmente.
  - **`handleDelete`** gana una línea, `removeSuggestionEntry(brdpId)`, justo tras el `DELETE` real -- pedido explícito del encargo ("borrar A con sugerencia pendiente → se elimina del mapa; si la respuesta llega después, se descarta"). Como el mecanismo de `commit()` ya comprueba "¿el mapa todavía tiene esta entrada?", basta con quitarla aquí para que cualquier respuesta que llegue después (incluida una ya en vuelo cuando se borró) se descarte sola, sin lógica adicional en `requestSuggestion`.
  - **Aviso de precedente insuficiente y entrada de error ganan su propio botón `Discard`** (antes, ninguno de los dos lo tenía -- bajo el modelo viejo no hacía falta, `busy` se liberaba solo al terminar la petición y pedir otra vez estaba siempre permitido). Bajo el modelo nuevo esto habría sido un bug real: una BRDP que topara con precedente insuficiente (`kind=proposal`/`rule`) o un fallo del LLM se habría quedado con los 3 botones bloqueados PARA SIEMPRE, sin ningún `Accept` posible (no hay texto que aceptar) y sin ninguna forma de vaciar la entrada -- exactamente el caso límite que el encargo pide explícitamente para el error ("no dejar los botones bloqueados para siempre"), aplicado aquí también al aviso de precedente insuficiente por la misma razón estructural.
  - **Indicador ✨ en la tabla**: junto al identificador de CUALQUIER fila con entrada en `suggestionsByBrdpId` (cargando o resuelta), esté o no seleccionada -- `title` con el tipo real (`pendingSuggestionIndicator`, i18n: "Pending suggestion: {{kind}}" / "Sugerencia pendiente: {{kind}}", `kindLabels` nuevo para "Definition"/"Proposal"/"Rule" humanizados en ambos idiomas, HR21). CSS nueva mínima (`.pendingSuggestionIcon`, `margin-left` + `cursor:help`) en `RecordsPage.module.css`.
  - **`acceptSuggestion`/`discardSuggestion`/`logSuggestionFeedback`** reescritas para leer/escribir por `brdpId` en vez de un único `suggestion` -- `acceptSuggestion` sigue comprobando `entry.brdpId !== selected.id` como defensa adicional (ya prácticamente inalcanzable, dado que la propia búsqueda en el mapa es por `selected.id`, pero se mantiene por el mismo principio defensivo que la ronda anterior). Ambas terminan con `removeSuggestionEntry(selected.id)` -- la entrada desaparece del mapa, el ✨ se apaga y los 3 botones se desbloquean, en el mismo paso.
  - **Verificación real con retardo real** (`scripts/verify-suggest-persists-per-brdp.mjs`, nuevo, auto-limpia el proyecto que siembra; `scripts/mock-mistral-chat-server.mjs` gana `POST /error-next`, armado de un solo uso, independiente del contenido del mensaje -- necesario porque `ERROR_TEST` solo dispara si el marcador está DENTRO del mensaje de usuario, y el de Suggest Definition es siempre el mismo string fijo sin importar la BRDP, así que no hay dónde insertar un marcador; usar `Suggest Proposal`/`Rule` en su lugar habría exigido montar antes 3+ BRDPs `Validated` de precedente solo para llegar a la llamada real al LLM). Proyecto real `S1000D 4.2` con 3 BRDPs `Pending` (A, B, C). Confirma contra el proveedor mockeado y por UI real: (1) pedir en A bloquea los 3 botones de A, los de B siguen activos; (2) pedir en B con la respuesta armada para retardarse, cambiar a A antes de que llegue -- B muestra ✨ visto desde A, la sugerencia de A de antes sigue intacta; volver a B -- sigue "cargando" hasta que pasan los 2500ms, entonces aparece resuelta con los botones todavía bloqueados; (3) `Discard` en B la borra y desbloquea sus 3 botones, apaga su ✨; (4) `Accept` en A escribe de verdad en Postgres (confirmado por `GET /brdps` tras aceptar, no solo visualmente), apaga su ✨ y desbloquea sus botones; (5) borrar C con una petición en vuelo (armada para retardarse) -- tras recargar la página la fila de C ha desaparecido del todo, confirmando que la respuesta tardía nunca tiene dónde resurgir; (6) un error real (`/error-next`) sobre B dado con Suggest Definition dispara una entrada de error con botón `Discard`, que la limpia y desbloquea los 3 botones -- nunca se quedan bloqueados para siempre. Capturas reales en `/tmp/suggest-persist-sparkle-on-other-row.png` (✨ en A visible desde B, botones de B activos), `/tmp/suggest-persist-blocked-tooltip.png` (botones de B bloqueados a mitad de petición) y `/tmp/suggest-persist-error-discard.png` (entrada de error con Discard, botones bloqueados).
  - **Regresión**: `scripts/verify-suggest-definition-reference-expand.mjs` (ronda `49`) necesitó un `Discard` explícito antes de pedir una segunda sugerencia en el mismo BRDP -- bajo el modelo viejo, pedir dos veces seguidas sin descartar estaba permitido (la segunda simplemente sustituía a la primera); bajo el nuevo, está bloqueado a propósito, así que el propio script tenía que empezar a comportarse como un usuario real lo haría. `scripts/verify-suggest-clear-on-brdp-change.mjs` (ronda anterior, `2ad1bd3`) se ELIMINA por completo -- no se deja como código muerto ni se actualiza, porque verificaba literalmente el comportamiento CONTRARIO al que esta ronda establece a propósito (que la sugerencia se borre al cambiar de fila); mantenerlo habría sido un test activamente engañoso, no solo innecesario. El resto de scripts que tocan Suggest (`verify-suggest-definition-corpus.mjs`, `verify-suggest-definition-lang-wrap-dedup.mjs`, `verify-rule-box-scoped-scroll-check.mjs`, `verify-on-demand-embeddings.mjs`) re-ejecutados en verde sin cambios -- ninguno pedía Suggest dos veces seguidas sobre la misma BRDP sin descartar antes. `npm run lint`/`npm run build` limpios; `uvicorn`/`vite`/los mocks levantados solo para la verificación, parados al terminar.
- **Suggest Proposal: corpus propio de tres grupos (Same BRDP in other projects / Similar decisions / This project), bloqueo por Definition vacía, prompt propio** (backend: `app/api/routes/similar.py`, `app/schemas/similar.py`; frontend: `src/pages/RecordsPage.jsx`, `src/i18n/index.js`; sin cambios en Suggest Rule) -- misma arquitectura "corpus propio" ya establecida para Suggest Definition (`_get_definition_similar`/`buildSuggestDefinitionPrompt`), aplicada ahora a `kind=proposal`: función backend dedicada con `return` temprano en `get_similar` antes del bloque genérico (que queda ya solo para `kind=rule`), y rama `if (kind === 'proposal')` dedicada en `requestSuggestion` que también devuelve temprano.
  - **`_get_proposal_similar`** (`similar.py`, nueva): tres grupos, todos sobre BRDPs `Validated` con Proposal no vacía del mismo standard, catálogo excluido (no tiene Proposal). (1) **Same BRDP in other projects**: solo si el identificador de la BRDP origen existe en `brdp_catalog` del standard (comprobación real contra la tabla, no por prefijo -- una BRDP `EXT` autogenerada podría coincidir por casualidad entre dos proyectos sin relación, así que el match por identificador solo es semánticamente válido para un id realmente emitido por el catálogo); búsqueda directa por `identifier` en otros proyectos, sin embeddings, hasta `PROPOSAL_SAME_BRDP_AND_SIMILAR_LIMIT=5`. (2) **Similar decisions**: hasta `5 - len(same_brdp)` de otros proyectos (cualquier prefijo) con similitud ≥ `MIN_SIMILARITY` -- si `same_brdp` está vacío, este grupo solo provee los 5 por sí solo; si ya hay 5 en `same_brdp`, esta query ni se ejecuta. (3) **This project**: hasta `PROPOSAL_THIS_PROJECT_LIMIT=3` del mismo proyecto, disjunto por construcción de los otros dos grupos (`project_id == project_id` los excluye estructuralmente, sin necesitar dedup explícito). Dedup cruzado solo entre `same_brdp`/`candidates` (un `BRDP.id.not_in(same_brdp_ids)` en la query de "similar", añadido solo si el set no está vacío para no generar un `IN ()` vacío). `MIN_CANDIDATES` deja de aplicar a `kind=proposal` -- `sufficient_precedent` siempre `True`, sin respaldo de estilo si los tres grupos quedan vacíos (a diferencia de Definition, aquí no existe un grupo "distintas" -- el encargo lo prohibía explícitamente). `excluded_pending_other_projects` gana un filtro `BRDP.proposal != ""` propio de este `kind` (no presente en la versión genérica anterior ni en la de Definition) para no sobrestimar el impacto del hueco de embeddings contando filas que de todos modos ya quedarían excluidas por Proposal vacía.
  - **Bloqueo por Definition vacía**: `kind == "proposal" and not brdp.definition.strip()` → 400 explícito en `similar.py`, colocado junto al gate de catálogo de Definition, antes de la llamada real a embeddings (para no pagar una llamada a Mistral en un caso ya inválido). Explícitamente permitido sobre una BRDP de catálogo (a diferencia de Suggest Definition) -- el catálogo solo trae la Definition oficial, la Proposal siempre es decisión propia del proyecto. Frontend: `definitionEmptyForProposal = kind === 'proposal' && !selected.definition?.trim()` en el `SUGGEST_KINDS.map`, con tooltip nuevo `suggestProposalNeedsDefinition` ("Add or accept a Definition first" / "Añade o acepta primero una Definition") con prioridad justo después de `catalogDisabled`.
  - **`buildSuggestProposalPrompt(brdp, standard, sameBrdp, similar, thisProject)`** (`RecordsPage.jsx`, nueva, mismo criterio que `buildAskSystemPrompt`/`buildSuggestDefinitionPrompt` -- construida ÍNTEGRAMENTE desde los arrays estructurados de `/similar`, nunca desde texto del LLM): tres bloques condicionales (`SAME BRDP IN OTHER PROJECTS` cita solo Proposal por línea, sin score; `SIMILAR DECISIONS IN OTHER PROJECTS` cita Definition+Proposal+score; `THIS PROJECT'S RELATED DECISIONS` cita Definition+Proposal+score pero SIN nombre de proyecto -- ya implícito), fallback "sin referencias" solo si los tres están vacíos, bloque `THE PREVIOUS PROPOSAL WAS REFUSED` (Proposal rechazada + `comments`, solo si `validation === 'Refused'`) justo antes del bloque final de guardas de valores específicos del proyecto (nunca inventar/copiar CAGE codes, fechas, límites numéricos concretos de otro proyecto -- placeholder visible en su lugar) y de la línea `LANGUAGE` (misma prioridad ya establecida en Suggest Definition: el Title del BRDP manda sobre el idioma de las referencias, con fallback a la Definition si el Title no es claro). Mensaje de usuario fijo `"Write the Proposal for this BRDP."`, `temperature: 0.3`, sin historial -- mismo patrón que Suggest Definition.
  - **UI**: `ReferenceRow` gana un prop `showProposal` (booleano) -- cuando está activo, la fila expandida muestra Definition Y Proposal, cada una etiquetada (`referenceDefinitionLabel`/`referenceProposalLabel`, i18n nuevas), en vez de solo Definition; usado por los tres grupos de Suggest Proposal (incluido "Same BRDP", cuyo bloque de PROMPT solo cita Proposal, pero la UI es más generosa por pedido explícito del encargo: "despliega su Definition y su Proposal"). Suggest Definition sigue sin este prop, comportamiento sin cambios. Tres secciones nuevas bajo la caja de sugerencia (`selectedSuggestion.kind === 'proposal'`), modeladas en la rama ya existente de `kind === 'definition'`: `proposalSameBrdpGroup`/`proposalSimilarGroup`/`proposalThisProjectGroup` (i18n: "Same BRDP in other projects"/"Similar decisions"/"This project", "Mismo BRDP en otros proyectos"/"Decisiones similares"/"Este proyecto"), cada una con `showScore={false}` solo para `sameBrdp` (sin similitud real, match directo por identificador) y `showScore` activo para las otras dos; fallback `proposalNoReferences` ("Proposal generated without reference BRDPs"/"Propuesta generada sin BRDPs de referencia") cuando los tres arrays quedan vacíos. `requestSuggestion`'s nueva rama `kind === 'proposal'` lee `similar.same_brdp`/`.candidates`/`.this_project`, construye el prompt, y comete una entrada con `sameBrdp`/`similar`/`thisProject` + `sourceBrdpIds` (concatenación de los tres grupos, para el log de `/api/suggestion-feedback`) -- reutiliza el mecanismo ya establecido de mapa por-BRDP (`suggestionsByBrdpId`) y su bloqueo de los 3 botones mientras haya una entrada pendiente, sin cambios en ese mecanismo.
  - **Tests backend** (`test_similar.py`, 12 nuevos -- 35 tests en el fichero en total): match por identificador + dedup contra candidates (usando `_SAME_DIRECTION` para que el candidato SÍ calificara para "Similar decisions" por mérito propio, haciendo la aserción de exclusión-por-dedup significativa, no trivial), tope de 5 en `same_brdp`, `same_brdp` vacío para un identificador `EXT` (con el match coincidente apareciendo igualmente en "Similar decisions", no excluido en bloque), `candidates` solo provee los 5 cuando `same_brdp` está vacío, `candidates` completa el hueco hasta 5 combinado, `this_project` topado en 3, los tres grupos vacíos sigue dando `sufficient_precedent=True`, exclusión de Proposal vacía en los tres grupos, rechazo 400 con Definition vacía, 200 permitido sobre BRDP de catálogo, `excluded_pending_other_projects` scopeado a Proposal no vacía. 4 tests genéricos preexistentes que probaban semántica de `MIN_CANDIDATES`/tope-10 vía `kind=proposal` (ya no aplicable, esa semántica ahora es solo de `kind=rule`) migrados a `kind=rule` con un helper nuevo, `_fake_rule_format(monkeypatch, standard)` (`monkeypatch.setitem(STANDARD_TO_RULE_FORMAT, standard, fake_format)`) -- generaliza a `kind=rule` la técnica ya establecida de standard sintético por test, sin depender de un standard real mapeado. Un quinto test en `test_brdp_trash.py` (precedente tras soft-delete) migrado igual, con `RuleApproval` añadidas para las 3 BRDPs de precedente (necesarias para `kind=rule`, a diferencia de proposal/definition que leen directo de la fila BRDP). Bug propio detectado y corregido antes de commitear: una comparación `match.id not in {c["id"] for c in ...}` comparaba un UUID contra strings JSON-serializados (siempre `True` sin comprobar nada de verdad, mismo tipo de bug ya documentado en una ronda anterior) -- arreglado a `str(match.id) not in {...}`. Suite backend completa: 276 tests en verde.
  - **Verificación real** (`scripts/verify-suggest-proposal-corpus.mjs`, nuevo, auto-limpia los 5 proyectos que siembra; `backend/scripts/seed_suggest_proposal_catalog.py`, nuevo, mismo patrón reutilizable que `seed_suggest_definition_catalog.py` -- 1 fila de catálogo real `BRDP-SPCAT-LIVE-001` para `S1000D 4.2`) contra el proveedor mockeado (ambos transportes, chat y embeddings): 5 proyectos reales -- **A** (BRDP origen con el identificador catalogado, 3 BRDPs `Validated` propias para "This project", 1 `Refused` con motivo, 1 con Definition vacía), **B**/**C** (cada uno con una BRDP `Validated` del MISMO identificador catalogado -- confirma el caso límite exacto del encargo "BRDP S1 presente en 2 proyectos más → 2 en Same BRDP", más 2+1 BRDPs `Validated` de identificador distinto que completan "Similar decisions" a 3, para un combinado real de 5 con `same_brdp`), **D** (BRDP con identificador `EXT`, sin fila de catálogo -- confirma "sin grupo Same BRDP" real, con "Similar decisions" tomando los 5 restantes de B+C sin necesitar un 6º proyecto, ya que hay exactamente 5 BRDPs `Validated` fuera de A), **E** (standard `DITA 1.3 Xpath2.0` sin ningún precedente, real "sin referencias"). Backend confirmado con `GET /similar?kind=proposal` real: `same_brdp.length === 2` con los nombres reales de B/C como `source`, `candidates.length === 3` sin duplicar el identificador compartido, `this_project.length === 3` con `source === ""`; para D, `same_brdp` vacío y `candidates.length === 5` (las 5 BRDPs reales de B+C, incluidas las 2 del identificador compartido -- ahí SÍ cuentan como "Similar decisions" normales, ya que D no tiene catálogo); 400 real para la BRDP sin Definition; `sufficient_precedent=true` con los tres grupos vacíos para E. UI real: los tres grupos visibles con sus encabezados reales, una fila de "Same BRDP" expandida mostrando Definition Y Proposal reales (`Project B's own Definition...`/`Project B decided: clamps shall be spaced...`), los otros 2 botones Suggest bloqueados mientras la sugerencia está pendiente, botón deshabilitado real con el tooltip exacto para la BRDP sin Definition, fallback "Proposal generated without reference BRDPs" real para E. Prompt real capturado vía `GET /last-request` del mock: los tres bloques con el formato exacto de línea por grupo (`[id | proyecto] Proposal: ...` sin score para Same BRDP, `[id | proyecto | similarity X.XX]` con Definition+Proposal para Similar/This-project, este último sin nombre de proyecto), bloque `THE PREVIOUS PROPOSAL WAS REFUSED` real con la Proposal rechazada y el motivo citados verbatim para la BRDP `Refused` de A, `temperature: 0.3`, un único mensaje de usuario fijo sin historial, fallback de standard real para E. Capturas reales en `/tmp/suggest-proposal-three-groups-expanded.png` (los tres grupos + una referencia Same-BRDP desplegada), `/tmp/suggest-proposal-ext-no-same-brdp.png` (sin grupo Same BRDP), `/tmp/suggest-proposal-empty-definition-disabled.png` (botón deshabilitado + tooltip) y `/tmp/suggest-proposal-no-references.png` (fallback sin referencias). `npm run lint`/`npm run build` limpios; `uvicorn`/`vite`/los mocks levantados solo para la verificación, parados al terminar.
- **Ronda siguiente: Same BRDP destacado en rojo, Proposal como plantilla (nunca decide), y comprobación de vocabulario contra el esquema real** (`RecordsPage.jsx`/`.module.css`, `src/i18n/index.js`, `src/utils/vocabularyCheck.js` nuevo, `src/api/llmAPI.js`, `backend/scripts/generate_schema_vocabulary.py` nuevo, `public/schema-vocabulary-dita.json` nuevo; sin cambios de backend Python) -- tres piezas del mismo encargo, la tercera bastante más grande que las dos primeras.
  - **1. "Same BRDP in other projects" destacado en rojo**: observado por el usuario que el grupo más relevante (match EXACTO por identificador, no una similitud aproximada) pasaba desapercibido junto a los otros dos. `ReferenceRow` gana un prop `danger` (aplica `.referenceIdentifierButtonDanger`, rojo `#dc2626` -- el mismo hex ya usado para `Refused`, nunca un color nuevo) y el bloque `sameBrdp` de `RecordsPage.jsx` envuelve heading+lista en `.referencesGroupHighlighted` (borde+fondo `#fecaca`/`#fef2f2`, misma paleta de "atención" ya usada en otros puntos de la app) con `.referencesGroupTitleDanger` en el `<h4>`. **Bug real de especificidad CSS encontrado y arreglado en el propio commit** (mismo tipo de bug ya documentado una vez en este fichero para el centrado de cabeceras de `BRDP Projects`): una regla de una sola clase `.xDanger{color:...}` tiene la MISMA especificidad que la regla base de una sola clase que debía vencer, así que gana la que aparezca más tarde en el fichero CSS -- `.referenceIdentifierButtonDanger` se había colocado ANTES de `.referenceIdentifierButton` (que fija `color:#2563eb`), así que el azul seguía ganando en el render real pese a que la clase roja SÍ estaba aplicada en el DOM -- confirmado con `getComputedStyle` antes/después, no solo mirando el CSS fuente. Arreglado con selectores compuestos `.referenceIdentifierButton.referenceIdentifierButtonDanger`/`.referencesGroupTitle.referencesGroupTitleDanger` (especificidad `(0,2,0)`, vence a cualquier regla base de una sola clase sin importar el orden del fichero) -- mismo criterio ya establecido para el bug de centrado anterior, aplicado aquí antes de que llegara a producción.
  - **2. Proposal como plantilla, nunca decide**: observado con Mistral real por el usuario -- para `BRDP-S1-00006` la Proposal copiaba literalmente la lista concreta de Lufthansa en vez de dejarla como hueco; para una BRDP con un elemento inventado, la Proposal se POSICIONABA ("no se utilizará") en vez de dejar la decisión al usuario. El bloque `PROJECT-SPECIFIC VALUES` de `buildSuggestProposalPrompt` (que ya avisaba de no copiar valores concretos como CAGE codes, pero nunca decía "no decidas") se sustituye por el bloque literal del encargo (`DO NOT MAKE THE DECISION`, plantilla rellenable con placeholders `[LIST: ...]`/`[YES/NO]`/`[VALUE: ...]`/`[CHARACTERS: ...]`, ejemplos de otros proyectos permitidos como opciones pero nunca presentados como decisión propia) + la línea "Keep element and attribute names exactly as written in the BRDP's Title and Definition — never rename them." (mismo problema observado con `<pokemon>`→`<stranger>`). La misma línea "never rename" (adaptada a "Title" solo, sin Definition, ya que ese prompt no repite el nombre del campo) se añade también a `buildSuggestDefinitionPrompt`, justo después de "Do not cite specification chapter numbers...", antes del bloque `LANGUAGE:`.
  - **3. Comprobación de vocabulario contra el esquema real** (la pieza grande):
    - **3.1 Fuentes de esquema, investigadas antes de tocar nada**: `sources/D1.3/schema` existe completo en este repo (119 ficheros `.xsd` reales, confirmado por `find`) y cubre DITA 1.3 de punta a punta (topic/concept/task/machineryTask/map/bookmap + dominios + MathML/SVG embebidos) -- ambos standards `DITA 1.3 Xpath2.0`/`Xpath3.0` comparten el mismo vocabulario (el dialecto XPath solo afecta a cómo se escribe la Rule, nunca al vocabulario de elementos). **`SchemasS1000D` NO existe en absoluto en este entorno** -- búsqueda exhaustiva por todo el filesystem (`find / -iname "*SchemasS1000D*"`) sin ningún resultado, y `sources/S3.0.1`/`S4.1`/`S4.2` solo tienen `brex*.xsd`+`dc`/`rdf`/`xlink.xsd` (ningún `descript.xsd`/`proced.xsd`/`ipd.xsd`/`crew.xsd` para ningún Issue) -- mismo tipo de limitación de sandbox ya documentada repetidamente en este fichero (datos de producción no disponibles aquí). Siguiendo la instrucción explícita del encargo ("si falta alguna Issue o está incompleta, no activar la comprobación"): **la comprobación de vocabulario queda activa SOLO para DITA 1.3 (ambos flavors); los 5 standards S1000D (3.0.1/4.1/4.2/5.0/6.0) muestran "not available" siempre**, nunca un falso "no encontrado" -- el mecanismo (`STANDARD_TO_VOCABULARY_FILE` en `vocabularyCheck.js`) es genérico: activar S1000D en el futuro es solo generar su JSON con el mismo script y añadir la entrada al mapa, sin más cambio de código.
    - **3.2 `backend/scripts/generate_schema_vocabulary.py`** (nuevo, reejecutable): en vez de partir de un XSD "entry point" y resolver `include`/`import` recursivamente (frágil -- un include mal resuelto pierde en silencio todo un módulo, justo el tipo de hueco que prohíbe HR7), escanea TODOS los `.xsd` bajo la raíz del esquema y une cada `xs:element`/`xs:attribute` con `@name` DECLARADO (nunca uno con solo `ref=`) -- superset seguro por construcción para una raíz que ya es "el esquema completo" (la propia definición del encargo). `public/schema-vocabulary-dita.json` generado: **767 elementos, 503 atributos**, de 119 ficheros `.xsd` reales (recuento de cordura pedido en el cierre). **Hallazgo real durante la generación, no un bug del script**: `machineryTask` (uno de los 6 `topic_types` que `generateSchematronDITA.js` ya trata como tipo real, confirmado cruzando contra `public/schematron-dita-schema-summary.json`) no aparece NUNCA como nombre de elemento declarado en ningún `.xsd` de este set -- confirmado por grep directo, no asumido. Razón real, no adivinada: `machineryTask.xsd` es un módulo de restricciones (su propio comentario dice literalmente "It must be declared in topic-type shells"), y este set OASIS no trae los XSD "shell" finales que sí declaran `topic`/`concept`/`task`/`map`/`bookmap` como elementos reales. Arreglado con un 4º argumento opcional al script, una allowlist explícita y documentada (`machineryTask`), nunca una invención sin base -- mismo criterio ya establecido para `XPATH3_ONLY_VOCAB`/`XMETAL_AMBITO_MAPA_VOCAB` en rondas anteriores de este mismo generador DITA.
    - **3.3 Extracción de candidatos, dos vías unidas en `src/utils/vocabularyCheck.js`**: (a) determinista, `extractContextCandidates()` -- `<tag>`/`</tag>` → elemento, `@attr` → atributo, la palabra que sigue a `elemento(s)/element(s)/atributo(s)/attribute(s)` saltando conectores (`el/la/los/las/del/de/un/una/unos/unas/the/of/a/an`) con re-anclaje si se encuentra OTRO trigger word mientras se saltan conectores (caso límite exacto del encargo, "atributos del elemento pokemon" → `pokemon`, nunca `elemento`/`atributos`), y palabras en lowerCamelCase (`proceduralStep`) -- estas dos últimas van a un bucket `ambiguous` comprobado contra la UNIÓN de elementos+atributos (3.4), nunca clasificado a ciegas. (b) LLM, `extractVocabCandidatesViaLLM()` -- llamada PREVIA y separada (`temperature: 0`), recibe solo Title/Definition/Proposal, prompt que prohíbe explícitamente juzgar si un nombre existe y nunca incluye el vocabulario real; JSON parseado tolerantemente (`parseVocabExtractionResponse`, acepta un bloque ```` ```json ```` envolvente) y si falla (llamada caída o JSON inválido) degrada a `unavailable:true` sin lanzar. Cacheado en memoria (nunca localStorage, HR1) por `brdpId + hash(title+definition+proposal)` vía `hashVocabInputText()` -- "mismo texto dos veces → una sola llamada", verificado real con un contador dedicado en el mock (`GET /extraction-calls`). Se ejecuta solo al pulsar `Ask`/`Suggest Definition`/`Suggest Proposal` (nunca `Suggest Rule`, ni al teclear).
    - **3.4 Verificación, 100% determinista** (`checkAgainstVocabulary`): el LLM nunca decide existencia, solo el propio código contra `elements`/`attributes` del JSON generado -- dedup por nombre subyacente entre buckets (si el mismo nombre sale como `<tag>` Y como ambiguo, se queda solo la forma `<x>`, más específica).
    - **3.5 UI**: aviso no bloqueante en el panel de la BRDP (bajo el título del asistente, visible para Ask y para los tres Suggest, guardado por `brdpId` para no mostrar un resultado de otra BRDP tras cambiar de fila) -- `"Not found in the {standard} schema: <pokemon>"`, o `"Schema vocabulary check not available for {standard}"` (nunca un falso positivo), más `"Extended name check unavailable"` si (b) falló (mientras (a) sigue funcionando). Los prompts de Ask/Suggest Definition/Suggest Proposal ganan el bloque exacto del encargo cuando hay nombres desconocidos (`buildUnknownNamesBlock`, compartido, nunca Suggest Rule). Pista discreta bajo Title/Definition/Proposal (ambos paneles, crear y editar): "Write element names as `<element>` and attributes as `@attribute`".
    - **Fix incidental necesario para poder testear**: `llmAPI.js` importaba `../services/apiClient` SIN extensión `.js` -- Vite lo resuelve igual, pero Node ESM puro (el mecanismo de test ya establecido en este repo, sin test runner) no puede importar la cadena sin ella. Corregido a `../services/apiClient.js` (cero cambio de comportamiento, confirmado con `npm run build`) para poder testear `vocabularyCheck.js` importando el módulo REAL, no una copia.
    - **Tests**: `scripts/test-vocabulary-check.mjs` (nuevo, 30 aserciones, importa `src/utils/vocabularyCheck.js` real bajo Node plano) cubre el extractor por contexto (los 5 casos límite literales del encargo: `<pokemon>`, `atributos del elemento pokemon`, `el pokemon ese que va dentro del step` → nada desde (a), `decidir si se usa la lista` → nada, `proceduralStep` camelCase), el parseo JSON (válido, envuelto en fence, inválido, forma incorrecta, no-string), la comparación (`<topic>` conocido sin aviso, dedup entre buckets, sin vocabulario → `available:false` sin falsos positivos, bucket ambiguo contra la unión), `mergeCandidates`, el hash de caché, y que el prompt de extracción nunca menciona el vocabulario real. Los tests de los TRES prompts (plantilla, never-rename, bloque de nombres desconocidos) se verifican por el método ya establecido en este repo para funciones privadas de módulo sin test runner JS: capturar el prompt REAL vía `GET /last-request` del mock, no una copia -- ver el script de verificación live.
    - **Verificación real** (`scripts/verify-vocab-check-and-proposal-template.mjs`, nuevo, auto-limpia los 3 proyectos que siembra; `mock-mistral-chat-server.mjs` gana detección de la llamada de extracción por su system prompt distintivo -- nunca por el user message, que es siempre `Title/Definition/Proposal` sin importar qué BRDP -- con `GET /extraction-calls` para contar llamadas reales y reutilizar `/error-next` para el caso de fallo): proyecto DITA 1.3 Xpath2.0 con una BRDP con `<pokemon>` (desconocido) + `@conref`/`<topic>` (conocidos, sin aviso) -- confirma el aviso real, el bloque exacto inyectado en los 3 prompts (Ask/Suggest Definition/Suggest Proposal), 1 sola llamada de extracción tras dos preguntas seguidas sobre el mismo BRDP (caché real), y con `/error-next` armado antes de la llamada de extracción, el aviso real "Extended name check unavailable" con (a) sola siguiendo activa. Proyecto S1000D 4.2 (reutilizando `BRDP-SPCAT-LIVE-001`, sembrado en una ronda anterior) confirma "not available" real, sin ningún falso "no encontrado", y el prompt SIN el bloque de nombres desconocidos. El mismo proyecto S1000D, emparejado con un segundo proyecto que comparte el identificador catalogado, produce un grupo "Same BRDP" real para la captura del highlight rojo. Prompt de Suggest Proposal capturado línea por línea confirma el bloque `DO NOT MAKE THE DECISION` completo (las 4 líneas de ejemplo exactas) y que `PROJECT-SPECIFIC VALUES` ya no aparece. Capturas reales en `/tmp/vocab-check-unknown-name-banner.png` (aviso real + pista de campos), `/tmp/vocab-same-brdp-highlighted-red.png` (grupo Same BRDP en rojo real, con `getComputedStyle` confirmando `rgb(220, 38, 38)` tanto en la cabecera como en el identificador de la fila, más un fondo teñido real) y `/tmp/vocab-check-extraction-unavailable.png`. **Regresión completa**: `verify-suggest-proposal-corpus.mjs` (con su aserción de `PROJECT-SPECIFIC VALUES` corregida al nuevo bloque), `verify-suggest-definition-lang-wrap-dedup.mjs`, `verify-ask-question-standard-in-prompt.mjs` y `verify-suggest-definition-reference-expand.mjs` re-ejecutados en verde sin cambios -- confirman que añadir la llamada de extracción ANTES de la llamada principal de Ask/Suggest no rompió ningún flujo existente (la petición capturada por el mock sigue siendo siempre la ÚLTIMA, la principal, nunca la de extracción). Suite backend completa (276 tests, sin cambios de backend Python en esta ronda) y `npm run lint`/`npm run build` limpios; `uvicorn`/`vite`/los mocks levantados solo para la verificación, parados al terminar.

## Qué es esta app

BRDP Manager es una app React + Express para gestionar Business Rules Decision Points (BRDPs) de proyectos S1000D/DITA. Sus funciones principales son:

- CRUD de BRDPs con persistencia en SQLite
- Generación de BREX DM (S1000D 4.2 y 3.0.1) y Schematron 1.0 vía LLM
- Extracción de BRDPs desde documentos (DOCX/PDF) o texto pegado (AI Extract)
- Asistente AI con contexto del dataset de BRDPs

## Arquitectura

```
Browser (React) → Express server.js (puerto 3000)
                  ├── GET /*           → sirve dist/ (Vite build)
                  ├── POST /api/proxy  → LLM externo (Mistral/Anthropic/OpenAI)
                  ├── /api/brdps       → SQLite (better-sqlite3)
                  ├── /api/config      → SQLite
                  ├── /api/settings    → SQLite
                  └── /api/notes/:id   → SQLite
```

### Modos de ejecución

- **Desarrollo**: `npm run dev` → Vite dev server en puerto 5173, proxy Vite para Mistral
- **Producción**: `npm run build && npm start` → Express en puerto 3000

### Detección de entorno en frontend

```javascript
if (import.meta.env.PROD) {
  // llama a /api/proxy (Express)
} else if (import.meta.env.DEV) {
  // llama a /mistral-proxy (Vite proxy)
}
```

### Nota de entorno: `better-sqlite3` (módulo nativo)

`better-sqlite3` se compila de forma nativa. En Node muy reciente (24+) puede no haber binario precompilado y fallar el build (requiere C++ build tools). Soluciones: `npm install better-sqlite3@latest` (trae prebuilt para Node nuevo) o usar Node LTS 20/22. No es un problema del código de la app.

### Nota de entorno: proxy corporativo con inspección SSL (ATEXIS)

Dos problemas relacionados pero distintos, ambos con la misma causa raíz (el proxy corporativo con inspección SSL — confirmado Zscaler — re-firma el tráfico HTTPS con su propio CA raíz, que ni npm ni Node reconocen por defecto). Detalle completo orientado al usuario en el README ("Troubleshooting: Corporate Network / SSL-Inspecting Proxy"):

1. **`npm install` falla con `UNABLE_TO_GET_ISSUER_CERT_LOCALLY`** — afecta a cualquier instalación o adición de dependencia futura. Es un problema de la propia herramienta npm, no del código de la app. Fix permanente: `npm config set cafile "ruta\al\certificado-corporativo.pem"` (pedir el `.pem` del CA raíz a IT, o exportarlo de `certmgr.msc` → Entidades de certificación raíz de confianza). Fix rápido/temporal si no se tiene el `.pem` a mano: `npm config set strict-ssl false` → `npm install` → `npm config set strict-ssl true` inmediatamente después — nunca dejarlo desactivado.
2. **La propia app (`npm start`) falla con el mismo error de certificado** — problema distinto: Node en runtime no usa la config de npm ni el almacén de certificados de Windows por defecto. **Ya resuelto en el código** vía `win-ca` (`server.js`, se activa solo si `process.platform === 'win32'`, no-op en Linux/Mac) — no requiere ninguna acción manual, a diferencia del problema 1.

## Ficheros clave

| Fichero | Responsabilidad |
|---|---|
| `server.js` | Express: static files + proxy LLM + REST API SQLite |
| `src/db/database.js` | Conexión SQLite con better-sqlite3, WAL mode |
| `src/db/schema.sql` | Definición de tablas: brdps, config, settings, notes |
| `src/services/api.js` | Capa de servicio frontend: fetch a endpoints REST |
| `src/api/generateBREX.js` | Generador BREX S1000D 4.2 + helpers compartidos |
| `src/api/generateBREX41.js` | Generador BREX S1000D 4.1 |
| `src/api/generateBREX301.js` | Generador BREX S1000D 3.0.1 |
| `src/api/generateBREXSch.js` | Generador Schematron 1.0 — S1000D (enfoque A2, ver abajo) |
| `src/api/brexToSchematron.js` | Conversor determinista BREX → Schematron (sin LLM) |
| `src/api/generateSchematronDITA.js` | Generador Schematron 1.0 — DITA (pipeline directo LLM, ver abajo) |
| `src/api/buildBREXdocReport.js` | Constructor del informe BREXdoc |
| `src/api/extractBRDPs.js` | AI Extract: DOCX/PDF/texto plano → BRDPs |
| `src/api/llmAPI.js` | Cliente LLM agnóstico (Anthropic/OpenAI/Mistral/Custom) |
| `src/context/BRDPContext.jsx` | Estado global de BRDPs + carga desde API |
| `src/hooks/useBRDPs.js` | Hook de BRDPs (usa BRDPContext internamente) |
| `src/hooks/useAPIKey.js` | Hook de configuración AI (lee/escribe settings API) |
| `src/hooks/useProjectConfig.js` | Hook de configuración del proyecto (lee/escribe config API) |
| `src/hooks/useLocalNotes.js` | Hook de notas (sync con API, fallback localStorage) |
| `src/components/GenerateModal.jsx` | Modal de generación: selector de formato + llamada al generador |
| `src/components/AIExtractModal/` | Modal de AI Extract (fichero o texto plano) |

## Schema JSON (few-shot para LLM)

Los generadores de BREX cargan su estructura + ejemplos few-shot desde `public/`:

- `brex-schema-summary-4-2.json` — estructura 4.2 + ejemplos few-shot
- `brex-schema-summary-4-1.json` — estructura 4.1 + ejemplos few-shot (reutiliza los mismos few-shot examples que 4.2 — son datos planos BRDP→XPath, no dependen del issue)
- `brex-schema-summary-3-0-1.json` — estructura 3.0.1 + ejemplos few-shot
- `brex-schema-summary-sch.json` — **ya no se usa** (el Schematron se genera por conversión determinista, no por LLM directo). Se conserva por si se quisiera retomar la generación directa.

Los ejemplos few-shot van en `few_shot_examples` y se inyectan en el system prompt vía `buildBREXPrompt*()`. El schema JSON completo se serializa SIN el array few-shot (para no duplicar tokens); los ejemplos van en un bloque separado.

- `public/schematron-dita-schema-summary.json` — equivalente DITA, con estructura distinta (no hay `dmodule_opening_tag` ni `objectPath`): `sch_header` (esqueleto determinista del `<sch:schema>`), `topic_types` (mapa estructural de los 6 tipos: topic/concept/task/machineryTask/map/bookmap, confirmado contra los XSD reales de `sources/D1.3/schema/`), `vocabulary_by_domain` (content models reales por dominio DITA — hazard-d, taskreq-d, hi-d, ut-d, elementos base — usado también por el lint de vocabulario, ver abajo) y `few_shot_examples` (29 reglas Schematron reales, validadas en oXygen).

## Generadores BREX / Schematron — arquitectura defensiva

Los generadores (4.2, 4.1, 3.0.1, Schematron vía 3.0.1) comparten la **misma arquitectura defensiva**, diseñada para que con datasets grandes (+400 BRDPs) el LLM no trunque el XML, no invente IDs, no salte BRDPs ni produzca XML/XPath inválido.

### Patrón común

1. **Chunking** — `CHUNK_SIZE = 10` BRDPs por llamada LLM, `MAX_RETRIES = 2`.
   - Chunk 1: genera el DM completo (header + reglas del chunk).
   - Chunks 2..N: generan solo las reglas.
   - Un chunk con respuesta vacía NO se descarta: sus BRDPs pasan al reintento individual.
2. **Verificación por chunk** — detecta reglas faltantes e inventadas; elimina inventadas; reintenta faltantes individualmente.
3. **Barrido final de cobertura** — tras procesar todos los chunks, recalcula qué BRDPs faltan en el documento completo y los reintenta.
4. **Red de seguridad anti-pérdida** — si un BRDP sigue sin poder generarse como regla, se emite como entrada de trazabilidad para que NUNCA desaparezca en silencio. La cobertura siempre es total.
5. **Finalización determinista** — una pasada final que corrige de forma determinista lo que el LLM tiende a romper (ver por versión).

Helpers compartidos: `extractXML()` y `checkWellFormed()` se importan SIEMPRE desde `./generateBREX.js`. NUNCA duplicarlos.

### BREX 4.2 (`generateBREX.js`)

- Elemento de regla: `<structureObjectRule>`; ref BRDP: `<brDecisionRef brDecisionIdentNumber="..."/>`; flag: `allowedObjectFlag` (0/1/2) en `objectPath`.
- Reglas sin contexto: elemento formal `<nonContextRule>` (con `simplePara`). El `id` es `xs:ID` (único en TODO el documento).
- Red de seguridad: `<nonContextRule>` formal.
- `finalizeDocument(xml, projectConfig, schemaSummary)` aplica, en orden:
  - `forceDmoduleTag` — fuerza el tag de apertura de `dmodule` (evita corrupción de namespace por el LLM).
  - `fixFlagPlacement` — mueve `allowedObjectFlag` de `structureObjectRule` a `objectPath`.
  - `promoteOrphanSplitRules` — sufijos de split (`-b`/`-c`) sin base → renombra a base.
  - `forceDmCodeFields` (`resolveDmCodeFields`) — fuerza atributos del `dmCode`: respeta `modelIdentCode`/`systemDiffCode` de config (en mayúsculas) y hardcodea el resto (`systemCode=00`, `disassyCodeVariant=0A`, `itemLocationCode=D`, etc.).
  - `dropRedundantNonContextRules` — si un BRDP existe como `structureObjectRule`, elimina su `nonContextRule` homónimo (evita id `xs:ID` duplicado).
  - `dedupeNonContextRules` — dedup de `nonContextRule` por id.

### BREX 4.1 (`generateBREX41.js`)

- Mismo mecanismo core que 4.2 (`<structureObjectRule>`, `allowedObjectFlag` 0/1/2 en `objectPath`, `<nonContextRule>` formal) — confirmado idéntico contra el XSD real (`sources/S4.1/brex4.1.xsd` vs `sources/S4.2/brex.xsd`).
- **Sin `brDecisionRef` ni `brSeverityLevel`** (no existen en el XSD 4.1, confirmado con cero apariciones): la trazabilidad BRDP→regla es solo el atributo `id` de `structureObjectRule`/`nonContextRule`. Todas las funciones de `generateBREX.js` que hardcodeaban estas referencias (plantillas few-shot, `splitMultipleObjectPaths`, el fix-up de `<brDecisionIdentNumber>`, el "safety net" de `nonContextRule`) se adaptaron quitándolas por completo — NO solo renombrándolas.
- Todas las funciones internas llevan sufijo `41` (mismo patrón que `301` en `generateBREX301.js`): `buildBREXPrompt41`, `buildBREXPromptChunk41`, `finalizeDocument41`, etc.
- `extractXML()` y `checkWellFormed()` se importan desde `./generateBREX.js` (no se duplican), igual que hace `generateBREX301.js`.
- `finalizeDocument41(xml, projectConfig, schemaSummary)` aplica el mismo orden que 4.2 (`forceDmoduleTag41`, `fixFlagPlacement41`, `promoteOrphanSplitRules41`, `forceDmCodeFields41`/`resolveDmCodeFields41`, `dropRedundantNonContextRules41`, `dedupeNonContextRules41`) — ninguna de estas funciones tocaba `brDecisionRef`/`brSeverityLevel` en origen, así que no necesitaron más cambio que el renombrado.

### BREX 3.0.1 (`generateBREX301.js`)

- Elemento de regla: `<objrule>`; flag: `objappl` (0/1, NO 2) en `objpath`; header `avee` con elementos hijos de patrón estricto.
- NO existe `nonContextRules` en el XSD 3.0.1 → las reglas sin contexto se representan como comentarios XML `<!-- nonContextRule id="...": ... -->`.
- Red de seguridad: comentario `nonContextRule` (no elemento).
- `finalizeDocument301(xml, projectConfig, schemaSummary)` aplica: `forceDmoduleTag301`, `fixObjapplPlacement301`, `promoteOrphanSplitRules301`, `forceAveeFields301` (`resolveAveeFields301`), `dedupeNonContextComments301`.

### Schematron 1.0 — S1000D (`generateBREXSch.js` + `brexToSchematron.js`) — enfoque A2

El Schematron NO se genera con el LLM directamente (era frágil). En su lugar:

1. `generateBREXSch` reutiliza internamente `generateBREX301` para producir un BREX 3.0.1 (con toda su robustez).
2. Lo convierte a Schematron ISO con `brexToSchematron()`, que es **100% determinista** (port del XSL de referencia de Docuneering, Apache-2.0).

`brexToSchematron.js` es robusto frente a XPaths complejos generados por LLM:

- `_isSafePattern(ctx)` — valida que un `context` sea un patrón XSLT legal (balance de `()`/`[]`, sin `..`/ejes inversos como paso del patrón, sin operadores colgando).
- `_splitTopLevel(path)` — split consciente de la profundidad de corchetes para separar parent/step (evita romper rutas con `/` dentro de predicados).
- Si el `context` calculado no es un patrón válido, hace fallback a `context="/dmodule"` y mueve la ruta al `test` (donde `..`/ejes inversos SÍ son válidos como XPath).
- `_buildHeader(brexXml)` — declara dinámicamente los namespaces que use el BREX (p. ej. `ns2`), además de los base.

Opciones de `brexToSchematron(brexXml, options)`: `preserveBrdpId` (usa el id del BRDP en el assert en vez de uno secuencial) y `carryComments` (arrastra los comentarios `nonContextRule` del BREX 301 al `.sch` como trazabilidad).

El motor `brexToSchematron.js` es independiente y reutilizable (p. ej. para un futuro botón de migración BREX→Schematron sobre un BREX subido por el usuario).

### Schematron 1.0 — DITA (`generateSchematronDITA.js`) — pipeline directo, sin BREX

DITA no tiene un equivalente a BREX, así que aquí el pipeline es **directo**: BRDP → LLM → Schematron final, sin conversión determinista intermedia (a diferencia de S1000D, que pasa por BREX 3.0.1 → `brexToSchematron()`). Esto hace que la validación post-generación sea la única red de seguridad real.

- **Opción B**: un único `.sch` combinado por proyecto con múltiples `<sch:pattern>`, cada uno auto-limitado por su propio `context` XPath — no hay detección explícita de tipo de topic; una regla simplemente no dispara si su contexto no existe en el documento validado.
- Mismo patrón de chunking/verificación/reintento/barrido de cobertura que `generateBREX.js` (`CHUNK_SIZE=10`, `MAX_RETRIES=2`), simplificado: como cada chunk solo emite bloques `<sch:pattern>` autocontenidos, no existe el caso especial "chunk 1 = documento completo" de BREX; el ensamblado es un simple array de bloques.
- El header `<sch:schema>` se añade de forma determinista al final (`finalizeSchematronDocument`), nunca lo genera el LLM.
- **Red de seguridad**: si un BRDP no tiene gancho estructural real en DITA, se emite un comentario XML de trazabilidad (mismo patrón real usado para desactivar `BRDP-D1-00089` en el dataset curado) — nunca se fuerza ni se inventa una regla.
- `checkWellFormedSchematron(xml, schemaSummary)` — implementación propia sin `DOMParser` (funciona igual en navegador y en Node, útil para test scripts aislados), con 7 comprobaciones: balance de tags, `<sch:schema>` raíz correcto (namespace + `queryBinding="xslt2"`), ids de `sch:assert`/`sch:report` únicos en TODO el documento, `context` no vacío y patrón XSLT válido (reutiliza `_isSafePattern()`, exportado desde `brexToSchematron.js`), `test` no vacío + cada `sch:rule` con al menos un `sch:assert`/`sch:report`, `role` restringido a valores conocidos, sin placeholders — más un **lint de vocabulario no bloqueante**: compara los nombres de elemento/atributo usados en `context`/`test` contra `vocabulary_by_domain` del schema summary y avisa (sin bloquear) si alguno no está confirmado contra el XSD real.
- `options.callLLM` y `options.schemaSummary` son inyectables (no solo vía `fetch`/`sendMessageStream`), pensado para poder testear el generador de forma aislada fuera de la app.

## AI Extract (`extractBRDPs.js` + `AIExtractModal.jsx`)

Extrae BRDPs desde un documento o texto pegado.

- **Entrada (modo único, sin tipos de documento):** fichero `.docx`/`.pdf` O texto plano pegado. En el modal, `inputMode` ∈ `'file' | 'text'`; si hay `rawText`, tiene prioridad sobre el fichero.
- **Extracción de texto:**
  - DOCX → `mammoth.extractRawText`.
  - PDF → `pdfjs-dist` (paquete npm, NO CDN) con worker local vía Vite (`pdfjs-dist/build/pdf.worker.min.mjs?url`). Importante: NO volver a cargar pdf.js por CDN con `import()` dinámico — la build UMD deja `GlobalWorkerOptions` undefined y rompe la lectura.
- **Pipeline:** texto → chunks (6000 chars, overlap 600) → `buildExtractionPrompt(chunkText)` (prompt único) → `sendMessage` (temp 0.2) → parseo JSON → dedup por similitud → IDs secuenciales `BRDP-EXT-NNNNN`.
- **IDs de origen:** el prompt único pide que, si el texto trae un identificador de regla (`BRDP-S1-...`, `BR002`, etc.), se mencione en el campo `comment` (NO se conserva como id; los ids de salida son automáticos).
- **Validación de texto pegado:** máximo 3000 caracteres (aproximadamente una página). Textos más largos deben subirse como fichero `.docx` o `.pdf`.
- **Errores:** los fallos de chunk NO se tragan en silencio — se registran (`console.error`) y, si no se extrae nada, se propaga el primer error real.

## Patrones a seguir

### Hooks de datos

> ⚠️ **Este patrón viola HR1 del AACF** ("no localStorage/sessionStorage para estado autoritativo o persistente — Postgres es la fuente de verdad"). Se documenta tal cual está implementado hoy, no como recomendación a seguir en código nuevo. Ver "Auditoría de cumplimiento AACF (pendiente)" al final de este fichero — la reconciliación se hace en una ronda dedicada, no de forma ad hoc.

1. Estado inicial desde localStorage (carga instantánea).
2. `useEffect` que hace fetch a la API al montar (fuente de verdad).
3. Saves van a la API + localStorage en paralelo.
4. La interfaz pública del hook no cambia aunque cambie el backend.

NUNCA acceder a localStorage directamente desde componentes — siempre usar los hooks.

### Proxy LLM

El frontend siempre manda a `/api/proxy` en producción. El body tiene esta forma:

```javascript
{ targetEndpoint, apiKey, provider, payload }
```

donde `payload` es el body ya construido. El servidor Express solo añade headers de autenticación y reenvía.

### Base de datos

- Motor: SQLite vía `better-sqlite3` (síncrono), WAL mode.
- Fichero: `data/brdp.db` (ignorado en git).
- Tablas: `brdps`, `config`, `settings`, `notes`.
- Campo `comments` en DB = campo `comment` en frontend (compatibilidad legacy). `GET /api/brdps` devuelve ambos.

### Selectores de versión en GenerateModal

```javascript
const isBREX42 = format === 'BREX — S1000D 4.2';
const isBREX41 = format === 'BREX — S1000D 4.1';
const isBREX301 = format === 'BREX — S1000D 3.0.1';
const isSchS1000D = format === 'Schematron 1.0 — S1000D';
const isSchDITA = format === 'Schematron 1.0 — DITA';
```

Schematron 1.0 se divide en dos formatos independientes en el selector: "Schematron 1.0 — S1000D" (implementado, `generateBREXSch.js`) y "Schematron 1.0 — DITA" (implementado, `generateSchematronDITA.js` — ver "Schematron 1.0 — DITA" arriba). A diferencia de S1000D, DITA no usa `xmllint-wasm` ni validación XSD/DTD en tiempo real (decisión explícita: no es necesaria); la única validación post-generación es `checkWellFormedSchematron()`.

Añadir un nuevo formato BREX requiere: nuevo generador + schema JSON + rama en `handleGenerate()` + actualizar `disabled`/"Coming soon".

### Guardia de validación en el chat

`useChat.js` intercepta en `sendUserMessage()` los mensajes con triggers de cambio de estado (`validationTriggers`) y responde sin llamar al LLM. Segunda capa de restricción en el `basePrompt`.

## Lo que NO está implementado todavía

- S1000D 5.0, 6.0 (selector existe, botón deshabilitado con "Coming soon").
- Botón de migración BREX→Schematron sobre un BREX subido (el motor `brexToSchematron.js` ya está listo; falta la UI).
- Migración automática de localStorage a SQLite en primera ejecución.
- Autenticación (no necesaria para uso local single-user).
- Docker con SQLite (el docker-compose actual usa nginx sin backend).

## Auditoría de cumplimiento AACF (pendiente — no tocar sin luz verde)

Primera pasada de lectura completa del AACF (2026-09-18) contra el código real de `v2-multiproyecto`. Esto son **hallazgos, no fixes** — se abordarán en una ronda dedicada cuando el usuario dé la prioridad; no se ha modificado ningún fichero de código fuente en esta ronda.

### HR1 — localStorage como estado autoritativo (conflicto confirmado)

Contradice tanto la Hard Rule HR1 como `SECURITY_CONTEXT.md` del propio framework ("no data persists in localStorage/sessionStorage — database only"). Afecta a:

- `src/context/BRDPContext.jsx` y `src/hooks/useBRDPs.js` — cachean el dataset completo de BRDPs.
- `src/hooks/useProjectConfig.js` — cachea la config del proyecto.
- `src/hooks/useLocalNotes.js` — notas.
- `src/hooks/useAPIKey.js` + `src/components/AIExtractModal/AIExtractModal.jsx` — **guardan la API key del LLM en localStorage** (además de HR1, roza la gestión de secretos de `ai-output-safety.mdc`: un secreto persistido sin cifrar en el navegador).
- `src/layouts/AppLayout.jsx` (`sidebarCollapsed`) — preferencia de UI pura, probablemente el único caso defendible tal cual (no es "estado autoritativo o persistente" en el sentido de la regla).

El propio patrón "Hooks de datos" de la sección "Patrones a seguir" de este fichero es la causa raíz: documenta explícitamente el patrón que el AACF prohíbe. Al reconciliar: la API (Postgres) debe quedar como única fuente de verdad; localStorage, si se conserva para algo, solo como cache explícitamente no autoritativo (con invalidación clara), nunca como estado del que la app depende para funcionar sin red.

### Otros puntos a revisar (menor prioridad, sin confirmar aún como violación real)

- **HR20 (UI de mutación optimista)** — no verificado todavía si las mutaciones de BRDP reflejan el cambio antes de la respuesta del servidor o esperan al round-trip completo.
- **HR21 (humanizar texto de UI)** — parcialmente cubierto (hay capa i18n real, ver `src/i18n/index.js`), pero no se ha auditado si se renderiza en algún sitio un token crudo (enum, slug, snake_case) sin pasar por humanización.
- **AI Extract / HR6** — el límite de 3000 caracteres en texto pegado es una validación de entrada con escalado explícito al usuario (subir como fichero en su lugar), no un truncado silencioso de contenido de calidad — probablemente conforme con HR6 tal cual está, pero pendiente de una revisión más amplia por si hay otro punto del código que sí trunque contenido en vez de resumir.
- **Design system (`styles/design-system.md`, `ui-kit.md`, `branding.md`)** — el AACF asume shadcn/ui + Tailwind + tokens DTCG (OKLCH) + Zustand como "golden path" de UI; este proyecto no usa nada de eso. No es en sí una violación de una Hard Rule, pero es una divergencia del framework que el usuario debe decidir si adoptar (migración de UI, coste alto) o mantener como excepción documentada.

### Nota de seguridad fuera del alcance de este repo (informativa)

Al leer `aacf/agents/codebase-hardening.agent.md` del repo `cursoFSD` (público en GitHub) se encontró una API key en texto plano embebida en el fichero (`RAG_MCP_KEY` para el MCP de políticas corporativas en `10.117.139.1:8200`). No se ha usado ni se usará esa clave desde aquí. Se avisó directamente al usuario en la conversación; no aplica ninguna acción sobre `brdp-manager`, se deja constancia aquí solo para no perder el hallazgo.
